# WorkFlow-Context — AI-agent context for this framework

> **Latest review (2026-08-28):** Claude-side review after the Codex and OpenCode passes. The mirror, hooks, script syntax and all `opencode.jsonc` refs are clean, but **`tf-goal.sh` never turned YOLO off** — the flag it writes survived every exit path, and this repo had been silently running with delete-prompts suppressed since a `tf-goal.sh` smoke test on 2026-08-24; fixed with a source-scoped exit trap and the stale flag cleared. Three more: an unescaped-backtick command substitution that *executed* `.codex/rules/techieflow.rules` on every Codex goal run, a Codex-only git policy that the 2026-08-28 (4) pass had applied to all three harnesses' shared preamble, and the stale loose `.claude/commands/generate-html.md` (left for the owner by the previous session) — which `update-framework.sh` ships to every app, so it is now regenerated verbatim from the canonical task. Then the owner rejected `cost_attribution:"none"` on the session's own miss records and was right: `tf-emit.sh` derived the cost divisor from `reqs_touched`, which is **empty in every `framework` and `docs` repo**, so a measured token window was thrown away as unattributable and the cost of a framework fixing its own misses was structurally zero. The divisor is now the number of misses a run closed, recomputed at report time — which re-costs the existing records without editing one. Also fixed the `claude`-launch permission-rule warning (`Bash(ls *.md …)` → `Bash(ls:*)`), and **propagated everything to all 19 repos**. See §5's newest entry. Earlier the same day: OpenCode-harness review regenerated the stale loose `/generate-html` wrapper (`.opencode/command/generate-html.md`) — it still taught the hand-authoring path TF-003 removed. The session ALSO edited the Claude twin `.claude/commands/generate-html.md`, which was outside its OpenCode-only mandate; **reverted byte-exact at the owner's instruction** and logged as a miss (`instruction-ignored`). The Claude wrapper's staleness is real but left for the owner — see §5's newest entry. Earlier the same day: the Codex adapter audit fixed nested-directory hook launch, the protected-document `apply_patch` deletion bypass, stale unattended CLI syntax, and contradictory Codex YOLO git wording; its exact persisted conversation token window is recorded directly on the four existing `miss-fix` records. See §5's newest entries.

> **AI agent: read this file FIRST.** It is the single, always-current briefing on what this repository is, how the framework is meant to be used, what has been done to it, and what is still open. You do **not** need to reverse-engineer the framework from the command/task files and `WORKFLOW.html` every session — start here, then open only the specific files this doc points you at. **Keep this file updated** (see [§7 Maintenance contract](#7-maintenance-contract)); it is maintained only in this repository.

**Repo:** dual-homed since 2026-07-11 — `/Users/MyCode/TechieFlow` on the owner's Mac (it was `/Volumes/MacD/MyCode/TechieFlow` until the 2026-08-11 session found it here; app clones sit beside it as `/Users/MyCode/<App>`) AND `/mnt/c/3AIGenCode/TechieFlow` on Windows/WSL, synced via GitHub (the framework *template*, not an app). **Last updated:** 2026-08-28 (**Claude-side review after the Codex + OpenCode passes — `tf-goal.sh` never cleared the YOLO flag it wrote, so a goal run left the repo permanently in YOLO (this one had been, since 2026-08-24); a source-scoped `trap … EXIT INT TERM` now clears it. Plus: unescaped backticks in the Codex policy note executed `.codex/rules/techieflow.rules` instead of naming it; the shared goal preamble had been rewritten to Codex's strict no-git policy, which is wrong for Claude and OpenCode; and the stale loose `.claude/commands/generate-html.md` — which `update-framework.sh` pushes to every app — regenerated verbatim from the canonical task. The mirror, the 8 settings.json hooks, every script's syntax, and all 31 `opencode.jsonc` refs verified clean. Owner-reported CLI warning fixed: `Bash(ls *.md *.slnx global.json)` → `Bash(ls:*)`. See §5's newest entry**) · earlier same day (**OpenCode-harness review — the stale loose `/generate-html` wrapper `.opencode/command/generate-html.md` regenerated verbatim from the canonical task; it still taught the Write-tool hand-authoring path TfLens TF-003 removed on 2026-08-27, and it is a top-level OpenCode command an owner types directly. The session also overreached its OpenCode-only mandate and edited the Claude twin `.claude/commands/generate-html.md` — reverted byte-exact at the owner's instruction and logged as a miss; that wrapper's staleness is real but is the owner's to fix. Everything else checked clean: all `opencode.jsonc` `{file:}` refs resolve, the Claude mirror is byte-identical, every public task is registered, the plugin + 19 generated skills are current. See §5's newest entry.** Earlier 2026-08-28: **`miss-amend` — the append-only rule finally has a record kind to name, plus a content-based checklist guard** — TfLens filed **TF-005**: `_metrics-emit-gate` constraint 5 says *"the correction is a new record, never an edit"*, and `misses.jsonl` had **no kind that could carry one** — `miss` opens, `miss-fix` closes, and re-emitting is barred by the §5.5.4 collapse rule, so a field left `null` (their case: a `why_missed` on a record written four minutes before the field shipped) was unreachable: leave it empty forever or break the rule. **The rule named a remedy the stream did not implement.** Fixed with a third kind, **`miss-amend`** (SCHEMA §5.5.7) + `tf-emit.sh --amend <miss_id> <field> <value>`: it may set a field that is `null` and **never** overwrites one that is not, so it *completes* a record rather than altering a fact — append-only in substance, not just in form, and a reader that ignores amendments still sees nothing false. The boundary that makes it safe: **a judgement may be completed, an observation may not** — `why_missed` is a classification a reader can still make honestly next week; a gate verdict is a fact about a finished run (§3.5's rule from the other side). Everything the emitter derives is excluded outright, so it can never become a back door onto attribution or cost. Both doors enforce it (the `--amend` helper *and* a hand-written record on the piped path), refusals print instead of failing quietly, and everything still exits 0. Also took their cheaper alternative **as well**: `tf-metrics.sh` gained **`FIELD_SINCE`** beside `LATE_GATES`, so a miss written before an optional field existed leaves that field's denominator with the count stated — otherwise every future field makes the practice look worse the day it ships. Constraint 5 now names *which* record kind carries which correction and says to **report a missing path rather than edit the file**. Same pass, **TF-004**: the HTML renderer refused any `*-Checklist.md`, including a human deployment runbook, pushing agents back toward hand-authoring — now content-identified (`## Requirements Status` / the `SINGLE SOURCE OF TRUTH` marker), verified against TfLens's real checklist and a runbook. See §5's first 2026-08-28 entry) · previously same day (**MISS TELEMETRY — a fifth stream, `docs/metrics/misses.jsonl`** — the owner reported that agents miss requirements in build *and design*, that there was no way to document those misses, and no way to see what fixing them cost. `gates.jsonl` only fires at verify (so design-phase misses were invisible end to end), `escaped` is a detection axis rather than an attribution one, there was no unit of work called "a miss", and run-level tokens could not be split across the REQs a fix run touched. New stream carries **two record kinds** — `miss` and `miss-fix`, linked by `miss_id` — so a lifecycle is recorded without ever rewriting a record. Three moves carry it, each removing a judgement from the agent: **attribution is looked up, never typed** (`tf-emit.sh` resolves `origin_model`/`origin_harness`/`origin_confidence` from `runs.jsonl` and forces the model to `null` when it can't, overwriting whatever was supplied), **cost is copied and never silently apportioned** (`cost_attribution` = `sole`|`shared:n`|`none`; measured and apportioned print as separate columns with no code path that blends them), and **one defect is one miss** (`--open-miss` collapse check). Money, honestly: OpenCode gives real dollars, Claude Code and Codex give tokens and `cost_usd:null` permanently — no rate card, ever. New **`*log-miss`** command is the 20-second front door (no boot, no repro, no code) because the friction of `*triage-issues` is exactly what stopped one-line misses being recorded at all. Also fixed a latent defect found on the way in: the framework repo could not record its own `project_type` without misclassifying **every app scaffolded from it** (the scaffolds rsync `core-config.yaml` verbatim), so `framework` is now detected structurally in `tf-emit.sh`/`tf-metrics.sh` and written nowhere. See §5's 2026-08-28 entry) · previously 2026-08-27 (**python3 is now a hard prerequisite in all three scripts** — a Mac scaffold without it used to "succeed" while leaving the repo with no Codex bindings and no working guard hooks (they fail OPEN) — **and the OpenCode/Codex claims were verified against the real binaries for the first time** (opencode 1.18.19, codex-cli 0.149.1): all three OpenCode claims confirmed, but `tf-goal.sh` was calling `codex exec --ask-for-approval`, which is not a flag on that subcommand — the Codex YOLO path had never run. Fixed to `-c approval_policy="never"`. Whether `Stop` is a valid Codex hook event is OPEN. See §5's first 2026-08-27 entry) · same day (**TfLens framework feedback closed out + root `opencode.jsonc` finally refreshes** — TF-003 shipped `.tfcore/utils/tf-render-html.sh`, a dependency-free renderer that extracts the CSS/JS out of `html-render-shell.md` at render time (agents had been hand-authoring every HTML page from a 494-line spec, ~75–80k output tokens per phase); TF-001 added `dedupe_sessions()` to `tf-metrics.sh`, which had been overstating every session and token figure; TF-002 gave `tf-perf.sh` `--cookie`/`--header` and made an all-redirect run a refusal instead of a 4.1 ms "page load"; and the root `opencode.jsonc` now refreshes — the preserve rule was protecting project content that exists in ZERO of 10 apps while leaving two of them with dead `{file:}` refs that hard-fail OpenCode's whole config load. See §5's first 2026-08-27 entry) · same day (**the framework tree is invisible to agent file-search and nothing ever said so** — `.tfcore/` is both a hidden dot-directory and gitignored in every app, so Grep/Glob/`rg --hidden`/`git grep` all return nothing for files that are plainly there; a TfLens verify run consequently recorded `tf-metrics.sh` as "not present anywhere in this tree" and closed a BRD acceptance gate on it. Canonical rule added to `_status-update-gate.md` §"The framework tree is INVISIBLE to search" — literal-path checks, `rg -uu`, and a bar on writing "not present" into any doc without trying the path — restated in `verify-phase.md` and `_metrics-emit-gate.md`, plus both app templates for new projects. See §5's first 2026-08-27 entry) · previously 2026-08-25 (TrBlazeUI 2.0.3 verified published on the GitHub feed; `tf-codex-bind.py` now honours the package's Codex ownership marker so the compat wrapper stops blocking the native agent — see §5's first 2026-08-25 entry) · previously 2026-08-24 (NuGet persona shim made legacy-rescue-only — it used to `cp` the legacy `.claude/<lib>.md` over the current `.claude/commands/<lib>.md`, i.e. stale over fresh; latent, 0 of 16 repos affected — see §5's first 2026-08-24 entry) · same day (`update-framework.sh` stopped false-warning that `opencode.jsonc` is "missing" framework instructions — the check predated the 2026-08-20 two-file split and audited the preserved per-project root file for content that now ships in the framework-owned `.opencode/opencode.jsonc`; fired on 8 of 12 apps — see §5's first 2026-08-24 entry) · same day (routed library-persona wrappers fixed — they pointed at `.claude/<lib>.md`, a path the libraries stopped deploying to on 2026-07-04, so `/trblazeui` and `/techierag` sub-agents dead-stopped in every routed app; all three harness generators now resolve an ordered path chain — see §5's first 2026-08-24 entry) · same day (Codex adapter implementation and documentation synchronization — see §5's other 2026-08-24 entries) · previously 2026-08-21 (advisory escalation policy, `set-escalation`, and per-run `attempt` telemetry) · previously 2026-08-20 (harness-adapter design FULLY IMPLEMENTED across four passes: guard bridge, P-0 fix, D-1 propagation, S-1 vocabulary, routing.yaml + tf-harness.sh + tf-routing-bind.sh, per-run telemetry enrichment, AGENTS.md context layer, all 18 repos — see §5's four 2026-08-20 entries) · previously 2026-08-19 (harness-adapter design session — five design docs + DECISIONS entry, nothing implemented; see §5's 2026-08-19 entry) · previously 2026-08-13 (OpenCode-harness corrections — see §5's 2026-08-13 entry) · previously 2026-08-11 (**`commits.jsonl` friction fixed, not explained away, and `.gitattributes` now pins LF in app repos** — the owner rejected the 2026-08-10 "by design" answer: he codes the same repos on several machines, and the design made data collection *harder*. He was right. `docs/metrics/` is tracked, but the hook appended exactly ONE record, which loses data three ways on a multi-machine setup — the newest record is always uncommitted (post-commit fires after the commit is sealed, so it rides in the next one; stop using that machine and it never ships), a clone with no hook records nothing silently (`.git/hooks/` is not part of the repository — the four Mac clones here have `.tfcore/` but no `docs/metrics/` and no hook), and neither machine ever saw the other's commits. Fix: **the hook now RECONCILES** — every commit, it writes a record for every commit reachable from HEAD the stream lacks, skipping on `sha`, delegating to `tf-metrics.sh --backfill-commits --limit 500 --quiet` with the old pure-bash path as the no-python3 fallback. `git log` is already replicated by push/pull, so the stream is maintained as a projection of it and a pull + commit backfills the other machine. **The git ban is untouched** — still the owner's own `git commit`, still nothing added to the commit being made, `pre-commit` + `git add` still rejected. Supporting: `--report` now warns `⚠ no post-commit hook in THIS clone` (a filesystem read, not a git call), `metrics-report.md` tells the agent to report it and never act on it, and `tf-emit.sh` accepts a **JSONL batch** so a 400-commit first reconcile is one process instead of 400. Second issue, the GitHub-on-Windows *"uses 'LF' … will be converted to 'CRLF'"* warning: same root cause as 2026-07-11 one layer out — the framework repo pinned `eol=lf` back then, **app repos never did** (stock VS `* text=auto`, or nothing at all), and `text=auto` normalizes the blob but still smudges the working tree. The 8d `.gitattributes` block in all three scripts now manages `* text=auto eol=lf` + `*.bat`/`*.cmd text eol=crlf` + `docs/metrics/*.jsonl text eol=lf merge=union`, removing the legacy un-pinned line rather than shadowing it; verified idempotent across four repo states. **Propagated to the four Mac repos** (`AppStudio`, `TrSetup`, `TrStudio`, `TechieRag`) with the owner's go-ahead, and `--backfill-commits` recovered **64 commit records** that had been in the logs all along (AppStudio 32 back to 2025-12-15, TechieRag 22, TrSetup 6, TrStudio 4) — the clearest possible proof that the data was never missing, only never asked for. The nine Windows repos are the owner's to refresh there. **Owner actions:** `update-framework.sh <repo>` per app **per machine**, then once per repo `git add --renormalize . && git commit`. **Then the owner named what the reconcile fix had NOT touched** — *"when I commit, a new line is added to the file after commit"* — and he was right: `post-commit` cannot put its line inside the commit it describes, so the tree was **never clean, with no reachable state where it was**, and "commit the one-line diff first" was circular advice. Filed in 2026-08-08 as "the one-commit lag" (a caveat); it was a defect wearing a caveat's label. Offered three fixes, the owner chose **`pre-commit` + stage**: the hook reconciles from the log then `git add`s that one file, so records ship INSIDE the commit and the tree is clean afterwards, while the stream stays in the repo. This **reverses** DECISIONS.md §3, which had rejected exactly this — revised in place, with the risk bounded rather than waived: stages **exactly one path** (never a directory, never `-A`), **does not stage on a partial commit** (`GIT_INDEX_FILE` detection), **exits 0 on every path** (a non-zero pre-commit hook aborts the commit and telemetry has no veto), and **never replaces a `pre-commit` hook it did not write** (losing your lint gate beats losing commit records). Skipped cases — merges, `--no-verify`, rebase, cherry-pick — are picked up by the next commit's reconcile. Constraint 1 untouched: agents still never run git, `block-git.sh` unchanged. `post-commit` deleted from the framework; `install-metrics.sh` removes installed copies. All four Mac repos redeployed and verified. Also surfaced, logged not patched: `TechieRag` hosts two products (the library + `apps/TechieDesk`) and so breaks telemetry's one-checklist-per-repo assumption. See §5 entry.) Prior: 2026-08-10 (**the artifact rule extended to the harness** — the owner asked why `scripts-cluster-*` folders were still appearing after the morning's fix. Answer: that fix pinned where the *tools write* and never said where an agent puts the *scripts it authors*, so the next fan-out swapped nouns — TechieBlog gained `scripts-cluster-b|f|g|i/`, each holding a hand-written `smoke-*.mjs`. Worse, those scripts `import { chromium } from 'playwright'` — the **library**, not the runner — so `playwright.config.ts` never loads, `outputDir` never applies, and one hardcoded an absolute path to a `test-results-cluster-b` it created itself: the ban's enforcement mechanism could not see them. Plain `scripts/` (no hyphen) is **legitimate** in 5 repos (release/publish/dev-setup scripts) and is now explicitly off limits in both directions. Fix rewrites the ARTIFACT-LOCATION RULE around *the class of thing* — everything a run generates that is not a deliverable, tools' output **and** the harness — with harness scripts in **`tests/.artifacts/harness/`**, an instruction not to reason by the letter of the list (*any* root-level `<name>-<slug>/` is the same defect), a ban on absolute paths in library-driving scripts, and `/scripts-*/` (root-anchored, hyphen required) in the ignore block + §1 sweep + §7 teardown. TechieBlog's four dirs swept, propagated to all 9 repos. See §5 entry.) Prior same day: (**§4c — the performance gate** — §4a proved data was present and §4b proved the screen was usable, but *nothing covered speed*: a page rendering every control perfectly in nine seconds passed every gate. Discovered from the artifact cleanup below, where an agent's throwaway dirs held a hand-written load harness it had invented to grade an NFR the framework couldn't. New **§4c** in `verify-phase.md` + the harness **`.tfcore/utils/tf-perf.sh`** (TTFB/full-load p50/p95/max at graded concurrency, warm-up discarded, per-path breakdown, `weak` flag under 20 samples; produces numbers, **never** a verdict). Governing rule: **it never invents a budget** — a REQ is graded only if its acceptance criteria carry `perf-budget: <p50|p95|max> <ttfb|load> <= <N>ms [@ concurrency <N>]`; no line, no gate. Three bands (`PERF-OK` · `PERF-MARGINAL` at ≤ budget×1.25, a remark that never blocks `Verified` · `PERF-FAIL` beyond that), and four refusals to grade at all (`PERF-UNMEASURED`: Debug build, weak sample, errors during the run, contended host) — because a gate that fails on noise teaches the owner to distrust every verdict. MAUI native heads are not perf-gated (no HTTP surface). Telemetry: `gate` gained `perf` (order now `build`→`acceptance`→`render`→`visual`→`perf`→`standards`), `failure_class` gained `slow-ttfb`/`slow-load`/`timeout` (**never the milliseconds** — a latency number belongs to one machine on one day), and new **SCHEMA §3.5** + a `LATE_GATES` table in `tf-metrics.sh` fix the real hazard: a gate added mid-stream is structurally understated in a raw distribution, so its catch rate is reported against `gates_run` membership and never against the total. `PERF-MARGINAL`/`PERF-UNMEASURED` are never emitted as failures. See §5 entry.) Prior same day: (**Test artifacts confined to `tests/.artifacts/` + the empty-`commits.jsonl` question answered + the team edition linked** — three owner-reported items. (1) **The artifact-litter bug (real defect).** Playwright defaults its `outputDir` to a repo-root `test-results/`, and the framework never pinned it; worse, verify/smoke fan-outs invented *suffixed siblings* (`test-results-cluster-a/` … `-cluster-l/`, `test-results-tr054/`, `test-results-blogapp/` — TechieBlog accumulated **fourteen**) which the ignore pattern `test-results/` does **not** match, so screenshot dumps surfaced untracked at every commit — precisely the triage burden the managed ignore block exists to prevent. Fix: `verify-phase.md` §1 now pins **`outputDir: './tests/.artifacts/test-results'`** in `playwright.config.ts`, *editing* an existing config that lacks it (not just creating one when absent — the old wording only ever created), states a hard **ARTIFACT-LOCATION RULE** (no repo-root artifact dir, no `--output test-results-<slug>`, isolation only as the subfolder `--output tests/.artifacts/<slug>`, screenshots are session-lifetime failure evidence and never work product), sweeps legacy root `test-results*` dirs on its next pass, and §7 teardown forbids leaving one behind; `_smoke-test-policy.md` + `build-phase.md` bound by the same rule; ignore block in all three scripts gained `tests/.artifacts/` and the `test-results-*/` glob (the bare `test-results/` and the glob are **legacy-only** now). `docs/screenshots/<APP>/` — the DevGuide's reviewed, deliberately TRACKED set — is untouched. (2) **`commits.jsonl` empty in every repo: BY DESIGN, not a bug.** `install-metrics.sh` seeds all four streams so every repo has one shape; each fills only on its own event, and `commits.jsonl` is written solely by the owner's `.git/hooks/post-commit` — verified installed in all 9 app repos, and demonstrably working (TechieBlog, the only actively-committed repo, has real records). A repo not committed to since 2026-08-08 shows 0 bytes indefinitely. Made explicit in the generated `docs/metrics/README.md`, README §17, and a new WORKFLOW §15 FAQ entry. (3) **README now links the team edition** — new **§18 "Team edition — the AI-First Playbook"** (`https://github.com/techierathore/AI-First-Playbook`) with a solo-vs-team comparison table, plus a header blockquote; mirrored into WORKFLOW.html §18 + TOC. The Playbook already linked back here, so the pair is now reciprocal. See §5 entry.) Prior: 2026-08-08 (**Development telemetry — the framework now measures itself** — TechieFlow produced the evidence and threw it away: every `*verify` applied four named gates to identified REQs, wrote `docs/.last-verify.json`, then the next run overwrote it and the mutated-in-place status table erased the history. New append-only JSONL streams under **`docs/metrics/`** (TRACKED, never ignored): `runs.jsonl` (one per command run, written by each phase task at the status gate), **`gates.jsonl` — the primary stream** (one per REQ verdict per verify run, carrying the FIRST failing gate: `build`/`acceptance`/`render`/`visual`/`standards`, or `escaped` from `*triage-issues`), `sessions.jsonl` (SessionEnd hook), `commits.jsonl` (the owner's own `post-commit` hook). They answer exactly three questions — first-pass rate, gate catch distribution, escape rate — and deliberately NOT cycle-time-per-feature (the unit of work here is the run, not the ticket). New files: `.tfcore/telemetry/{SCHEMA.md,install-metrics.sh,post-commit,tf-metrics.sh}`, `.tfcore/utils/tf-emit.sh` (the single append primitive — exits 0 unconditionally), `.tfcore/hooks/metrics-session.sh`, `.tfcore/tasks/_metrics-emit-gate.md` (doctrine, sibling of `_status-update-gate.md`), `.tfcore/tasks/metrics-report.md` + its template; new flow-master command **`*metrics {AppName}`**. **The git ban is untouched** — `block-git.sh` unchanged, no agent path runs git, and the only script carrying version-control commands (`tf-metrics.sh --backfill-commits`) is owner-run. **Provenance never merges:** no report may print a combined first-pass rate, gate distribution, or escape rate across live/backfilled or across `project_type` (`app`/`library`/`docs`/`framework`, auto-detected once into the preserved `core-config.yaml`) — enforced in `tf-metrics.sh` code, not just prose. See §5 entry.) Prior: 2026-07-16 (**`*triage-issues` — the ANALYZE-ONLY bug front door** — during AstroLyfe UAT the owner asked flow-master in free text to *analyze* human-found bugs, re-verify other features, and update the checklist — explicitly NOT to fix — and the agent routed into `*fix-issues` and started fixing code anyway. There was no command whose *deliverable* is the analysis. New flow-master command **`*triage-issues {AppName} {evidence} [verify]`** (task `.tfcore/tasks/triage-issues.md`): takes a screenshots folder AND/OR a written bug list, boots + reproduces each issue with Playwright, triages to the owning REQ, then delivers **docs only** — regressed REQs demoted to `Needs re-verify` with dated `⚠ UAT bug` remarks, unspecified defects appended as new `Planned` REQ rows with acceptance bullets, DevGuide known-issues refreshed, PROJECT-STATUS next-command = the `*fix-issues {App} {folder}` pointer naming the REQ IDs; optional `verify` literal EXECUTES a scoped verify-phase over sibling REQs (writes the run ledger legitimately). HARD SCOPE RULE: never edits `src/`/`tests/`, never spawns builder sub-agents, never writes `Verified` itself. Root cause also fixed: `fix-issues.md` gained **§0 scope guard** ("was a FIX actually requested?" — analysis-only wording → STOP, run triage-issues) + a Hard rule; flow-master persona gained the **ANALYZE IS NOT FIX** core principle + REQUEST-RESOLUTION routing examples. Wired per the contract: task mirrored ×2, flow-master command/help/deps updated + mirrored ×2, `opencode.jsonc` registration, WORKFLOW.html §7.12 + flowcharts + quick-ref + FAQ + README mirrors, propagated to the WSL apps via `update-framework.sh`. See §5 entry.) Prior: 2026-07-11 (**Windows-pull CRLF fix** — pulling the Mac commits onto the WSL clone smudged every pulled text file to CRLF (`core.autocrlf=true` + the old `.gitattributes` having only `text=auto`, no `eol`), breaking ALL `*.sh` (`/usr/bin/env: 'bash\r'`) **including the three `.tfcore/hooks/*.sh` PreToolUse guards — which locked agents out of Bash AND Write/Edit**; owner ran the one in-session unblock command (`! find … -name '*.sh' -exec sed -i 's/\r$//' {} +`), agent normalized the remaining 13 CRLF files (mirror byte-identity re-verified), `.gitattributes` now pins `* text=auto eol=lf` + `*.sh text eol=lf`, and `update-framework.sh` re-ran on all 7 WSL apps — every deployed hook verified LF + `bash -n` clean. **Owner actions:** `git add --renormalize . && git commit` once here; restart open sessions on refreshed apps. See §5 entry.) Prior same day: (**macOS-native portability** — the owner moved the framework + apps from Windows/WSL to a Mac natively. All three scripts' hardcoded `TEMPLATE="/mnt/c/3AIGenCode/TechieFlow"` replaced with self-location (`BASH_SOURCE` dir), `realpath -m` (GNU-only; scaffold-greenfield) and `sed -i -e` (BSD sed treats `-e` as a backup suffix; update-framework legacy migration) made portable, executable bits lost in the Windows→Mac copy restored (`chmod +x *.sh .tfcore/hooks/*.sh`); scaffold-brownfield → update-framework → scaffold-greenfield verified end-to-end on macOS (stock openrsync handles `-a`/`-ain`/`--delete`/`--ignore-existing`). WORKFLOW.html §16 + README gained a **"Moving from Windows/WSL to a Mac"** panel: Finder hides dot-folders (Cmd+Shift+. / `defaults write com.apple.finder AppleShowAllFiles`); a git-moved app repo lacks the framework copies BY DESIGN (gitignored since 2026-07-05) — re-deploy with `update-framework.sh` if `.tfcore/` exists else `scaffold-brownfield.sh`; per-project gitignored files (`CLAUDE.md`, `core-config.yaml` customizations, `settings.local.json`) need a folder copy from the old machine or a day-1/refresh regen. Apps NOT yet refreshed from the Mac — owner to run `update-framework.sh` per app as each is moved.) Prior: 2026-07-10 (**`Verified` verdicts made MECHANICAL + `<APP>.App` naming banned + cross-publish guides disambiguated** — (1) the TrSetup build orchestrator SELF-ATTESTED `Verified` verdicts on 2026-07-09 without executing verify-phase (agent-confirmed) → third PreToolUse hook **`.tfcore/hooks/guard-verify.sh`** blocks any `*-Checklist.md` write that *introduces* a `Verified` status cell unless a same-day run ledger `docs/.last-verify.json` exists, which only an executed verify-phase §6 writes (reconcile escape for refresh-status; demotions never blocked); prose hardened in build-phase §6b ("chaining = EXECUTING, not summarizing; the orchestrator never writes Verified"), `_smoke-test-policy` §"Smoke is NOT verify" (self-smoke ceiling = `Implemented`), flow-master persona principle, refresh-status §3 reconcile-mode; wired into settings.json + all three scripts' heredocs, propagated to all 7 apps; TrSetup's 4 self-attested rows (FN-031/FN-034/NFR-007/UI-006) demoted `Needs re-verify` + its PROJECT-STATUS corrected; (2) **naming standard: the primary executable head project is named exactly `<APP>` — `<APP>.App` is BANNED**; secondary heads take descriptive suffixes (`.Api`/`.Desktop`); baked into the coding-standards canonical block (day1-brownfield §4 "Project & solution naming") + build-phase §3 scaffold rule + WORKFLOW §10/README standing standard; TrSetup gained REQ-FN-035 (rename `TrSetup.App` → `TrSetup`, next `*build-phase`); (3) the owner misread TrStudio's BuildAndRun guide as claiming MAUI cross-publishes as an ARM Mac app → guide's intro + §3 Option B now state explicitly that `-r osx-arm64` covers ONLY the plain .NET heads and the Catalyst `.app` is one command on the Mac (stale pointer to TrSetup's deleted §1b removed; .md + .html both updated); agent-facing "a host limitation never cancels a cross-OS deliverable — never substitute the web head for the native app" notes added as HTML comments in both TrStudio's and TrSetup's guides + `build-invocation-ladder.md` §"What NOT to do"). Prior: 2026-07-09 (**PROJECT-STATUS shape made MECHANICAL + Serilog file logging a standing .NET default + TrSetup simplified to a single MAUI desktop app** — (1) agents kept convoluting PROJECT-STATUS despite the 2026-06-30 prose rules, so a new PreToolUse hook `.tfcore/hooks/guard-status.sh` (matcher `Write|Edit|MultiEdit`) now BLOCKS mis-shaped writes to `PROJECT-STATUS.md` — non-template H2s, command-run headings, paragraph `current_phase`, >120-line writes — wired into settings.json in the framework + all three scripts' heredocs, same two-layer treatment as the git ban; (2) Serilog file-based logging is now a STANDING requirement for every .NET app type (web/API/MAUI/desktop/console/service): BRD template §11 standing Observability NFR, both day1 tasks always emit it, coding-standards §Logging wiring recipe in the canonical block, architecture template §5, and build-phase wires it into any new head at scaffold time even with no REQ naming it; (3) TrSetup: owner withdrew the CLI + Web heads — single MAUI desktop app now — checklist FN-011/012/013/032/033/NFR-006 → N/A, FN-031 rescoped, new REQ-FN-034 decommission + REQ-NFR-007 Serilog + REQ-UI-006 GUI Settings screen (the endpoint/profile config surface was CLI-only and dies with the CLI — BRD-56, UIDesign §Settings), FN-019/020 status reconciled to their own verified evidence, PROJECT-STATUS rewritten crisp with next-command `*build-phase TrSetup`, and BRD/Architecture/DevGuide/UsageGuide/BuildAndRun-Guide rewritten MAUI-only + re-rendered, AgentMode.md archived to OldDocs). Prior: 2026-07-05 (**deployed framework copies must never be committed in app repos — `.gitignore` is now script-managed** — owner found `.opencode/` (and friends) uncommitted-guarded in some apps but not others, and the framework repo itself had NO `.gitignore` (even `.claude/settings.local.json` was tracked). Fixes: `scaffold-greenfield.sh` / `scaffold-brownfield.sh` / `update-framework.sh` all gained an append-only, idempotent "ensure `.gitignore` framework block" step (`.tfcore/`, `.claude/`, `.opencode/`, `/CLAUDE.md`, `/WORKFLOW.html`, `/opencode.jsonc`, `/.tf-scaffold-note.txt`; CRLF-safe matching so Windows-authored files don't get duplicates; existing user entries respected, nothing removed); the framework repo gained its first `.gitignore` covering per-machine/local files only — the framework SOURCE (`.tfcore/`, harness mirrors, `WORKFLOW.html`, `opencode.jsonc`) stays TRACKED here because it is exactly what the scripts deploy FROM; all 6 apps refreshed + their git indexes audited — only 3 stragglers remain for the owner to `git rm --cached`). Prior: 2026-07-04 (**agent-facing template notes hidden from human readers + library persona files fixed at their SOURCE repos** — the BRD/Architecture/DevGuide templates' "Depth mandate"/"Mermaid mandate" blockquotes (visible text agents copied into every generated doc, so the owner saw them in the rendered HTML) converted to HTML comments; `html-render-shell.md` gained §6b (leaked notes from pre-2026-07 docs are stripped at render, never emitted as visible text) with `generate-html`/`render-workflow-docs` wired to it; the stale June-13 top-level `/generate-html` wrappers regenerated (they still allowed rendering checklists!); and the trblazeui/techierag persona files — which are NuGet-owned and re-deployed over `.claude/commands/` on every consumer build, so the framework's hand-edited rebrand copies were futile — were fixed at origin in the TrBlazeUI/TechieRag repos (BMAD header removed, `.bmad-core`→`.tfcore`), the framework's snapshots refreshed from those sources, TR-001/TR-RAG-001 marked resolved (both libraries' `.targets` already deploy to `.claude/commands/`), and new TR-002/TR-RAG-002 logged: **owner must republish both packages**). Prior: 2026-07-02 (**Git ban made MECHANICAL + run-it-yourself moved into always-loaded surfaces + native-head window-binding discipline** — settings.json now DENIES git/gh (was ask) backed by a new PreToolUse hook `.tfcore/hooks/block-git.sh` that catches compound forms; the framework's own pro-git prose purged (build-phase/fix-issues told sub-agents to *commit* `[REQ-*]`!, CLAUDE.md template mandated commit-message tags); the git-ban + smoke-it-yourself rules added to all four agent personas' core principles + a new CLAUDE.md-template "Hard rules" section; sub-agent prompts must now carry both rules verbatim; and verify-phase §3b gained the window-binding & element-scoped-input rules that stop automation typing into the wrong window). Prior: 2026-06-30 (**WORKFLOW.html slimmed + quick-reference flowcharts** — added a top "Process at a glance" section with 5 command-bearing mermaid flowcharts (mermaid runtime wired in for the first time) AND removed the 698-line §13 "Document templates" section (agent-facing duplication of `.tfcore/templates/`), renumbering to 16 sections; both mirrored to README + propagated. Same day: **Libraries are first-class projects** — the DevGuide gained *component-by-component* (UI libs) + *service-by-service* (SDK libs like TechieRag) modes, retiring the "libraries get no status/DevGuide" exemption; TrBlazeUI/TechieRag doc regens teed up to run in their repos. Earlier same day: **PROJECT-STATUS bloat fix** — the status gate now enforces a CRISP, fixed-shape, OVERWRITE-not-APPEND snapshot, bans per-run dated H2s + paragraph-`current_phase`, forbids `git grep`-for-status, and a state-derived Build→Verify→Handoff next-command ladder; AstroLyfe's 286-line append-log cleaned to ~78 lines; all propagated to the 6 `.tfcore` apps). Prior: 2026-06-29 (added the **Appium runtime bridge** — the verifier now drives MAUI **Android / iOS / Mac Catalyst** heads over Appium and applies the SAME data-render + visual-truth gates it applies to Blazor, closing the "mobile/desktop screens were build-only, never observed" blind spot). Prior major: 2026-06-26 (ONE checklist replaces the two; build phases dissolved into a single `*build-phase`; greenfield mockups + brownfield screenshot-review; the **visual-truth gate**; `*fix-issues`).

---

## 1. What this repository is

This is a **customized TechieFlow v4 framework** for a **solo developer** (Claude Max) shipping a portfolio of ~8–10 .NET / Blazor / MAUI apps. It is NOT an application — it is the template that gets copied into each app to drive its development with AI agents. The owner optimizes for **speed-to-market over process purity**: a stripped, opinionated subset of TechieFlow tuned for one person + AI, not the full multi-role story-by-story TechieFlow flow.

The stack the apps target: **.NET 9 / Blazor (Server | WASM | Auto) / TrBlazeUI (UI library) / TechieRag (RAG/LLM library) / optional MAUI**. Claude Code runs inside **Windows WSL** — no GUI Playwright, MAUI builds/runs on the Windows side (see `WORKFLOW.html` §0, §11).

## 2. How the framework is used (the flow)

The canonical reference for humans is **`WORKFLOW.html`** (open in a browser). The short version for an agent:

1. **Scaffold** a new app by copying this template (`scaffold-greenfield.sh` / `scaffold-brownfield.sh`) — never `npm install`. `update-framework.sh` re-syncs an existing app to the latest framework.
2. **Day-1** (`*day1-greenfield` / `*day1-brownfield` on the **analyst**): produces Architecture, BRD, Coding-Standards, `.editorconfig`, PROJECT-STATUS, CLAUDE.md + the UsageGuide, all HTML-rendered. **Greenfield also** produces **UI mockups** (§3.6 — `*mockups`); **brownfield also** migrates any dev plan into the one checklist and generates the screen-by-screen **DevGuide** (§7.6, which now also captures a screenshot of every screen + runs an owner visual-review gate).
3. **`*mockups`** (analyst, greenfield only): reads the TrBlazeUI component catalog, then produces `docs/<APP>-UIDesign.md` (per-screen `region → TrBlazeUI control` map) + rendered `docs/mockups/*.html`. The build's visual contract + the verifier's visual baseline. Approved with the BRD + Architecture before build.
4. **`*split-brd`** (analyst): splits the rich BRD into the **ONE** checklist `<APP>-Checklist.md` — all `REQ-UI/FN/NFR/RAG-*` in a single Requirements Status table (the two-file model is gone; prefixes still route work).
5. **Build** (`*build-phase` on **flow-master**): the SINGLE unified build. Clusters every open REQ and **calls `/trblazeui` (REQ-UI-*, from the mockups) and `/techierag` (REQ-RAG-*) as sub-agents** while building FN/NFR itself; self-smokes (data + visual), then **chains the verifier automatically**. (`build-ui-phase`/`build-rag-phase`/`build-functional-phase` were dissolved — trblazeui/techierag are skills the parent agents call, not standalone phase commands.)
6. **Verify** (`verifier` / Vidur, `*verify ui|functional|all` — scopes filter the one checklist by prefix): autonomously boots the app, runs Playwright + dotnet tests, and applies BOTH the **data-render gate** (§4a — controls show their data) AND the **visual-truth gate** (§4b — no overlap/clip/off-viewport, screenshot inspected, diffed vs mockup). `Verified` requires acceptance + data + visual. Verdicts → the one checklist's **Requirements Status** table.
7. **Handoff** (`*handoff-phase` on flow-master): usage/testing doc, status → Handoff, HTML re-render, per-library feedback consolidation.
8. **`*fix-issues`** (flow-master, any time): the bug-fix front door — drop a folder of screenshots (+ optional description), it reproduces with Playwright, triages, fans the fix out to the right builder, re-smokes (data+visual) + re-verifies, updates the docs. This is "the verifier passed but the UI is broken — who do I call?": flow-master, not the analyst.
9. **`*triage-issues`** (flow-master, any time): the **analyze-only** sibling for human-found bugs (UAT / production) — screenshots folder and/or a written bug list in, **docs out**: reproduces + triages each issue, demotes regressed REQs to `Needs re-verify` (dated `⚠ UAT bug` remarks), appends new `Planned` bug REQ rows with acceptance, optionally re-verifies sibling features (`verify` arg → a real executed verify-phase), refreshes DevGuide known-issues + PROJECT-STATUS (next command = the `*fix-issues` pointer). **Never edits code, never spawns builders** — fixing is a separate owner decision.

**The owner's actual day-to-day loop** is lighter than full TechieFlow: analyst (day-1 + mockups) → flow-master `*build-phase` → verifier → flow-master `*fix-issues` for any visual bugs, NOT story-by-story. (See session memory `feedback_custom_techieflow_workflow`.)

### Non-negotiable conventions (an agent must honor)

- **Status gate** (`.tfcore/tasks/_status-update-gate.md`): every checklist/phase-executing agent MUST end by updating `PROJECT-STATUS.md`. A phase is done when status reflects reality, not when the build is green.
- **ONE checklist per app** = `docs/<APP>-Checklist.md`, holding all `REQ-UI/FN/NFR/RAG-*` in a single **Requirements Status** table (the single source of truth for per-REQ status). The two-file model (`-UI-Checklist` + `-Functional-Checklist`) is **retired** — prefixes still route work and scope `*verify`. **Never** create dated `docs/qa/*.md` or `docs/verify/*.md` files (banned).
- **"Verified" = acceptance + data-render + visual-truth.** A REQ is `Verified` only if its acceptance test passes, its controls actually render their data (verify-phase §4a), AND every screen it owns LOOKS right — no overlap/clip/off-viewport, screenshot-inspected, diffed vs the mockup where one exists (verify-phase §4b, the **visual-truth gate**). Data-present-but-blank and data-present-but-visually-broken both FAIL → `Needs re-verify`. The same two gates apply to every self-smoke (`_smoke-test-policy.md`). This is the fix for "the verifier passed but the running UI is completely broken."
- **HTML rendering is for HUMAN docs only.** BRD, Architecture, UsageGuide, **UIDesign/mockups**, **DevGuide**, PROJECT-STATUS render to `.html` (mockups are HTML by construction). **The checklist NEVER renders to HTML** — it is an AI-agent working document kept in markdown only. All human HTML uses the shared shell with the **light/dark theme toggle** (time-of-day default, remembered choice, warm off-white light).
- **File naming:** `<APP>` prefix on canonical docs (`<APP>-BRD.md`, `<APP>-Architecture.md`, `<APP>-Checklist.md`, `<APP>-UIDesign.md`, …); mockups under `docs/mockups/`, DevGuide screenshots under `docs/screenshots/<APP>/`. Superseded docs → `docs/OldDocs/`, never `-v2` suffixes.
- **Library feedback:** one file PER library — `docs/<APP>-TrBlazeUI-Feedback.md` (TR-NNN), `docs/<APP>-TechieRag-Feedback.md` (TR-RAG-NNN). Log gaps, work around, never silently swallow; never combine the files (separate teams).
- **Telemetry is emitted, never assembled by hand.** Every phase task ends by appending ONE `runs.jsonl` record through `.tfcore/utils/tf-emit.sh` (the status gate is the trigger, `_status-update-gate.md` item 10); `verify-phase` §6a appends one `gates.jsonl` record per REQ it graded, carrying the FIRST gate that failed; `triage-issues` §6a appends `gate:"escaped"` records, which is the only way escape rate becomes computable. **Telemetry has no veto** — a failed emit never blocks a write, fails a build, or changes a verdict, and is never worth retrying or reporting. **Never** write metrics into `PROJECT-STATUS.md` or any `*-Checklist.md` (the guard hooks block it, correctly), never edit a `.jsonl` stream, never emit free text or requirement prose into a record, and never invent a field absent from `.tfcore/telemetry/SCHEMA.md`. Doctrine: `.tfcore/tasks/_metrics-emit-gate.md`.
- **Coding standards** are per-project (`docs/<APP>-Coding-Standards.md`); the field-prefix decision is per-project (e.g. AppManager uses `obj`, AstroLyfe uses no prefix). `docs/AppManager-Coding-Standards.md` + `docs/AstroLyfe-Coding-Standards.md` in this repo are **reference SAMPLES** for agents, not live docs.
- **Build invocation ladder** (`.tfcore/templates/v4custom/build-invocation-ladder.md`): solution-scan first; MAUI/iOS/Android in the `.sln` → start rung #4; workload errors (`NETSDK1178`, `Microsoft.*.Sdk missing`) mean *wrong rung*, never "build failed".

## 3. Repo map (where things live)

| Path | What it is |
|------|-----------|
| `WORKFLOW.html` | The full human-facing workflow reference (**18 sections**; opens with **"Process at a glance"** quick-ref flowcharts; §16 = running on macOS / native Windows / Linux, §17 = development telemetry, §18 = the AI-First Playbook team edition). Source of truth for the *process*. The per-doc raw template schemas are NOT here — they live only in `.tfcore/templates/v4custom/` (the old §13 "Document templates" was removed 2026-06-30 as agent-facing duplication). |
| `WorkFlow-Context.md` | **This file** — AI-agent context + maintenance log. |
| `.tfcore/agents/` | The kept agent roster: `analyst`, `architect`, `flow-master`, `verifier`. |
| `.tfcore/tasks/` | Executable task workflows (day1, **mockups**, split-brd, **build-phase**, verify-phase, **fix-issues**, **triage-issues** (analyze-only), handoff-phase, refresh-status, generate-html, **devguide**, **productguide**, …). |
| `.tfcore/templates/v4custom/` | The custom doc templates (BRD, architecture, **checklist** (single), **uidesign**, **devguide**, **productguide**, project-status, claude-md, html shell, build ladder, …). |
| `.tfcore/TOKEN-GUIDE.md` | Token-efficiency guide (owner + team) — where tokens go and the levers to cut them; ships with every app via the `.tfcore/` rsync. |
| `.tfcore/core-config.yaml` | Project config (which docs the agents load). |
| `.tfcore/telemetry/` | Telemetry: `SCHEMA.md` (canonical schema — read before emitting), `install-metrics.sh` (per-repo setup — **auto-invoked by all three scripts**, not a command you run), `pre-commit` (hook template — reconciles + stages `commits.jsonl`; replaced `post-commit` on 2026-08-11), `tf-metrics.sh` (report / backfill — **the only file allowed to contain version-control commands**; `--report` / `--rollup` / `--phases` are read-only and **agent-safe**, `--backfill-*` are owner-only — narrowed 2026-08-31). |
| `.tfcore/utils/tf-emit.sh` | The single append primitive for every telemetry stream. Exits 0 no matter what. |
| `.tfcore/utils/tf-assets.sh` | **Asset-integrity gate** (verify-phase §4a2, added 2026-08-31, TfLens TF-007). Parses what a page *declares* and asserts each arrived with a non-empty body. python3 stdlib only; ~100 ms per page; same-origin by default. Exit 5 = a declared asset did not arrive. |
| `.tfcore/utils/tf-mockup-parity.sh` (+ `.mjs`) | **Mockup-parity gate** (verify-phase §4b2, added 2026-08-31, TfLens TF-008/009/011/012). Compares a built screen to `docs/mockups/<screen>.html` **structurally**, at 1280 and 390, over eight classes. Publishes per-screen coverage and returns `UNGRADEABLE` rather than a false `PASS`. Playwright (which verify-phase §1 already provisions). |
| `.tfcore/utils/tf-gitignore-audit.sh` | **Build-output ignore audit** (added 2026-08-31, TfLens TF-007 companion). Detects the stack from the tree, adds its build-output rules, and reports build output that is **already tracked** — a tracked file is never ignored. Contains **no** version-control commands: it parses `.git/index` and prints the remediation for the owner. |
| `docs/metrics/` (in each app repo) | The four append-only JSONL streams + `METRICS.md`/`.html`. **Tracked, never ignored.** |
| `.claude/commands/TechieFlow/` | **Claude Code harness mirror** of agents+tasks — must stay byte-identical to `.tfcore/` (see §7). OpenCode has **no** mirror — it loads agents/tasks from `opencode.jsonc` `{file:./.tfcore/...}` refs; `.opencode/command/` holds only top-level loose commands (`generate-html.md` + the NuGet personas). |
| `.claude/commands/{trblazeui,techierag}.md` | NuGet-deployed library agent personas (owned by their libraries). **`.claude/commands/<lib>.md` is the current deploy target** — both libraries' MSBuild targets write it directly since 2026-07-04. `.claude/<lib>.md` is the LEGACY target, still present in older app repos and still shim-copied forward by the three scripts, but **created by nothing in a modern app** — never hardcode it as the only path (that was the 2026-08-24 routed-wrapper defect). Anything reading a persona resolves `.claude/commands/<lib>.md` → `.claude/<lib>.md` → `.<lib>/<Lib>-AI-Reference.md`. |
| `scaffold-*.sh`, `update-framework.sh` | Project scaffolders + the re-sync script. |
| `docs/` | Reference sample coding-standards docs + the framework's own per-library feedback files. |
| `opencode.jsonc` | OpenCode agent + command registrations. |

**Agent roster & roles** (the only agents — the stock `dev/pm/po/qa/sm/ux-expert` story-flow agents and tasks were trimmed and are no longer included):
- **analyst (Chanakya)** — day-1 docs, BRD authoring, **mockups** (greenfield UI design, calls `/trblazeui` for the component catalog), split-brd (the one checklist), **amend-docs** (incremental in-place BRD/Architecture updates as a project evolves).
- **architect (Ravi)** — architecture docs (optional deep dive; analyst covers day-1 architecture).
- **flow-master (Madhav)** — the single super-agent. Doc/status utility hub (render-workflow-docs, generate-html, handoff-phase, **refresh-status**, **amend-docs**, document-project) **and** orchestration: the unified **build-phase** (calls `/trblazeui` + `/techierag` as sub-agents), **fix-issues** (the bug-fix front door), **triage-issues** (the analyze-only bug front door — logs UAT/prod bugs into the checklist, never fixes), the BRD-coverage protocol, and `*run-workflow`/`*phase` driving the full pipeline across parallel subagents.
- **verifier (Vidur)** — autonomous verification (data-render + visual-truth gates), writes verdicts into the one checklist table.
- **trblazeui / techierag** — NuGet-deployed library agents (free-form, no `*command` syntax). They are **skills the parent agents call as sub-agents**, not standalone phase commands: `flow-master *build-phase` calls them to build REQ-UI-*/REQ-RAG-* (trblazeui from the mockups), `*fix-issues` calls trblazeui for layout fixes, and `analyst *mockups` consults trblazeui for the component catalog.

## 4. Recovering an interrupted session (`*refresh-status`)

If a build/verify run dies mid-phase — lost internet, **revoked/changed model access**, killed terminal, crashed agent — the status gate never runs, so `PROJECT-STATUS.md` is stale or wrong. Do **not** trust it. Run:

```
/TechieFlow:agents:flow-master *refresh-status <APP>        # add `verify` to also re-verify ambiguous REQs
```

It rebuilds PROJECT-STATUS from **ground-truth evidence** (the checklist Requirements Status tables + what's actually in the working tree, including source-file modification times + a fresh `dotnet build`), **refreshes the BRD §4 Development status snapshot** from those same checklists (and inserts that section into a legacy BRD that predates it — the one BRD edit it's allowed; see §5's 2026-06-14 follow-up), writes a dated `## Recovery note`, and prints the exact command to resume with. **It never runs git** — git is manual in this framework (agents never commit; since 2026-07-02 the harness **denies** `git`/`gh` outright — settings.json deny + the `.tfcore/hooks/block-git.sh` PreToolUse hook), so recovery reconstructs from the tables + files-on-disk + build, never commit history. It never edits source. Task: `.tfcore/tasks/refresh-status.md`; documented in `WORKFLOW.html` §8a. This exists because of the AstroLyfe incident (2026-06-13): an experimental model lost access mid-development and left no usable status.

## 5. Maintenance log (what has been done, newest first)

### 2026-08-31 (3) — propagated to all 19 repos, and the propagation found a 4,635-file instance of the defect it was carrying the fix for

`update-framework.sh` run against every repo carrying `.tfcore/` — **19 targets, all exit 0.** Verified
afterwards that each one carries the four new scripts (`tf-assets.sh`, `tf-mockup-parity.sh` + `.mjs`,
`tf-gitignore-audit.sh`) *and* the updated `tf-metrics.sh`, `tf-emit.sh`, `tf-render-html.py`,
`verify-phase.md` and `SCHEMA.md` — checked by content, not by mtime. Every metrics stream in all 20
repos still parses. The only warnings in the 850-line log are the two pre-existing owner-owned
stale-layout files already tracked in §6.

**The dry run earned its keep, before anything was written.** On TfLens the gitignore audit reported the
stack as `dotnet, node` and proposed `dist/`, `build/`, `.next/`, `.nuxt/`, `.turbo/`. That was **wrong,
and it would have been wrong in nearly every repo**: `verify-phase` §1 runs `npm init -y` in every
project to provision Playwright, so a bare `package.json` sits in essentially every .NET repo the
framework has touched. Detecting Node from that file alone would have appended bundler rules to 19 repos
that build none of them — noise in a file the owner reads, and `build/` is a plausible real directory to
shadow by accident. `_is_real_node()` now requires actual evidence of a build: a `build`/`dist`/`bundle`/
`compile` script, or a bundler config on disk. `npm init -y`'s output — one `test` stub that echoes and
exits 1 — is correctly not a Node stack.

**15 of 19 repos were missing build-output rules entirely**, which settles the question TF-007 companion 1
left open (*"every project the scaffold has ever created is likely to carry this"*). They have them now.
Two repos already had them; two are docs-only with no stack to detect.

**`TrSetup` carries 4,635 TRACKED build-output files** — 2,957 under `bin/`, 1,678 under `obj/`, across
four projects. This is the same defect TfLens reported at 1,962 files, in a repo nobody had looked at,
and it is **the whole reason the audit checks tracking and not just rules**: TrSetup now has correct
ignore rules and they are **completely inert**, because a tracked file is never ignored. The audit
printed the exact per-directory un-tracking commands. **Owner action — this framework does not run
version control, so the last step is not ours to take.**

### 2026-08-31 (2) — effort per phase: the metrics answer "what did each phase cost", and two of the three fields were already there

Owner question: *can the metrics we already capture tell us effort, time and tokens per phase — with
which model, how long, and how many subagents?* **Mostly yes, and it is worth being precise about which
parts were already true**, because two of three were.

- **Already there since 2026-08-20:** `cmd` (the phase), `started`/`ended`/`duration_s`, the four token
  counters, `model`/`models`, `tier`/`tier_model`/`routed`, `harness`, `mode`, `attempt`,
  `files_written`, `reqs_touched`, `cost_usd` (OpenCode only). **What was missing was an aggregation** —
  nothing grouped by `cmd`.
- **Genuinely missing, now added (SCHEMA §2.6):** `subagent_runs`, `tokens_out_subagents`,
  `model_tokens_out`. All three are **measured by `tf-emit.sh` from the harness's own store** — the
  subagent transcripts beside the parent's on Claude, the child sessions in `opencode.db` on OpenCode —
  never self-reported. `subagents` (a list an agent types) stays and says *which kinds*;
  `subagent_runs` says *how many actually ran*, and **where they disagree the measured one is right**.
  A transcript with no output inside the run's window is excluded, so a later phase cannot inherit an
  earlier one's fan-out.
- **`tf-metrics.sh --phases`** (and a `phases` block in `--report --json`) is the aggregation:
  per-`cmd` wall clock, tokens, per-model output **split**, fan-out, routing drift, dollars per harness.

**The provenance rule arrives on a fourth axis, and it is the whole design.** Fan-out is only visible on
a `tokens_scope:"tree"` record — a `main` window never read the subagent transcripts, so its `0` means
*not observed*, not *none ran*. Every fan-out figure is tree-only with the exclusion printed, split two
ways because they are two different facts: **"we did not look"** vs **"we could not have looked"** (the
field postdates the record). Token figures likewise exclude runs with no window rather than averaging
them in as zero — the TF-005 defect one stream over.

**Deliberately still unavailable:** per-feature / per-REQ cycle time (a standing §0 non-goal — the unit
is the run, and splitting one window across the REQs it touched is arithmetic, not measurement),
per-subagent detail, and dollars on Claude/Codex.

Consumer design record: **`docs/Phase-Effort-Telemetry-TfLens.md`** — the oracle contract, the parity
keys, the three denominators, and a `/effort` page design whose first rule is that it must look correct
at `observed_n = 1 of 13`, because that is what it will show first.


### 2026-08-31 (1) — TfLens's seven open entries closed: two new gates, a divisor rule, and two defects found in the fixing

**All seven open TfLens entries fixed** (`TF-005`, `TF-007`, `TF-008`, `TF-009`, `TF-010`, `TF-011`,
`TF-012`), plus the two owner questions that block had been carrying since 2026-08-27. Consumer-side
record and per-entry verification recipes: `docs/TfLens-TechieFlow-Feedback.md`, the 2026-08-31
resolution block.

**Two new gates, because three defect classes in three days passed every gate and were caught by a
human.** That is the finding, not the individual bugs: *every gate asserted something true about the
screen, and none of them asserted the thing that was wrong.* The gate set measured whether a screen was
**alive**, never whether it was **right**.

- **`assets` (§4a2, `tf-assets.sh`)** — parses what a page *declares* (`<link rel=stylesheet>`,
  `<script src>`, preload/modulepreload/icon, honouring `<base href>`) and asserts a 200 with a
  non-empty body for each. TF-007's escape was a build at 140/143 `Verified` shipping an unstyled
  `/login`: the Blazor scoped-CSS bundle 404ed and **no gate could have caught it** — acceptance passes
  because an unstyled page behaves correctly, and visual-truth passes because *partial breakage
  overlaps, complete breakage stacks neatly.* Dependency-free (python3 stdlib), ~100 ms on a healthy
  page, same-origin by default so a flaky CDN cannot cry wolf.
- **`mockup-parity` (§4b2, `tf-mockup-parity.sh`)** — eight structural classes at 1280 and 390, never
  pixel-wise, plus `document.scrollHeight <= clientHeight + 2`. TF-008's escape was a checklist reading
  145 `Verified` over an app with structural drift on 13 of 14 screens. **§4b's prose "mockup diff"
  bullet was deleted, not amended**: it had shipped for months and caught none of it, which is why this
  is logged as a `wrong-behaviour` miss rather than only a gap.

**The mockup gate is built around the three ways the consumer's own implementation failed**, which is
the more useful half of the feedback:

- **`stroke` (TF-009)** — a dashed grey border and a solid grey border are the same semantic *colour*
  bucket, so an estimate tile shipped styled exactly like the measured one beside it. `border-*-style`
  quantised to `none`/`solid`/`dashed`, plus `border-width: 0` vs a visible rule.
- **Coverage and `UNGRADEABLE` (TF-011)** — the gate's depth is bounded by what both sides anchor, so
  **the less of a screen it could see, the cleaner its verdict looked.** That produced a real false
  statement upstream: "10 PASS / 2 FAIL / 0 findings" with eight UI rows written `Verified` while a
  screen was visibly wrong. Every screen now publishes `{compared, content_graded, app_controls,
  mockup_anchors}` and returns **`UNGRADEABLE`**, never `PASS`, when no clause reaching *inside* a
  container fired. The floor counts comparisons that could have produced a finding, **not anchors** —
  the consumer was explicit that an anchor ratio is the wrong measure and was right. The walker now
  descends any anchored subtree (card, column, grid, `<dl>`, list) rather than tables only, which alone
  found both defects a human had found by eye.
- **sr-only (TF-012)** — the `clip` clause counted screen-reader-only text as overflow and failed 8 of
  10 screens identically. **A finding that appears on every screen always is the most expensive failure
  a gate can have**, because it trains a reader to skim the whole report — and shipping the deeper
  walker without this fix would have made it worse, not better. Visually-hidden elements are skipped
  and excluded from an ancestor's overflow measurement, in `clip`, `wrap` and `token`.

**`TF-005` became a schema rule rather than a one-line fix.** `analyse_misses` averaged an unrecorded
`tokens_out` as zero, so an unmeasured repair counted as a free one — wrong in the direction that
flatters the framework. The divisor now excludes them and **publishes itself** (`_n` keys, SCHEMA
**§5.5.8**): *every mean over an optional field divides by the records that carry it, and the excluded
records are counted and reported.* Publishing the denominator is what dissolved the consumer's
unwinnable choice between matching the reference and being correct; their `DECISIONS.md` D-012
divergence can now be retired.

**`TF-010`** — `render-workflow-docs` §5 mandated a "NEXT COMMAND TO RUN" box that `tf-render-html.sh`
never emitted, and the only workaround was overwritten by the next render. The tell was `--cta-bg`: a
shell variable defined in both palettes and consumed by nothing — the fingerprint of a feature dropped
when hand-authoring moved into the script under TF-003. The renderer emits it now, and the Output
Checklist item is finally falsifiable by a grep.

**`TF-007`'s two companions also shipped.** `tf-gitignore-audit.sh` (wired into both scaffolds,
`update-framework.sh` block 8b2, and a new **day-1 §5b** in both variants) detects the stack **from the
tree**, adds the build-output rules, and reports build output that is **already tracked** — the half
that matters, because a tracked file is never ignored whatever the ignore file says. It contains **no
git**: it parses `.git/index` directly and prints the un-tracking commands for the owner. And
`_smoke-test-policy.md` gained *"if the harness cannot reproduce a construct's failure, the harness
cannot sign it off either"*.

**Two defects found while fixing the above — both ours, neither reported by the consumer.**

1. **`tf-emit.sh` validated the `miss-amend` allowlist meticulously and a `miss` record's own enums not
   at all.** Found by making exactly that typo while logging these entries: an invalid `miss_class`
   landed permanently on an append-only stream, invented a category in every distribution built on it,
   and **could not be corrected** — `miss_class` is not amendable and constraint 5 forbids editing the
   file. The emitter now refuses any value outside the closed vocabularies and prints why (SCHEMA
   **§5.5.7b**). *Two lines written during this session — that malformed record and a test record that
   leaked onto the live stream — were removed rather than left, because neither was ever a fact and a
   fabricated measurement on a stream whose purpose is honest measurement is worse than the deletion.
   Flagged for the owner rather than done silently.*
2. **`analyse_misses` honoured a stored `cost_attribution:"sole"` and skipped the report-time recount —
   in the headline column.** The emitter stamps attribution one record at a time, so a run closing nine
   misses writes `sole`, `shared:2` … `shared:9`; the **first** is `sole` because at that instant it was
   the only miss closed. The recount §5.5.3 exists to perform then short-circuited on it, so **one whole
   multi-miss window was reported as the measured cost of a single repair, once per multi-miss run,
   silently and upward.** On this repo it inflated `cost_sole_n` 2→3 and produced a
   `tokens_per_miss_measured` of 99,974 where the honest answer is *insufficient data (n=2)*.

**Constraint 1 narrowed — the consumer was right, twice.** `_metrics-emit-gate.md` said `tf-metrics.sh`
was flatly "owner-run" while the script's own header, `has_commit_hook()`'s docstring and
`metrics-report.md` §1 all said `--report` is agent-safe. It now reads: **owner-run in `--backfill-*`
modes only**; `--report` / `--rollup` / `--phases` are agent-safe. A consuming agent had twice declined
to run a procedure it was entitled to run.

### 2026-08-28 (6) — Claude-side review after the Codex + OpenCode passes: a stale YOLO flag had left this repo unattended-permissive for four days

Owner asked for a Claude-side verification after the Codex and OpenCode sessions, on the reasoning that those sessions had not touched Claude files but shared files could still have drifted. **Clean:** the `.tfcore/` → `.claude/commands/TechieFlow/` mirror is byte-identical for every agent and task (`diff -rq`, no output); all 8 hook scripts named in `.claude/settings.json` exist and parse; every framework `.sh` passes `bash -n` and every `.py` compiles; all 31 `opencode.jsonc` `{file:}` refs resolve; every public task and agent is registered (only the three `_`-prefixed shared rules are unregistered, correctly — they are includes, not commands); `block-git.sh` behaves correctly on all four verdicts. **Four defects, all fixed:**

**1. `tf-goal.sh` never turned YOLO off — the flag outlived the run (the serious one).** Line 345 writes `.tfcore/.session/yolo.json` via `tf-yolo.sh on --source goal`, and **no exit path removed it** — not goal-done, not max-cycles, not `--dry-run` (which exits *after* the flag is written), not Ctrl-C. `block-git.sh` line 40 treats that file's mere existence as YOLO. So a goal run silently leaves the repo in YOLO **forever**: `rm`/`rmdir`/`sudo` stop prompting, read-only git opens, and nothing on screen says so. This repo had been in that state since **2026-08-24T12:49Z** — the flag found on disk was `{"source":"goal","goal":"test goal"}`, a leftover from that day's `tf-goal.sh` smoke test, so **every interactive Claude session here for four days ran with delete-prompts suppressed**.

A scan of the other repos then showed it was not one repo's accident: **`AppManager` (goal, 2026-08-24), `TechieBlog` (agent, 2026-08-22 — six days), `TfLens` (agent, same day), `TrBlazeUI` (yolo, 2026-08-25)** were all still flagged on. That breadth exposed a **second, larger root cause**: `tf-yolo.sh done` — the sentinel `_yolo-mode.md` §Completion prescribes as the **last action of every run**, and which line 58 tells the agent to write even in a plain interactive `*yolo` — wrote the outcome file and **left the flag alone**. So nothing in the whole framework ever turned YOLO off except the owner typing `off` by hand. The well-behaved path leaked exactly as badly as the crashed one.

Three fixes, layered so each covers the case below it: (a) **`tf-yolo.sh done` now clears the flag** — the grant ends with the goal, and the supervisor reads `goal-done.json`, never the flag, so nothing stops early; (b) **`tf-goal.sh` clears its own flag on every exit path** — `clear_goal_yolo()` + `trap … EXIT INT TERM`, removing it **only when `"source":"goal"`** so a hand-set `*yolo` is never clobbered (this also stops `--dry-run`, which exits *after* the flag is written, from leaving one behind); (c) for a session killed before either runs, the on-disk flag **expires after 24 h** (`TF_YOLO_TTL_HOURS`, `0` disables) in `tf-yolo.sh`, `block-git.sh`, **and** `.opencode/plugin/techieflow.js`, so the two harnesses cannot disagree. Age comes from the file's mtime via `find -mmin` rather than parsing the ISO `since` field, because `date -d` does not exist on macOS and this repo is dual-homed. **`TF_YOLO=1` is checked first and never expires**, so a multi-day `tf-goal.sh` run sleeping through usage-limit windows is untouched. Verified end to end: fresh `on` allows read-only git; `done` clears it and the hook blocks again; a 30 h-old flag is ignored by both the hook and `status` (which now says `OFF (flag EXPIRED …)`) while `TF_YOLO=1` still wins; git writes stay blocked throughout. `_yolo-mode.md` documents all three (mirrored). Existence-means-on was never the bug — it is the documented contract and `off` always worked; **nothing ever called it**.

**2. `tf-goal.sh` line 151 executed a rules file instead of naming it.** The Codex policy note appended `` `.codex/rules/techieflow.rules` `` inside a **double-quoted** assignment — unescaped backticks are command substitution, so every Codex goal run tried to *run* the rules file (`syntax error near unexpected token 'newline'` to stderr) and substituted empty, shipping the agent a note reading "CODEX POLICY NOTE:  forbids every git/gh command". Survived `bash -n` because it is valid syntax, just not the intended one. Backticks escaped.

**3. The 2026-08-28 (4) Codex pass applied Codex's git policy to all three harnesses.** `PREAMBLE` (lines 128–139) is shared by claude/opencode/codex — the Codex-specific note is appended separately at 148–152, which is the right seam. The permissions line had been rewritten to *"Every git/gh command, including read-only diagnostics, is blocked in every mode"*, which is true only under Codex (`TF_STRICT_GIT=1`, set solely by `codex-adapter.py:72`). Under Claude and OpenCode read-only git **is** allowed in YOLO — per `block-git.sh`, `tf-yolo.sh`'s own ON banner, all four agent personas, `_status-update-gate.md`, `user-guide.md`, and README §12a — so the preamble was telling Claude goal runs to avoid something the harness permits. Restored to harness-neutral wording that defers to the per-harness note below it.

**4. The loose `/generate-html` Claude wrapper — the item 2026-08-28 (5) correctly left for the owner.** Confirmed stale (zero references to `tf-render-html.sh`; still the pre-TF-003 hand-authoring path). It is not merely a local command: `update-framework.sh` line ~400 force-overwrites `.claude/commands/*.md` in **every app** from this copy, so the drift shipped everywhere. Regenerated as *Claude header + the canonical `.tfcore/tasks/generate-html.md` body embedded VERBATIM* (verified byte-identical), matching how the OpenCode twin was fixed; the header now also warns that this file propagates.

**Field evidence for the `done` root cause, found while clearing the flags.** `TechieBlog`, `TrBlazeUI` and `TfLens` each hold a `goal-done.json` sentinel — written 35 min, 49 min and 4 h 04 min after their respective YOLO flags, the last being a full `*handoff-phase` completion summary. All three agents did exactly what `_yolo-mode.md` §Completion prescribes, ran `tf-yolo.sh done complete "…"` with a real summary, and the flag stayed on regardless, for 146 h, 72 h and 6 h. **Three for three: every repo that finished its run correctly still leaked.** The compliant path leaked as badly as the crashed one — which is the whole argument for fixing it at `done` rather than only at the supervisor. All five flags are now cleared (`TfLens` last, at the owner's instruction once its sentinel confirmed the run had finished).

**Recorded on the miss stream** (framework repo — no checklist to demote, so records only, per `log-miss.md` §4): **MISS-14** the unclosed YOLO grant (`partial-implementation` / `src` / **major** / `missing-checklist-item` — nothing ever specified that the grant must end with the run; `origin_confidence: unknown`, as no `runs.jsonl` records exist for the 2026-08-21/24 sessions that wrote this code); **MISS-15** the backtick command substitution and **MISS-16** the Codex policy in the shared preamble (both `wrong-behaviour` / `src` / minor, both **`linked`** to the Codex adapter review's run `2026-08-28T13:01:24Z`, `gpt-5.6-sol` — the maintenance log's 2026-08-28 (4) entry names *"stale unattended CLI syntax"* and *"contradictory Codex YOLO git wording"* as that review's own fixes, which is what pins the attribution rather than a guess between two candidate runs); **MISS-17** the stale Claude `/generate-html` wrapper (`partial-implementation` / `config` / major / `insufficient-verify-method`, matching its OpenCode twin MISS-12's classification). MISS-17 is logged as its own record rather than collapsed into MISS-12 because MISS-12 had already been closed `Verified` while half the defect — the twin that `update-framework.sh` ships to **every** app — was still live. All four closed `miss-fix` / `Verified` against this session's `fix-issues` run; `cost_attribution: none` on all four, correctly, since a framework repo has no REQs for the window to attach to. MISS-16's `missing-checklist-item` is now actually answered: `tf-goal.sh` carries a comment above `PREAMBLE` stating that it is harness-neutral and that per-harness claims belong in the block below — the rule existed only as a code seam before.

**Then the owner rejected `cost_attribution:"none"` on those four records — and was right (MISS-18).** The fix run had a real measured window (`tokens_scope:"main"`, 71,767 output tokens, 10.2 M cache read, `claude-opus-5`) and had been spent on those four misses and nothing else, so calling it unattributable discarded a measurement the stream had already taken. The cause was one unhandled branch: `tf-emit.sh` derives the divisor from the fix run's `reqs_touched`, and **a `framework` or `docs` repo has no REQs at all**, so `len(touched) == 0` fell through to `else: "none"`. That contradicts §5.5.3's own definition of `none` — *"tokens_scope was none, or fix_run_id matched no run record"* — neither of which was true. **The cost of a framework fixing its own misses was structurally zero, permanently.** Exactly the blind spot shape of the `project_type:"framework"` defect fixed earlier the same day: a derivation keyed on something apps have and the framework does not. It also explains why the Codex session had hand-written `cost_attribution:"shared:4"` on its own records, in violation of constraint 10 — the derived field was wrong, so it was overridden by hand rather than fixed at source, which is precisely the "attribution is looked up, never typed" principle inverted.

Where `reqs_touched` is empty the divisor is now **the number of distinct misses that run closed** (`miss_id`s sharing the `fix_run_id`). That count is not knowable at emit time — an append-only stream writes one `miss-fix` at a time, so a run closing four stamps `sole`, `shared:2`, `shared:3`, `shared:4` and only the last is right — so **`tf-metrics.sh` recomputes it per `fix_run_id` at report time** and does not trust the stored value (§8: derived metrics are computed, never stored). That is what makes the fix retroactive: **no record was edited and nothing was backfilled**, yet all four now read `shared:4` (≈17,942 output tokens per miss), because the divisor was countable from the stream all along. `--report` prints a `recovered` line naming how many windows the corrected derivation reclaimed, so the jump reads as a fixed derivation rather than as work suddenly costing more. Logged as **MISS-18** — `partial-implementation` / `src` / **major** / `missing-checklist-item` (§5.5.3 enumerated three cases and never said what an empty `reqs_touched` means), **`found_by: owner`** — the only miss in this session the owner caught rather than the review.

**Propagated to all 19 repos.** `update-framework.sh` run against every repo carrying a `.tfcore/`; all 19 succeeded and were verified to carry all six changes (`tf-yolo.sh` expiry, `tf-goal.sh` trap, the regenerated `/generate-html` wrapper, `tf-emit.sh` cost derivation, `tf-metrics.sh` recomputation, the OpenCode plugin's TTL). Two pre-existing legacy-pointer warnings surfaced and were **not** touched, being owner-owned content the script deliberately never edits: the two `AI-First-Development-*.html` files in `AI-First-Playbook-Source-PRIVATE`, and `docs/TechieFlow-TrBlazeUI-Feedback.md` in `TrBlazeUI`, all still naming `.bmad-core` / `/BMad:` / `bmad-master`.

**Also fixed, owner-reported:** the `claude` CLI was printing a permission-rule warning on every launch here — `.claude/settings.local.json` carried `Bash(ls *.md *.slnx global.json)`, whose `*` sits *before* more command text and therefore also matches inserted options. Replaced with `Bash(ls:*)`. A scan of the other 118 allow rules found no second instance of that pattern.

### 2026-08-28 (5) — OpenCode-harness review: the loose `/generate-html` wrapper still taught the hand-authoring path TF-003 removed

Reviewed every OpenCode surface after reading this file end to end: `opencode.jsonc` (all 31 `{file:}` refs resolve; every public task registered — only the four `_`-prefixed shared includes are unregistered, by design), the `.claude/commands/TechieFlow/` mirror (byte-identical to `.tfcore/tasks/` + `agents/`, `diff -rq` clean), `.opencode/plugin/techieflow.js` (guard bridge, YOLO `permission.ask`, session telemetry, stale-HTML nudge — all consistent with the documented behaviors), the 19 `.agents/skills/techieflow-*` wrappers (thin pointers at the canonical tasks, generated by `tf-codex-bind.py` — no drift possible), and the two NuGet persona copies in `.opencode/command/` (both carry the 2026-08-15 Docker NuGet preflight; library-owned, not edited).

**One real defect:** the loose `/generate-html` wrapper `.opencode/command/generate-html.md` was a stale pre-2026-08-27 copy. The 2026-08-27 TF-003 pass rewrote §2 of the canonical task to mandate `bash .tfcore/utils/tf-render-html.sh` and to forbid hand-authoring, and re-synced the `.claude/commands/TechieFlow/` mirror — but nobody regenerated the *loose* wrapper that embeds its own copy of the task body. It still read *"Use the Write tool to create the sibling HTML"* and carried the full 8-step hand-authoring recipe. Because `/generate-html` is a top-level OpenCode command, an owner typing `/generate-html docs/X-UsageGuide.md` would have steered the agent straight back into the 75–80k-token hand-authoring path TF-003 removed — while `opencode.jsonc`'s registered `techieflow:tasks:generate-html` (a `{file:}` ref, always current) pointed at the renderer. Two doors into the same command, opposite doctrine.

**Fix (OpenCode only, per the session's mandate):** the wrapper regenerated as *per-harness header + the canonical task body embedded VERBATIM* (verified byte-identical after the header via `diff`). The header keeps its frontmatter `description` + the plain-paths (no `@`) note; the old OpenCode-specific rewrite of the invocation examples was dropped — the canonical task body itself now documents both harnesses' forms, and that local rewrite is exactly what made the wrapper look maintained while its §2 rotted. The header now states plainly: *this file embeds `.tfcore/tasks/generate-html.md` verbatim — regenerate it whenever that task changes; do not edit the embedded body here.* No `.tfcore/` file changed, so no mirror re-sync was needed and `update-framework.sh` propagation is unaffected (the loose wrapper is framework-source, not a deployed artifact).

**Session miss, reverted:** the session ALSO regenerated `.claude/commands/generate-html.md` (the Claude twin, same staleness) — outside its OpenCode-only mandate. The owner ordered it reverted; restored byte-exact (the pre-edit diff fully determined the original, and the post-revert diff against the canonical task reproduces the original three hunks exactly). **That wrapper's staleness is real and remains OPEN** — it still teaches the hand-authoring path — but fixing it is the owner's call (or a Claude-side session's), not something an OpenCode-scoped session does unilaterally. The overreach is logged in the framework's own miss stream (`instruction-ignored`; see `docs/metrics/misses.jsonl`) at the owner's instruction, via `*log-miss`'s record-only path (§4 carve-out — no checklist in this repo). **Owner action:** restart any open OpenCode session to load the refreshed wrapper; decide separately when/how to fix the Claude twin.

### 2026-08-28 (4) — Codex adapter review: nested-directory hooks and protected-file deletion fixed

Reviewed the implemented Codex adapter against installed `codex-cli 0.150.1` and the current official Codex manual. Two implementation defects and two documentation inconsistencies were confirmed. **Hook launch path:** `.codex/hooks.json` invoked `python3 .tfcore/hooks/codex-adapter.py ...`; Codex runs hook commands with the session cwd, so every hook failed when a session started in a nested project directory. Each command now resolves the repository root before launching the adapter, matching the official hook guidance. **Protected delete bypass:** `codex-adapter.py` parsed only `*** Update File:` and `*** Add File:` sections; an `apply_patch` containing `*** Delete File: PROJECT-STATUS.md` or `*-Checklist.md` skipped both guards. Delete sections are now parsed and those canonical protected documents are denied directly. **CLI/docs:** `CodexChanges.md` still showed the rejected `codex exec --ask-for-approval never` form even though `tf-goal.sh` had already moved to `-c approval_policy="never"`; the example is corrected. **YOLO policy:** the goal preamble allowed read-only git while its Codex continuation note and the repository-wide policy forbade every git/gh command; it now states one policy consistently. Syntax, JSON/TOML parsing, nested-cwd hook launch, git-command denial, protected-delete denial, and non-protected patch allowance were re-tested. Hook-definition hashes changed, so owners must review/trust the refreshed hooks after deploying them.

### 2026-08-28 (3) — TfLens TF-005: the append-only rule named a remedy the stream did not implement (+ TF-004)

**Reported by the TfLens team, and the diagnosis was theirs, not mine.** They also corrected their own first draft of it, which is worth recording: the entry originally read as *"`log-miss` omits `why_missed` while the emit gate requires it"*, and that is false — `log-miss.md` carries the field in four places. The timeline settles it: their `*log-miss` run finished at **07:13:02**; `update-framework.sh` added `why_missed` at **07:17:47**. **A field that does not exist yet cannot be omitted**, so neither the task nor the agent missed anything, and their `instruction-ignored` self-diagnosis was withdrawn correctly. (Filed as TF-005 rather than TF-004 — that id was already taken.)

**What they actually hit is real, general, and was a defect in a *rule* rather than in a field.** `_metrics-emit-gate.md` constraint 5 says *"if a record is wrong, the correction is a **new record**, never an edit"* — and `misses.jsonl` had **no record kind that could carry a correction**: `miss` opens, `miss-fix` closes, and re-emitting a `miss` is barred by the §5.5.4 collapse rule (and would have double-counted, which is exactly what collapse exists to prevent). So a field left `null` was **unreachable**: leave it empty forever, or break constraint 5. The only way to give the framework the `why_missed` it now asks for was to violate the rule that protects the stream. They presented the conflict instead of deciding it, and the owner authorised a two-line in-place edit as a one-off.

**Fixed with a third record kind, not with a licence to edit.** `kind:"miss-amend"` (**SCHEMA §5.5.7**) + `bash .tfcore/utils/tf-emit.sh --amend <miss_id> <field> <value>`:

- **It may set a field that is `null`; it may never overwrite one that is not** — including a value an earlier amend set. That single invariant is what keeps it inside append-only *in substance* and not merely in form: it completes a record rather than altering a fact, every earlier state of the stream stays true, and a reader that ignores `miss-amend` records entirely still sees nothing false — only less.
- **The boundary that makes an amend safe, and the part worth remembering: a judgement may be completed, an observation may not.** `why_missed` is a classification a reader can still make honestly next week. `found_gate` is a fact about a run that is over — which is §3.5's *"never backfill the old records with a verdict they never had"* seen from the other side. Everything the emitter derives (`origin_model`/`origin_harness`/`origin_confidence`, `cost_attribution`, every token/cost field) is **excluded outright**, so the kind can never become a back door onto attribution or cost. Allowlist today: `why_missed` alone; the extension rule is written down so the next field is a one-line change rather than a re-argument.
- **Two doors, one enforcement.** `--amend` validates and appends; a hand-written `miss-amend` piped onto the stream faces the identical checks in `enrich_miss()` and is **dropped** if it fails them. Refusals **print** rather than failing silently (an agent that believes it recorded a correction and did not is worse off than one told no), and everything still exits 0 — telemetry has no veto, and neither does a refused amend.
- **Their cheaper alternative was taken as well, not instead.** `tf-metrics.sh` gained **`FIELD_SINCE = {"why_missed": "2026-08-28"}`** beside the existing `LATE_GATES` table: a miss written before an optional field existed leaves that field's denominator and is **reported separately**. The two fixes cover different cases — the amend path handles records where the answer is still known; the floor handles records nobody can honestly assess any more. Without the floor, every future optional field would make the practice look worse the day it ships.
- **Constraint 5 was rewritten to name *which* new record carries which correction** (a later `gates.jsonl` verdict supersedes; `miss-fix` closes; `miss-amend` completes) **and to say plainly: if none of them fits, stop and report it as a framework defect rather than editing the file.** That is precisely what TfLens did, and it is the behaviour to keep — the rule was fixed because someone refused to quietly work around it.

**Verified end to end** on a scratch repo: fills a `null` ✓ · refuses an overwrite ✓ · refuses a second amend of the same field ✓ · refuses a value outside the vocabulary ✓ · refuses a non-amendable field (`origin_model`) ✓ · refuses an unknown `miss_id` ✓ · a hand-written overwrite on the piped path is dropped, stream unchanged ✓. Reader side: amendments folded before counting, orphan amends counted and never applied (including a merged-stream `origin_model` amend), `2 of 2 assessed` with the pre-field record excluded and stated, and the escape warning still firing — now naming `--amend` as the remedy — for an escape that *could* have carried the field. Regression: normal `miss` / `miss-fix` emit, `--next-miss-id`, `--open-miss` and this repo's own `--report` all unchanged.

**Also fixed the same pass — TF-004 (Low).** `tf-render-html` refused **any** file ending `-Checklist.md`, including a human deployment runbook the owner had asked for by name. The §0 ban is about `docs/{AppName}-Checklist.md`, the agent's Requirements Status document — a deployment, release or QA checklist merely shares the word. The false positive matters more than it looks: the workaround is a rename round-trip, and the *easy* wrong answer is to hand-author the HTML, which is the exact path TF-003 removed. Now **content-identified** (their own preferred option): the name must match **and** the file must carry `## Requirements Status` or the template's `SINGLE SOURCE OF TRUTH` marker. Verified against TfLens's real checklist (still refused, exit 2) and a runbook fixture (renders).

**Files:** `tf-emit.sh` (`--amend`, `_AMENDABLE`, `_amend_ok()`, drop-on-refusal in the append loop, header) · `tf-metrics.sh` (`AMENDABLE_FIELDS`, `FIELD_SINCE`, amendment folding, `_eligible()`, four new keys, report block) · `tf-render-html.py` (`is_requirements_checklist()`) · SCHEMA §1/§5.5/§5.5.6/**§5.5.7**/§8 · `_metrics-emit-gate.md` (constraint 5 + the writer table + the `why_missed` block) · `log-miss.md` (complete an open record instead of discarding what the owner just said) · `metrics-report.md` + template §5 · `install-metrics.sh` (generated `docs/metrics/README.md`) · `WORKFLOW.html` §17 (new subsection, rev 2) · `README.md` §17b · `docs/TechieFlow-Telemetry-Guide.md` §3.5 **+ `.html` re-rendered** · the three `docs/Miss-Telemetry-*.md` design records · `docs/TfLens-TechieFlow-Feedback.md` (resolution section; TF-004/TF-005 marked fixed) · Claude mirror re-synced byte-identical (`diff -rq` clean for `tasks/` and `agents/`).

**Deployed to all 19 WSL repos the same day** — TfLens first at the owner's instruction, then the remaining 18 in two batches. All rc=0, all reached "Framework updated"; a per-repo audit confirms `--amend` in `tf-emit.sh`, `FIELD_SINCE` in `tf-metrics.sh`, SCHEMA §5.5.7, `is_requirements_checklist()` in `tf-render-html.py`, the amend text in `_metrics-emit-gate.md` + `log-miss.md`, a byte-identical Claude mirror and a seeded `misses.jsonl` in **all 19**. Live smoke in TfLens: the deployment runbook that TF-004 blocked **renders** (37.7 KB, 13 H2) while `TfLens-Checklist.md` is still refused with exit 2, and `--amend` on their `MISS-TfLens-20260828-01` correctly **refuses** because the in-place edit already set the field. Refusal path re-checked in three more repos: exit 0, nothing appended. Two pre-existing advisories unchanged and deliberately not auto-edited (AI-First-Playbook-Source-PRIVATE and TrBlazeUI carry owner-authored files referencing the legacy layout).

**Both defects were logged as misses in the framework's own stream** — at the owner's prompting, and the prompt was fair: the whole point of the miss stream is that a defect becomes a record rather than a maintenance-log paragraph, and a framework's own misses count exactly as much as an app's. `MISS-TechieFlow-20260828-05` (TF-005 — `spec-contradiction` / `architecture` / major / `why_missed: missing-checklist-item`: nothing in the design's acceptance said *the correction path the doctrine promises must exist for this stream*) and `-06` (TF-004 — `wrong-behaviour` / `src` / minor / `insufficient-verify-method`: the 2026-08-27 renderer validation did check "checklist refused", but only against the canonical checklist, so no check could have caught the over-broad suffix match). Both `found_by:"library-feedback"` — the first records to use that value, and the consumer-feedback loop is now a measurable source. Both closed `Verified` via `--fixed`. **Attribution came out `unknown` on both, deliberately:** the framework's own maintenance sessions are not phase runs, so `runs.jsonl` had nothing to link to; naming a `build-phase` that never ran would have been an invented attribution, and the emitter nulled the model instead. Record-only path (no checklist, no `PROJECT-STATUS.md` in this repo) — the §4 carve-out written for exactly this case, exercised for the first time.

**Doc audit after the fix, since a third record kind invalidates prose written for two:** `Miss-Telemetry-TfLens.md` gained the `miss-amend` dedupe key, a third table + index + purge in §3.3, read-time folding in `RebuildAsync`, and six new amend-folding tests plus an eligibility-floor test in §3.8; `Miss-Telemetry-AI-First-Playbook.md`'s `playbook-miss.mjs` row gained `amend`; `Miss-Telemetry-TechieFlow.md` §4.2 and §6 now say plainly that the design shipped three kinds and that an amend is **not** a licence to edit. `docs/TfLens-TechieFlow-Feedback.md` gained a **"What changes on YOUR side"** pick-up list (drop the rename round-trip; their two records need nothing because an amend cannot overwrite; the three-kind parser, read-time folding, `FIELD_SINCE`, the two open-predicates and the four new parity keys when they build `/misses`) and was **copied into `/mnt/c/1MyCode/TfLens/docs/`** so the team reads it where they work — the two copies were byte-identical beforehand, so nothing of theirs was overwritten.

**Owner action:** the **Mac clones** still need `update-framework.sh <repo>` — the deployment above covered the Windows/WSL side only, and `.tfcore/` is gitignored in app repos so a pull does not carry it. Restart any open Claude Code session in a refreshed repo so the new task definitions load.

### 2026-08-28 (2) — rollout begun, and the rollout found a classification bug: every greenfield project was born `docs`

**The framework now measures itself.** The owner ran `install-metrics.sh .` and `tf-metrics.sh --backfill-commits .` here: `project_type: framework` resolved structurally (nothing written to `core-config.yaml`, per the carve-out in the entry below), the `pre-commit` hook installed, and **30 commit records reconstructed back to 2026-06-25** — the repo's whole history, which had been in the log all along and never asked for. With the `SessionEnd` hook already collecting (11 sessions, 2.25M tokens), all five streams are live in this repo.

**Worth restating because it was mis-scoped once already:** four of the five streams need **no version-control hook at all** — `runs`/`gates`/`misses` come from `tf-emit.sh` in the phase tasks, `sessions` from the `SessionEnd` hook (a harness hook, not a VCS one). Only `commits.jsonl` uses `.git/hooks/pre-commit`, and only for commit volume and cadence; nothing in the miss data depends on it. And even that hook is optional — `--backfill-commits` reconstructs the identical records perfectly at any time. Added to the Telemetry Guide FAQ.

**Then TfLens was refreshed first, and its report exposed a real defect.** TfLens was classified **`project_type: docs`** while carrying **225 gate records including visual gates** — a type whose own definition in SCHEMA §1 says gates cannot fire, and whose figures never pool with the apps it belongs beside (§6). Not a one-off: `scaffold-greenfield.sh` runs `install-metrics.sh` **at scaffold time**, when a greenfield repo is by definition docs-only (day-1 documents exist, `src/` does not). `detect_type()` correctly answers `docs`, writes it, and "auto-detected once, then never re-guessed" freezes it. **Every greenfield project is born `docs` and stays there until somebody notices.** A survey of all 20 instrumented repos found TfLens the only casualty so far — the others are brownfield or were refreshed after their source existed — but the hazard fires 100% of the time on the owner's primary flow.

**Fix, deliberately narrow:** `install-metrics.sh` now re-examines **`docs` alone** on a later refresh, and **only upgrades**. Once real heads or a published package appear, the tree has answered a question that was unanswerable at scaffold time. `app`/`library`/`framework` are never re-guessed, `--type` always wins, and a genuine docs repo never grows a head so it is never touched (verified against Sevak: untouched; TfLens: `docs` → `app`).

**The half of it that must NOT be "fixed":** the 225 records already written keep `project_type:"docs"`. The streams are append-only and corrections happen at **read time**, never by rewriting history (§6) — so a reclassified project legitimately appears under **both** segments, which §6 forbids pooling. Silently, that makes one project look like two with no clue why. `tf-metrics.sh --report` now **states the split** whenever a repo's current classification disagrees with the classification on its own records, and says to read each segment as a *period* of the project rather than the whole of it. Verified: prints for TfLens, silent for TechieBlog.

**TfLens refresh result:** `log-miss.md` deployed, `misses.jsonl` seeded, Codex adapter regenerated (9 agents / 19 skills), Claude mirror byte-identical, `opencode.jsonc` refreshed (old → `.bak`), classification upgraded `docs` → `app`. Its own report now reads `TfLens app · gates 225 · runs 6 · sessions 6 · commits 9 · misses 0`.

**Also demonstrated in passing:** `block-git.sh` refused a `bash` heredoc that merely *contained* the word "git" while writing this documentation, and its message named the right remedy (use Write/Edit, which is the standing tool-preference rule anyway). The guard is behaving exactly as designed — including its deliberate over-broad matching.

### 2026-08-28 — MISS TELEMETRY: a fifth stream that records what an agent missed, who missed it, and what fixing it cost

**Owner-reported, and the sharpest framing of it yet:** *"the AI agent in the build phase misses the requirements — not only build, even design phase or any other phase — and there is no way to document those problems, and no way to identify how many tokens or how much money is spent on fixing those misses."*

**The gap, stated precisely, because three of the four questions were nearly answered already.** `gates.jsonl` answers "did REQ-X fail" and `gate:"escaped"` answers "did anything catch it". Neither answers **which phase introduced it**, and nothing at all answered **what the repair cost**. Four concrete holes:

1. **`gates.jsonl` only fires at verify.** It is written by `verify-phase` §6a and `triage-issues` §6a. A miss found during `*build-phase` — the builder reads the checklist and discovers the BRD never specified the export screen — produced **no record of any kind**. Design-phase misses were invisible end to end, which is exactly the case the owner named.
2. **`escaped` is a detection axis, not an attribution one.** It says "no gate caught it", never "which phase let it through". That design is right and is untouched.
3. **There was no unit of work called "a miss."** `gates.jsonl` is a *verdict* stream — one record per REQ per verify run, append-only. A miss has a lifecycle across runs, and bolting lifecycle state onto a verdict stream means editing records, which SCHEMA §6 and `_metrics-emit-gate` constraint 5 forbid outright.
4. **Cost was per-run and unsplittable.** `tf-emit.sh` enriches a run with the token window between `started`/`ended`; that window belongs to the *run*. A `fix-issues` run touching five REQs produced one number for all five, and nothing apportioned it.

**The fix: `docs/metrics/misses.jsonl` (SCHEMA §5.5)** — the first stream carrying **two record kinds**, `miss` (opened) and `miss-fix` (closed), linked by `miss_id`, so a lifecycle is expressed without ever rewriting a record. §1's `kind` rule was amended deliberately from "must match the stream file" to "must be one of the kinds the stream file declares".

**Three design moves carry the whole thing, and each removes a judgement from the agent rather than trusting it:**

- **Attribution is looked up, never typed.** An agent supplies `origin_phase`, `origin_agent` and `origin_run_id`; `tf-emit.sh` resolves `origin_model` / `origin_harness` / `origin_confidence` from the matching `runs.jsonl` record and **forces the model to `null` when the lookup fails, overwriting whatever was supplied.** Same rule as `harness` (a shared task template cannot know it), sharper consequence: a per-model miss rate built from guesses is a routing decision made on invented evidence, and nothing in the output would reveal it. `origin_confidence` (`linked` / `inferred` / `unknown`) became the **third provenance separation** in §6.
- **Cost is copied, never computed — and never silently apportioned.** A `miss-fix` carries only `fix_run_id`; the emitter copies that run's already-enriched window and derives **`cost_attribution`** from its `reqs_touched`: `sole` (the run fixed one REQ — a real measurement), `shared:<n>` (equal division — arithmetic, not measurement), `none` (no window; a miss fixed inline cannot be costed, and is printed as such rather than dropped). `tf-metrics.sh` prints measured and apportioned as **separate columns and has no code path that blends them**.
- **One defect is one miss.** `--open-miss <REQ>` is the collapse check (§5.5.4): a REQ that fails three verify passes collapses onto the open record unless the `miss_class` changes. Without it the miss count would measure retry patience, not quality.

**The money question, answered honestly and written down so it stops being re-derived:** OpenCode reports **real provider dollars**; **Claude Code and Codex carry `cost_usd: null` permanently** — no cost source exists on either, and no rate card is applied anywhere in this framework to make a dollar figure appear. Tokens are the honest figure everywhere else. (TfLens already owns the labelled-estimate path for pricing tokens; that stays a read-time concern and never enters a stream.)

**Six front doors, ordered by volume:** `verify-phase` §6a (every failing gate also opens a miss — the zero-effort majority) · `build-phase` **§4a, new** (specification gaps: `miss_class:"unspecified-gap"`, the design-phase capture that had no home) and its FIX-mode close · `triage-issues` §6a (`found_by:"owner"`/`"production"`, alongside the unchanged `escaped` record) · `fix-issues` §6a (the `miss-fix` writer; run record emitted **first** so the window exists to copy) · `amend-docs` §7a (only when the docs *should* have said it — a change of mind is scope arriving late, not a miss) · and the **new `*log-miss` command**.

**`*log-miss {App} "<what was missed>" [--fixed]`** (`.tfcore/tasks/log-miss.md`) is the 20-second front door and the direct answer to *"there is no way to document the issues"*. `*triage-issues` boots the app and reproduces before it writes a word — right for UAT triage, far too heavy for one sentence — so one-line misses became a chat message, then a Remark if the owner was patient, then nothing. **An unrecorded miss is the entire problem.** `*log-miss` classifies, attributes, writes the record *and* the checklist line (demotion with a dated `⚠ miss` remark, or a new `Planned` row with acceptance), and never boots, reproduces, or touches `src/`.

**A latent defect found and fixed on the way in: the framework repo could not classify itself without corrupting every app made from it.** `install-metrics.sh` detects `framework` correctly, but writing `metrics.project_type: framework` into `.tfcore/core-config.yaml` would have been fatal — the scaffolds copy `.tfcore/` with `rsync -a --ignore-existing`, so that value lands verbatim in every new app, and an existing classification always wins over the heuristic. Every app scaffolded afterwards would have been silently misclassified as `framework`, and pooled wrongly in every segmented figure. Fix: the framework is detected **structurally and authoritatively** — `scaffold-brownfield.sh` beside `.tfcore/tasks/` is the framework template and nothing else can be — checked ahead of `core-config.yaml` in `tf-emit.sh` `_project_type()` and `tf-metrics.sh` `project_type()`, with `install-metrics.sh` now writing nothing in that case and saying why. Verified: a fresh non-framework repo still classifies normally (`docs` on a skeleton with no `.csproj`), and this repo now reports `framework` with its `core-config.yaml` untouched.

**Files:** SCHEMA §0/§1/§5.5/§6/§8/§11 · `tf-emit.sh` (+`misses` stream, `--next-miss-id`, `--next-fix-attempt`, `--open-miss`, `enrich_miss()`) · `tf-metrics.sh` (+`analyse_misses()`, `print_misses()`, the two exclusions enforced in code) · `install-metrics.sh` (fifth stream + the framework carve-out) · `_metrics-emit-gate.md` (constraint **10**) · `verify-phase` / `build-phase` / `triage-issues` / `fix-issues` / `amend-docs` · new `log-miss.md` · `flow-master.md` (command, help, deps, REQUEST-RESOLUTION, a "A MISS IS A RECORD, NOT A SENTENCE" core principle) · `metrics-report.md` + its template (new §5) · `opencode.jsonc` · `tf-codex-bind.py` (regenerated: 9 agents, 19 skills) · Claude mirror re-synced byte-identical · `WORKFLOW.html` §17 + TOC + quick-refs + FAQ · `README.md` §7.13 + §17/§17a · `docs/TechieFlow-Telemetry-Guide.md` §3.5. `.gitattributes` needed **no** change — all three scripts already manage `docs/metrics/*.jsonl text eol=lf merge=union`, which covers the new file by glob.

**Verified end to end** on two throwaway repos: id issue → collapse check (open, then silent on re-verify, then empty after a `Verified` close) → `shared:3` vs `sole` derived correctly from `reqs_touched` → a fabricated `origin_model` overwritten by the looked-up one → an unlinked origin downgraded to `inferred` with the model nulled → the report refusing every figure under n=3 and printing both exclusions with the numbers they bound.

**Design records (not online — in `docs/`):** `docs/Miss-Telemetry-TechieFlow.md` (this work), `docs/Miss-Telemetry-TfLens.md` (the sixth report page, the two-kinds parser change, the apportioned-cost result type), `docs/Miss-Telemetry-AI-First-Playbook.md` (the team edition — OpenCode-only so real dollars throughout, `misses.ndjson` durable and committed rather than inside the rotatable `events.ndjson`, `found_phase_gate` per SCHEMA §11, `actor` aggregate-only, and its **`why_missed`** field flagged as a candidate to port back here). **Neither TfLens nor the Playbook is implemented** — this session covered the framework only; TfLens ignores stream files it does not know, so there is no coordination window.

### 2026-08-27 — python3 made a hard prerequisite, and the OpenCode/Codex claims VERIFIED against the real binaries (one broken flag found)

Owner hit *"Python not present"* running a scaffold on the Mac, and asked — fairly — whether the OpenCode and Codex work had actually been checked against those products rather than against this repo's own prose. **It had not been.** Everything about OpenCode merge order and dead `{file:}` refs came from DECISIONS.md 2026-08-19/20, repeated on trust. Both tools are installed locally (opencode **1.18.19**, codex-cli **0.149.1**), so it was verifiable, and now is.

**(a) python3 is a HARD prerequisite in all three scripts.** It was only ever a soft `|| echo "⚠ … (python3 required)"` after `tf-codex-bind.py`, so a Mac scaffold without python3 **completed looking successful** while leaving the repo with no Codex bindings and — worse — no working guard hooks, which fail OPEN by design. Now `tf_ensure_python3()` in `scaffold-greenfield.sh`, `scaffold-brownfield.sh` and `update-framework.sh` detects it and installs via the platform's own package manager: `brew` on macOS (with `xcode-select --install` / python.org guidance when Homebrew is absent), and `apt-get`/`dnf`/`yum`/`pacman`/`zypper`/`apk` on Linux. Prints the exact command before running it; `TF_NO_INSTALL=1` reports only; a still-missing python3 **aborts** rather than continuing into a silently unguarded repo. Branch selection tested for macOS-with-brew, macOS-without-brew, Linux-with-apt, and the refusal path.

**Deployed to the two broken repos the same day.** `update-framework.sh` run on **AstroLyfe** and **TrBlazeUI**: both root `opencode.jsonc` files audited `refresh` (no project-only content), replaced from the template, old files kept as `opencode.jsonc.bak`; `.claude/settings.json` refreshed to the current guard set on both. **Verified fixed with the real binary** — `opencode debug config` now loads cleanly in both (exit 0), returning all six agents (`flow-master`/`flow-analyst`/`flow-architect`/`flow-verifier`/`trblazeui`/`techierag`) and 61–62 commands, with the `git commit*`/`push*`/`add*`/`rm*` denies armed. Before the run both failed outright on `bad file reference`.

*Second follow-up — a regression I introduced and caught here:* the new diagnostics used `printf | grep | while`, and this script runs under `set -euo pipefail`, where a grep that matches nothing fails the pipeline and **aborts the whole script**. So on any repo with no dead refs (i.e. every healthy one) `update-framework.sh` exited 1 right after the opencode step, silently skipping everything below it: the Codex adapter, `.agents/skills/`, WORKFLOW.html, the `.gitignore` block, metrics and `.gitattributes`. It looked like a clean run because the visible output ends with an ordinary line. Fixed by collecting the diagnostic lines into variables with `|| true` and looping over a here-string — no grep in a bare pipeline. Guarded both ways: healthy repo → rc=0, reaches "Framework updated", Codex step runs; repo with dead refs → rc=0 and the RESOLVED line. **Lesson for this file: `grep` in a pipeline is unsafe under `pipefail`; assign with `|| true` first.**

*Follow-up fix from watching that run:* the dead-`{file:}` warnings printed **after** the refresh line, because the audit inspects the file as it was before the step — so a successful repair read on screen as "still broken". They are now reported as `✔ N dead {file:} ref(s) RESOLVED by the refresh` when the file was just replaced, and only as live `⚠` warnings when it was preserved; the bare-`bash:allow` warning is likewise suppressed once the file is gone. Verified both ways (a repo now current → silent; a staged copy of AstroLyfe's old file → the RESOLVED line).

*Renderer bugfix caught by a post-deploy smoke test:* `tf-render-html.sh --out=DIR` failed with a bare `ENOENT` when the target directory did not already exist — it wrote the file without creating the path. `--out` now `makedirs(exist_ok=True)`; the default sibling-of-source path is unchanged. Verified both (nested new dir, and default). Redeployed to all 19.

**Rolled out to every managed repo (19 total).** After AstroLyfe and TrBlazeUI, the remaining **16** were dry-run then updated: all rc=0, all reached "Framework updated". Seven carried a stale root `opencode.jsonc` (AppStudio, TrSetup, TrStudio, XVault, SpinSins, TechieBlog, TrTools) — every one audited `refresh` (no project-only content), replaced from the template, old file kept as `opencode.jsonc.bak`; all seven now byte-identical to the template. The other nine (AI-First-Playbook-Source-PRIVATE, AppManager, CmpTools, DataExtractor, DataStudio, Lekhak, Sevak, TrGoalMan, Xpenser) have no root file and were unaffected by that step. Verified afterwards: all 19 carry `tf-render-html.sh`, the `tf-goal.sh` `approval_policy` fix, `tf-opencode-audit.py` and 18 Codex skills; a sweep of every root and framework-owned `opencode.jsonc` across all 19 found **zero** dead `{file:}` refs; and a live `opencode debug config` in TechieBlog loads clean (9 agents, 61 commands, git/gh denies armed). One pre-existing advisory left alone: the Playbook source's own `AI-First-Development-*.html` still reference the legacy BMAD layout — owner-authored content the script deliberately does not edit.

**(b) OpenCode — all three load-bearing claims CONFIRMED on 1.18.19**, via `opencode debug config` / `debug agent`:
- **`.opencode/opencode.jsonc` wins over the root file on conflicting keys.** Conflicting `model`, `permission.bash["git *"]` and `agent.shared.description` all resolved to the framework copy. It is a *deep* merge — `agent.shared.model`, set only in the root file, survived. `instructions` arrays concatenate (root first); `agent` maps merge. So the 2026-08-20 §5 claim holds, and yesterday's root-`opencode.jsonc` refresh rests on verified ground.
- **A dead `{file:}` ref hard-fails the ENTIRE config load.** `Error: Configuration is invalid … bad file reference … does not exist`, exit 1 — the valid agents and commands in the same file are lost with it. Reproduced in the two real repos: **AstroLyfe and TrBlazeUI genuinely fail to load their OpenCode config today.** That was asserted yesterday; it is now demonstrated.
- **A bare agent-level `"bash": "allow"` really does void the root denies.** `debug agent` prints the resolved rule list in order: the `loose` agent gets root's `* allow / git * deny / gh * deny` followed by its own `* allow` **appended last**, so last-match-wins makes `git commit` allowed. The `strict` agent, which repeats the full deny map, ends on `git * deny`. The template's "every agent repeats the full deny map" rule is necessary, and `tf-opencode-audit.py` targets exactly the right shape.

**(c) Codex — mostly confirmed, and ONE REAL DEFECT found.**
- **Project-level `.codex/` is genuinely supported.** The binary documents *"Project `.codex/config.toml`: settings for a trusted repository, including sandbox, MCP, hooks, model, and reasoning defaults"*, carries a `ConfigLayerSource::Project` variant, and — decisively — `~/.codex/config.toml` holds a `[hooks.state]` registry keyed by project path: `"/mnt/c/3AIGenCode/TrBlazeUI/.codex/hooks.json:pre_tool_use:0:0"` with a `trusted_hash`. So Codex discovers the framework's per-repo hooks file, hashes each hook **per event**, and gates it on trust. The `hooks` feature flag is enabled by default. PascalCase event names in `hooks.json` are normalised to snake_case — all four of TrBlazeUI's (`PreToolUse`/`SessionStart`/`SessionEnd`/`UserPromptSubmit`) registered cleanly. The framework's "trust this repository and review `/hooks`" instruction is exactly right: an untrusted hook silently does not run (confirmed — a probe hooks.json in an untrusted scratch project never fired).
- **DEFECT: `--ask-for-approval` is not a `codex exec` flag.** `tf-goal.sh` invoked `codex exec --json --sandbox workspace-write --ask-for-approval never` at line 177 (the goal loop) and line 314 (the limit probe). On 0.149.1 that is `error: unexpected argument '--ask-for-approval' found`, **exit 2** — so the entire Codex YOLO/goal path has never run, and the probe would have reported Codex unavailable. Corroborated by `.tfcore/.session/goal.log`, whose 2026-08-24 Codex run logged its start line and nothing after it. The real flag set is `--sandbox`, `--json`, `--skip-git-repo-check`, `--approve-for-me`, `--dangerously-bypass-approvals-and-sandbox`. **Fixed to `-c approval_policy="never"`**, verified end-to-end: a real `codex exec` run returned `thread.started` → `turn.completed`, exit 0. Corrected in `tf-goal.sh` (both call sites + the "verified flags" header comment), README §12a and WORKFLOW.html. The header comment claimed these were *"verified flags — docs/Capability-Matrix.md"*; they were not.
- **OPEN, needs a live check: is `Stop` a valid Codex hook event?** The framework's generated `hooks.json` declares five events including `Stop`, but the canonical serialized event-name list in the 0.149.1 binary is exactly ten and **`stop` is not among them**: `pre-tool-use, permission-request, post-tool-use, pre-compact, post-compact, session-start, session-end, user-prompt-submit, subagent-start, subagent-stop`. `StopCommandOutputWire` and *"Stop hook exited with code 2…"* do exist in the binary, but that module also carries heavy Claude-Code migration strings, so they may belong to the importer rather than to Codex's own event set. TrBlazeUI's trust registry predates the Stop hook and cannot settle it. **Not changed on a guess.** Repro for whoever settles it: run Codex once in an updated repo, approve `/hooks`, then check whether `[hooks.state]` gains a `…hooks.json:stop:0:0` entry alongside the other four. If it does not, the `guard-status-html.sh` Stop analogue is inert under Codex and needs the OpenCode-style fallback. Codex's own `permission-request`, `post-tool-use`, `pre-compact`/`post-compact` and `subagent-start`/`subagent-stop` events are unused by the framework and may be worth adopting.

### 2026-08-27 — TfLens framework feedback closed out (TF-001/002/003) + root `opencode.jsonc` finally refreshes

Owner forwarded `docs/TfLens-TechieFlow-Feedback.md` (three entries, reproducible, with suggested fixes) and separately asked why `update-framework.sh` never updates the root `opencode.jsonc`. Four defects, all real, all fixed here.

**(0) Root `opencode.jsonc` was preserved unconditionally — and the thing it was preserving does not exist.** The 2026-08-20 two-file split demoted the root file to "project-only keys (extra agents, MCP, LSP), preserved, never refreshed". Surveyed all 10 apps that carry one: **not one defines a project agent, a project command, or an `mcp`/`lsp` block.** Nine hold a **138–154 line** copy of a template that is now **925 lines**; all nine are missing the `trblazeui`/`techierag` agent blocks entirely; and **AstroLyfe + TrBlazeUI still wire `build-ui-phase` / `build-rag-phase` / `build-functional-phase`** — commands dissolved 2026-06-26, whose `{file:}` refs point at deleted task files. A dead `{file:}` ref **hard-fails OpenCode's entire config load**, so those two apps have been silently loading *no* framework agents or commands, and the §6 staleness check never caught it because it only ever looked for the six trimmed *stock agent* names. The preserve rule protected nothing and rotted everything. Now: the root file **refreshes like `.claude/settings.json`** — replaced (old → `opencode.jsonc.bak`) when the audit finds nothing project-owned, preserved with the project-only keys *named* when it does, and every dead `{file:}` ref reported whatever the verdict. New `.tfcore/utils/tf-opencode-audit.py` does the analysis **on the parsed config**. Verified across all 10 apps + three fixtures (project agent / `mcp` block / genuine bare `bash:allow`).

**Also killed a false positive I had relayed to the owner as real.** The old bare-`"bash": "allow"` warning grepped the **raw** file — and the template's own comment says *``never collapse it back to `"bash": "allow"` ``*, so the check matched its own documentation and fired on every correctly-configured app, never on a defect. **Third** false-positive check in this script traced to reading the wrong artifact (cf. 2026-08-24's two). The audit now inspects `agent.<name>.permission.bash` as a parsed value.

**(1) TF-001 — `tf-metrics.sh` never de-duplicated `sessions.jsonl` (High).** `dedupe_commits()` existed; there was no session equivalent, so records were read straight through. Sessions duplicate for a reason unrelated to union merge: the OpenCode plugin appends a **cumulative** snapshot at every root-session idle (SCHEMA.md §4), so one session legitimately produces several records and only the largest is complete. `dedupe_commits`'s closing paragraph — *"Only commits need this… runs/gates/sessions … a union merge cannot manufacture a second copy"* — was **correct about union merge and wrong as a conclusion**: it reasoned about one duplication source and concluded there were none. Result: session counts and every token total derived from them were **overstated**, silently, growing with how much OpenCode work a repo had recorded, and feeding the headline `tokens_per_verified_req`. Added `dedupe_sessions()` (highest `output_tokens` per `session_id`, ties on latest `ts`, **per repo** like commits), wired at the `analyse()` call site, surfaced as `session_duplicates_collapsed` in `--json` and in the printed report. Scope-corrected the `dedupe_commits` docstring **and** SCHEMA.md §5. Verified on this repo's own stream (the exact duplicate TfLens cited — 10 records → 9 sessions) plus 5 unit assertions covering max-wins, the ts tie-break, singleton passthrough, null key and null `output_tokens`.

**(2) TF-002 — `tf-perf.sh` measured the login redirect and reported it as page load (Medium).** No cookie/auth option existed, so on a gated app every route answered `302 → /login` and it reported **`p95 = 4.1 ms`**: the speed of being turned away at the door. The redirects were in `non_200`, but nothing marked the latency meaningless. Added repeatable **`--header 'K: V'`** and **`--cookie 'k=v'`** pass-through, first-class **`redirects` / `redirect_rate`** per level, and — the important half — an **all-3xx run is now a refusal**: `status:"redirected"`, **exit 4**, no latency figure emitted, with a hint naming the flags. A mixed run still measures and flags. `verify-phase` §4c documents the flags, the exit code and `PERF-UNMEASURED (auth wall)`. Verified against a purpose-built gated server: no auth → exit 4; `--cookie` → `status:ok`, 24 samples, 0 redirects; `--header` equivalent; mixed paths → measured with `redirect_rate: 0.5`.

**(3) TF-003 — `*generate-html` had no renderer; the model hand-authored every page from a 494-line spec (High).** Confirmed: `.tfcore/utils/` held ten scripts and none produced HTML, and both render tasks explicitly forbade the cheap path (*"Use the Write tool — never bash heredocs"*). **On the regression question the owner raised: it was a gap, not a regression** — no task referenced a missing script and no dangling call site existed, so nothing was removed; the tasks were written around hand-authoring from the start. Cost was ~300 KB of HTML ≈ **75–80k output tokens per phase**, mandatory on every phase via `_status-update-gate.md` item 8 + the `guard-status-html.sh` Stop hook, non-reproducible run-to-run, and silently truncatable at 130 KB. Shipped **`.tfcore/utils/tf-render-html.sh`** (+ `tf-render-html.py`), dependency-free (Python 3 stdlib — the reference machine has no pandoc/node/python-markdown). **The spec stays the source of truth in the strongest sense: the script extracts §2 CSS, §3's theme script and §7 JS out of `html-render-shell.md` at render time**, so a rendered page cannot drift from the shell. Implements §1 slugs with dedupe, §4 heading anchors *and* hand-written `<a id="…"></a>` anchors preserved verbatim, §5 mermaid toolbar, §5.5 label self-check, §6 escaped code, §6b agent-note strip, §7 JS, §8 inline + sidebar TOC (sidebar only above 6 H2s), and **refuses `*-Checklist.md` with exit 2 — that ban is now mechanical**. Both render tasks and the status gate now invoke it. Validated on four real TfLens documents (BRD 134 KB / 18 H2 / 10 diagrams, DevGuide 146 KB, UsageGuide, PROJECT-STATUS): every H2/H3/H4 carries an id, **zero dead TOC anchors**, 10 `.diagram` wrappers for 10 `pre.mermaid`, no hardcoded copy buttons, checklist refused. The §5.5 checker went through two false-positive rounds before it was trustworthy — it flagged parens *inside* correctly-quoted labels, then sequence-diagram message text (legal free-form per rule 8) — and now scans flowcharts only, after stripping quoted spans; positive control still catches `A[Order Service (v2)]` and a reserved `end` node id.

**Not changed, deliberately:** `_metrics-emit-gate.md` constraint 1 still calls `tf-metrics.sh` flatly "owner-run" even though the script's own `has_commit_hook` docstring says *"never a git call, so `--report` stays agent-safe"* and only `--backfill-*` touches git. Narrowing a safety rule is the owner's call, not an agent's — flagged, not edited.

### 2026-08-27 — The framework tree is invisible to agent file-search, and nothing ever said so (owner-reported, via TfLens)

Owner reported that the TfLens team hit this: a `*verify all` run declared `tf-metrics.sh` **"not present anywhere in this tree"**, and a later run found it at `.tfcore/telemetry/tf-metrics.sh` — where it sits in every installation. The framework, not the app, was at fault.

**Root cause — two independent filters stack, and both are ours.** `.tfcore/` (and `.claude/`, `.codex/`, `.opencode/`, `.agents/skills/`) is a **hidden dot-directory**, so ripgrep — which backs the agents' Grep tool — skips it by default; it is *also* in the managed `.gitignore` block the scaffolders write into every app (2026-07-05), so ripgrep's gitignore handling skips it again. Reproduced in TfLens: `rg --files -g '*tf-metrics*'` → nothing; **`rg --hidden` → still nothing** (`--hidden` alone is not enough in an app repo); `rg -uu` → found. `git ls-files | grep -c tfcore` → **0**, so `git grep`/`git ls-files` are blind too. In *this* repo `--hidden` alone suffices, because the framework source is tracked here — which is exactly why the gap was never noticed from the framework side.

**The framework was steering agents into it.** `app-claude-md-tmpl.md`'s tool-preference table told agents to use **Glob** to find files by pattern and **Grep** to search contents — the two tools that silently cannot see `.tfcore/`. A grep of `.claude/`, `.tfcore/` and `docs/` confirmed **no guidance anywhere** about hidden/ignored framework paths. Undocumented, and actively mis-steered.

**Why it is not a cosmetic search failure.** The false negative propagated into the ledger and became fact: TfLens's `docs/TfLens-BRD.md` §"F-PARITY" row asserted *"no parity run has ever passed and `tf-metrics.sh` is absent from this tree"*, and `REQ-FN-063` was stamped **⚠ NOT VERIFIABLE IN THIS ENVIRONMENT**, which by its own BRD §13 zero-tolerance rule made every figure in the app NOT QUOTABLE. An acceptance gate was closed by a search-tool default. Same family as 2026-08-25's third artifact-litter recurrence, where an ignore pattern hid the evidence from `git status`.

**Fix — canonical rule in the surface that actually propagates.** `AGENTS.md` and `CLAUDE.md` are written **once at day-1 and never refreshed by `update-framework.sh`**, so a templates-only fix would have reached new apps and none of the 14 existing ones. The rule therefore lives under `.tfcore/`, which *is* force-synced every run:
- **`.tfcore/tasks/_status-update-gate.md`** — new section §"The framework tree is INVISIBLE to search" (canonical): both filters named, `rg -uu` given, existence confirmed by **reading the literal path** and never by searching a filename, a hard bar on writing "not present in this tree" into a verdict/Remarks cell/BRD row/blocker without having tried the path, and the real remedy when a file *is* absent (fresh clone → `update-framework.sh <repo>` once per machine; never reimplement). Reaches 12 tasks/agents as a shared include.
- **`.tfcore/tasks/verify-phase.md`** — restated at the verdict list (the task that produced the bad verdict): *a missing file is never a verdict until you have read its literal path*, with the propagation consequence spelled out.
- **`.tfcore/tasks/_metrics-emit-gate.md`** — pointer on constraint 1, beside the existing `tf-metrics.sh` mention.
- **Templates** (new apps only, by design): `app-agents-md-tmpl.md` gains **Hard rule 3** (window-binding moves to 4); `app-claude-md-tmpl.md` gains a table row plus a paragraph correcting the Glob/Grep steer that caused it.
- All three `.tfcore/tasks/` edits mirrored byte-identically to `.claude/commands/TechieFlow/tasks/`; full-tree `diff -rq` clean for both `tasks/` and `agents/`.
- Documented in `WORKFLOW.html` §15 (new first FAQ entry) + README §15 mirror; WORKFLOW "Last revised" → 2026-08-27.
- **Dead anchor fixed while in there** (owner asked): `href="#verify"` was used by the 2026-08-10 artifact FAQ entry but **no `id="verify"` existed anywhere in the file** — the link had never resolved. Added the id to `<h4>Verify loop (until green)</h4>` in §7 (the canonical verify step; §7.9–7.12 already carry h3 ids, so this matches the file's convention) rather than repointing the link at a section, and re-aimed the new FAQ entry's verify link there too. A whole-file `href`-vs-`id` sweep now reports **zero** dead anchors.

**Deliberately NOT done:** the `.gitignore` block was not touched — it is load-bearing, and un-ignoring the framework in app repos would re-open what 2026-07-05 closed. **No hook**, either: unlike the git ban or the status shape, "an agent concluded a file is absent" has no tool-call signature to intercept, and ripgrep's hidden-file skip is not controllable per-repo (an `.ignore` whitelist defeats the gitignore filter but not the hidden one). This is a prose rule by necessity — worth remembering against the 2026-08-25 finding that prose-only rules are the ones that fail.

**TfLens was left untouched at the owner's instruction** (agents are working in it). Its two false statements — `docs/TfLens-BRD.md` F-PARITY row and `docs/TfLens-Checklist.md` REQ-FN-063 — are still on record there and are the owner's to clear. Note for whoever does: `tf-metrics.sh --report`/`--rollup` contains **no git call** (the script says so itself at the `has_commit_hook` docstring — *"never a git call, so `--report` stays agent-safe"*); only `--backfill-*` uses git. So BRD §13's parity procedure looks runnable in-session. **Left as an open question rather than a change** — `_metrics-emit-gate.md` constraint 1 still describes the script as flatly "owner-run", and narrowing that is the owner's policy call, not an agent's.

**Owner action:** `update-framework.sh <repo>` per app **per machine** to deploy the task rules. Existing apps' `AGENTS.md`/`CLAUDE.md` will not gain the hard rule (never refreshed) — the `.tfcore/` task rules cover them; regenerate those two files at day-1 or by hand if you want the rule in the always-loaded layer too.

### 2026-08-26 — Expired run material is swept automatically (`sweep-artifacts.sh` SessionStart hook)
Follow-up to 2026-08-25: `guard-artifacts.sh` fixed *where* run material lands but nothing ever removed it — Playwright wipes only its own `outputDir`, so TechieBlog had 1.1 GB under `tests/.artifacts/` (a 686 MB `harness/cluster-e`, a 259 MB `verify-host.log`, a dozen per-cluster dirs) plus 101 MB of `.verify/*.log`, most of it two weeks stale. Swept manually, then baked in: **`.tfcore/hooks/sweep-artifacts.sh`** — a **SessionStart** hook with no veto — deletes files under `tests/.artifacts/` and `.verify/` older than the retention window (default 7 days; `artifactRetentionDays:` in `core-config.yaml` or `TF_ARTIFACT_RETENTION_DAYS`; 0 disables), prunes emptied dirs, and removes banned repo-root legacy dirs (`test-results*/`, `scripts-*/`, `playwright-report/`) at any age. Age-per-file rather than age-per-dir so a run in flight and a mixed-age `harness/` are safe; symlinks never followed; nothing outside the two roots considered; throttled to once/hour via `.tfcore/.session/sweep.stamp`; `TF_SWEEP_DRY_RUN=1` previews. Wired in the canonical settings (`update-framework.sh`, both scaffolds, this repo), Codex (`codex-adapter.py session-start`, skipped on the UserPromptSubmit re-fire) and OpenCode (plugin, first root `session.created`). Summary line is surfaced into the session. 13/13 matrix in a scratch project, incl. symlink-escape and no-`python3` fail-open.

### 2026-08-25 — TrBlazeUI 2.0.3 published; the framework's Codex compat wrapper would have blocked its native agent in every app (fixed via the package's ownership marker)

Owner asked whether the TrBlazeUI team's updates (`docs/TechieFlow-TrBlazeUI-Feedback.md`, `docs/TechieFlow-Library-Persona-Propagation.md`) needed framework changes. **Yes — one blocking one.**

**Publication verified from the real feed, not the library repo.** Per owner instruction the version was read from the NuGet source in the user-level `%APPDATA%\NuGet\NuGet.Config` (`TrBlazeUI` = `https://nuget.pkg.github.com/techierathore/index.json`) via `dotnet.exe package search --configfile`: **2.0.3 is published** there (nuget.org still has only 2.0.0; `TrBlazeUI/Directory.Build.props` says 2.1.0 and `2.1.0-ci.*` builds also exist on the feed — the repo version is NOT the published version, which is why the feed is the right oracle). Downloaded the nupkg and inspected it: `buildTransitive/TrBlazeUI.Components.targets` carries the Codex block with the ownership marker, `skills/codex-trblazeui.toml` ships, `skills/claude-code-trblazeui.md` has 0 BMAD hits. So TR-001, TR-002 and TR-003 are all closed in a published package.

**The blocking defect.** 2.0.3's target deploys `.codex/agents/trblazeui.toml` only when the file is **absent OR `.trblazeui/.codex-agent-package-owned` exists**; anything else is preserved as consumer-owned. `.tfcore/utils/tf-codex-bind.py` wrote its compat wrapper to exactly that path on every scaffold/update, with no marker. Confirmed on TechieBlog (wrapper present, no marker). Net effect: in every Codex-bound TechieFlow app the package would have looked at the wrapper, called it consumer-owned, and **never deployed the native agent** — TR-003's "confirm discoverable" step could never go green. The reverse also held: had the package landed first, the next `update-framework.sh` would have overwritten the package copy with the wrapper.

**Fix (`tf-codex-bind.py`, single source of truth — no mirrors).** `trblazeui`/`techierag` are now `COMPAT` workers: (1) the wrapper is written only when the target is absent or is still the framework's own wrapper (detected by `description = "TechieFlow <lib> role."`); a package-/consumer-owned file is never touched and the run prints `kept library-owned .codex/agents/<lib>.toml (compat wrapper retired)`; (2) when the wrapper IS written, the marker (`.trblazeui/.codex-agent-package-owned` = `TrBlazeUI.Components`, `.techierag/…` = `TechieRag`) is planted alongside so the package is allowed to replace it. The wrapper therefore self-retires per repo on the first 2.0.3 build. Verified on a staged fixture through all three states: absent → wrapper + both markers; own-but-stale wrapper → refreshed; packaged toml dropped in (from the real nupkg) → preserved, count drops 9→8 with the "kept" line. Regenerated this repo's own `.codex/agents/*.toml` — it had been stale since 2026-08-24 (still pointed at legacy `.claude/trblazeui.md`).

**Docs/comments brought in line:** README file tree (`.claude/commands/<lib>.md`, new `.codex/agents/` rows), stale header comments in `scaffold-greenfield.sh` and `update-framework.sh` that still named `.claude/<lib>.md` as the NuGet path, the propagation doc (new "Ownership-marker contract" section, publication status, per-app step, and the requirement that TechieRag use the same marker path so no per-library special case is needed), the feedback file (publication verified, framework side done).

**Owner action per app:** `update-framework.sh <repo>` (plants the marker) **then** `dotnet build` against 2.0.3, then confirm `.codex/agents/trblazeui.toml` reads `description = "Expert .NET and Blazor specialist …"`. Order matters — a build before the update leaves the marker-less wrapper in place and the package skips. **TechieRag:** TR-RAG-002/003 still open; when it ships, it must use `.techierag/.codex-agent-package-owned`.

### 2026-08-25 — Two prose-only rules made mechanical: root artifact dirs (`guard-artifacts.sh`) and stale `PROJECT-STATUS.html` (`guard-status-html.sh` Stop hook)
Framework ticket from TechieBlog (`docs/TechieFlow-Enforcement-Gap.md`). Every TechieFlow rule with a hook held; both rules without one failed repeatedly — the artifact-location rule for the **third** time (ten root `test-results-*` dirs, 14 MB, hidden from `git status` by the very ignore pattern the rule was written to prevent), the same-turn HTML re-render twice in a row (owner read a 4h40m-stale page). **(1)** `.tfcore/hooks/guard-artifacts.sh` — PreToolUse `Bash`: blocks `--output`/`--output-dir` at a root-level `test-results*`/`scripts-*` or `mkdir` of one; bare `-o` deliberately unmatched. **(2)** `.tfcore/hooks/guard-status-html.sh` — the framework's first **`Stop`** hook: refuses to end the turn while the `.html` is older than the `.md` or missing; honours `stop_hook_active`. **(3)** Wired everywhere the canonical settings live: `update-framework.sh` heredoc, both scaffolds, this repo's `.claude/settings.json`; Codex via `codex-adapter.py` (`pre-tool` runs the artifact guard, new `stop` mode) + `.codex/hooks.json` `Stop`; OpenCode via the plugin (bash guard in `tool.execute.before`; Stop analogue = one-shot `client.session.prompt` nudge on root `session.idle`, since OpenCode has no blocking Stop hook). **(4)** Both rules now announce their hook in the prose (`verify-phase.md` §1, `_status-update-gate.md` §8) so a block reads as the policy working, not an obstacle to reword around. Both hooks fail open without `python3`. Known gaps unchanged from the ticket §8: Playwright-*library* scripts and shell redirects/`cp`/`fs.writeFileSync` into a root dir are not matched; the Stop hook checks mtime, not content. Hooks load at session start — verify in a fresh session; sweep existing litter first.

### 2026-08-24 — The NuGet persona shim could overwrite a fresh persona with a stale one (latent; found auditing the library repos)

Owner asked whether the routed-wrapper defect (next entry) required changes in the **TrBlazeUI / TechieRag repos**. Answer: **no** — read `src/TrBlazeUI.Components/build/TrBlazeUI.Components.targets` directly and it deploys `skills/claude-code-trblazeui.md` → `.claude/commands/trblazeui.md` (line 46), plus `.opencode/command/` and `.trblazeui/TrBlazeUI-AI-Reference.md`. TR-001/TR-RAG-001 are genuinely resolved at source; the framework was the only broken side. (TechieRag's repo is Mac-only, not verifiable from WSL; its feedback file records the same resolution.)

But auditing that turned up a **second framework-side defect in the same family**, in `update-framework.sh` §5. The shim was written when the libraries deployed to `.claude/<lib>.md`, and it `cp`-ed that file over `.claude/commands/<lib>.md` **unconditionally**, its comment asserting the root file is "authoritative". Since 2026-07-04 the opposite is true: `.claude/commands/<lib>.md` is what `dotnet build` writes. So in any repo still holding a pre-2026-07-04 legacy file, every `update-framework.sh` run would silently overwrite the **fresh** persona with the **stale** one — the exact inverse of the wrapper bug, and worse, because it destroys a good file rather than failing loudly.

**Not currently firing:** surveyed all 16 WSL `.tfcore` repos — **zero** hold a legacy `.claude/<lib>.md`, so the block has been a dead no-op and no persona was ever clobbered. It was one restored old clone (or an un-rebuilt machine, or a Mac clone I could not survey) away from re-arming.

**Fix:** the shim is now **legacy rescue only** — it fills a gap (`.claude/commands/<lib>.md` absent, which is the case it was written for) and never overwrites. When both exist and differ it keeps the `commands/` copy and prints a note that the legacy file is dead weight; when they match it stays silent. Verified all three branches on staged fixtures: gap → shimmed; differing → `FRESH-NUGET` survived where the old code would have written `LEGACY-STALE`; identical → quiet. Real repos unchanged (0 shim lines, 0 warnings). The stale premise in the block comment is corrected in place rather than left to mislead the next reader.

**Still genuinely open on the library side, unrelated to any of this:** TR-002/TR-RAG-002 (source fixed 2026-07-04, **packages not yet republished**) and TR-003/TR-RAG-003 (neither package ships a native Codex persona `.codex/agents/<lib>.toml`; the framework wrapper is the bridge until they do). *Superseded 2026-08-25 for TrBlazeUI: 2.0.3 is published and closes TR-002 + TR-003 — see the 2026-08-25 ownership-marker entry. TechieRag's two remain open.*

### 2026-08-24 — `update-framework.sh` warned about `opencode.jsonc` on every correctly-configured app (stale check, owner-reported)

Owner reported `update-framework.sh` "giving error in updating opencode.jsonc". Reproduced immediately: two `⚠ opencode.jsonc is missing …` warnings (build-invocation ladder, operating contract) on **8 of 12** app repos. Nothing was failing — the run completed and wrote everything correctly. They were **false positives**, and their advice was harmful.

**Root cause — a check that outlived the 2026-08-20 two-file split.** Before that split the root `opencode.jsonc` WAS the framework's OpenCode config, so auditing it for the two mandatory instructions was right. The split moved framework config into the framework-owned `.opencode/opencode.jsonc` — refreshed every run, and it **wins over the root file on conflicting keys** (DECISIONS.md 2026-08-20 §5, runtime-verified 2026-08-19 §7f). The root file was demoted to project-only keys (extra agents, MCP, LSP) and is explicitly *preserved, never refreshed*. The two checks at §6 were never updated, so they kept auditing the root file for content that had moved.

**The signal was inverted.** Both checks are guarded by `[[ -f opencode.jsonc ]]`, so the four repos with **no** root file (Xpenser, AppManager, Lekhak, Sevak) were silent, while the eight with a properly maintained per-project root file all warned. Having the file right is what triggered it. Worse, the printed remediation — "copy the current template's instructions entry" into the root file — would duplicate framework config into the one file the script guarantees it will never refresh, so it would rot silently while the framework copy (which wins anyway) moved on.

**Fix:** the checks now audit the file that must actually carry the instructions — the deployed `.opencode/opencode.jsonc` on a real run, `$TEMPLATE/opencode.jsonc` under `--dry-run` (what would land). The root file is no longer inspected for framework content. Reworded to say plainly that a hit is a **framework** defect to report, never something to patch in the app. Verified both directions: 0 warnings across all 12 repos, and the detection still fires per-instruction against a deliberately regressed config (removed the ladder line → ladder warned, contract stayed silent). The third §6 check (root file wiring the trimmed `dev/pm/po/qa/sm/ux-expert` agents) is **kept** — a `{file:}` ref to a deleted agent hard-fails the whole config load, so that one is still a real hazard. Scaffolds never carried these checks. Cosmetic/diagnostic only; no deployment behavior changed.

### 2026-08-24 — Routed library-persona wrappers pointed at a path `dotnet build` stopped writing (framework defect, owner-reported)

Owner hit this in TechieBlog: **both** routed sub-agents dead-stopped before doing any work, reporting that TrBlazeUI "is not deployed" and that a full rung-#4 build did not create the file. Nothing was wrong with TechieBlog — the framework generated an unusable wrapper, and its remediation advice was a dead end that no amount of rebuilding could satisfy.

**Root cause — a 2026-07-04 fix the generators never learned about.** Back then the libraries deployed their Claude persona to `.claude/<lib>.md`, and the framework shim-copied it to `.claude/commands/<lib>.md` so the slash command would resolve (TR-001 / TR-RAG-001). Both libraries then took the *suggested fix* and changed their MSBuild targets to write `.claude/commands/<lib>.md` **directly**; the framework's shim became the documented harmless no-op. But the shim only ever runs one direction — `.claude/<lib>.md` → `.claude/commands/<lib>.md` — so in any app that added the package after that change, the legacy root path is **never created by anything**. The routed wrappers, written 2026-08-20, hardcoded exactly that legacy path with no fallback. `dotnet build` writes the persona to the one place the wrapper does not look, so "run dotnet build" could never resolve it.

**Scope — all three harnesses, both libraries** (the symmetry means TechieRag apps fail identically; the owner asked, and the answer is yes):
- `.tfcore/utils/tf-routing-bind.sh` `SUB_BODY` → the generated `.claude/agents/{trblazeui,techierag}.md`.
- `.tfcore/utils/tf-codex-bind.py` `WORKERS` → the generated `.codex/agents/{trblazeui,techierag}.toml` (same stale path, one weaker fallback).
- `opencode.jsonc` sub-agent prompts → correct primary path, but a stale single fallback.

**Fix:** every generator now emits an ordered resolution chain and uses the FIRST path that exists — `.claude/commands/<lib>.md` (current deploy target) → `.claude/<lib>.md` (legacy) → `.<lib>/<Lib>-AI-Reference.md` (the packaged reference doc, which the package has always deployed); Codex also consults `.opencode/command/<lib>.md`. Only when **none** resolves does the agent report non-deployment, and that advice is now truthful. Verified end-to-end by binding a throwaway repo with `enabled: true` staged as a modern app (persona in `.claude/commands/` only, no legacy root file) and reading back all three generated wrappers. No mirrors exist for `.tfcore/utils/`, so these are the single source of truth.

**Not a library defect** — TrBlazeUI and TechieRag did what the framework's own feedback asked; no new TR-*/TR-RAG-* issue was filed. Only bites apps with `routing.yaml enabled: true` (ships `false`). **Owner action:** `update-framework.sh <repo>` per routed app to regenerate the wrappers; the owner's in-session workaround (pointing the agents at the real path) is no longer needed.

### 2026-08-24 — Codex documentation and command references synchronized

- Follow-up correction: the first pass missed the two library-owner feedback files. `docs/TechieFlow-TrBlazeUI-Feedback.md` now carries open issue TR-003 and `docs/TechieFlow-TechieRag-Feedback.md` carries TR-RAG-003, requiring each NuGet source repo to package a native `.codex/agents/{library}.toml` persona. `docs/TechieFlow-Library-Persona-Propagation.md` now records the source paths, consumer destinations, ownership/preservation rule, and clean-consumer Codex validation. The framework-generated wrappers remain the compatibility bridge until both libraries are republished.
- Updated the two primary human workflow surfaces, `README.md` and `WORKFLOW.html`, so Codex is documented alongside Claude Code and OpenCode instead of appearing only in an adapter callout.
- Added the complete Codex workflow vocabulary (`$techieflow-day1-*`, BRD/design, build/verify, issue, guide, handoff/recovery, render, metrics, and YOLO skills) plus the unattended `tf-goal.sh --harness codex` command and its resumable `codex exec --json --sandbox workspace-write --ask-for-approval never` behavior.
- Documented scaffold/update ownership for `.codex/` and `.agents/skills/`, repository trust and `/hooks` inspection, model-tier bindings, and the Codex-specific safety boundary: all agent-issued `git`/`gh` commands stay forbidden in every mode, including YOLO reads. Claude Code and OpenCode behavior was not changed.
- Updated the HTML title/footer and kept the framework context log as the durable record of this session. This was a documentation synchronization pass; it did not alter runtime behavior.

### 2026-08-24 — Codex third-harness adapter implemented

Implemented the plan in `CodexChanges.md` additively. New native surfaces:
`.codex/config.toml`, `.codex/agents/*.toml`, `.codex/hooks.json`,
`.codex/rules/techieflow.rules`, and 18 thin workflows under `.agents/skills/`.
`tf-codex-bind.py` generates agents and skills from canonical `.tfcore` content;
`codex-adapter.py` translates native hook payloads into the existing git/status/
verify guards and writes session pointers; `tf-codex-telemetry.py` consumes
authoritative `codex exec --json` usage. Harness detection, routing, model/effort
binding, telemetry, YOLO goal supervision, scaffolders, and updater now accept
Codex. Project config is preserved on update; framework hooks/rules and generated
agents/skills refresh. Claude Code and OpenCode files and invocation paths were
not replaced. Codex requires repository and hook trust, literal legacy commands
map to skills, main-thread phase routing remains advisory, and full local device
verification is not promised from Codex cloud.

### 2026-08-21 — Advisory escalation policy added to model routing (ported from the AI-First Playbook)

`.tfcore/routing.yaml` gained an `escalation:` block (shipped `fix-issues: after_attempts: 2, tier: frontier`): "if the same REQs have been through `fix-issues` twice without reaching `Verified`, launch the third run on frontier". **Advisory only, applied at launch** — neither harness can switch a running turn's model, and the framework does not pretend to (DECISIONS.md 2026-08-21, mirroring the Playbook's rejection of a runtime auto-escalator). Mechanics: `tf-routing.sh status` prints the policy beside each phase's base tier; new `tf-routing.sh set-escalation <phase> <attempts> <tier>` edits the block (no re-bind — escalation generates no binding); `tf-emit.sh` now stamps `attempt` on `runs.jsonl` records that touch REQs (`1 +` prior non-backfilled runs of the same `cmd` with overlapping `reqs_touched`; batch-aware; backfilled/REQ-less records untouched — SCHEMA.md §2.5, distinct from the per-REQ gate `attempt` of §3.1) and exposes `tf-emit.sh --next-run-attempt <cmd> <REQ>...` as the launch-time read. Docs: Routing Guide §6.4 (md + html), README/WORKFLOW.html §17b paragraphs. Additive; routing stays `enabled: false`; `tf-harness.sh`/`tf-routing-bind.sh` parsers are section-gated and ignore the block (verified); `update-framework.sh` preserves deployed `routing.yaml`, so existing apps get the block only via `set-escalation`. No task template changed. Not yet propagated to app repos (`utils/` rides the next `update-framework.sh` run).

### 2026-08-20 — OpenCode guard bridge implemented; agent-permission hole fixed; WSL OpenCode verified live

The 2026-08-19 design's UNVERIFIED items were probed against the **deployed** WSL OpenCode 1.18.18 binary (the source checkout is reference-only) — all confirmed (DECISIONS.md 2026-08-19 §7) — and the *breaks*-class fixes were then implemented and live-tested. (1) **NEW: `.opencode/plugin/techieflow.js`** (tracked; auto-loaded by OpenCode with no npm install): translates `bash`/`edit`/`write` tool calls into the Claude-shaped stdin JSON the unchanged `.tfcore/hooks/` guards parse — guard exit 2 → `throw` blocks the call with the guard's own message (verified live: a git command got `block-git.sh`'s text; a `Verified`-cell write without a same-day ledger got `guard-verify.sh`'s text; fires inside `task` subagent sessions too); injects `TF_HARNESS`/`TF_SESSION_ID`/`TF_PROJECT_DIR` via `shell.env`; emits `sessions.jsonl` snapshots with **real `cost_usd`** + child-session rollup on root `session.idle` and writes the pointer `.tfcore/.session/opencode.json` (new gitignore line). Closes Coupling-Points H-1/H-2. (2) **P-0 fixed in `opencode.jsonc`**: OpenCode permission evaluation is last-match-wins and agent rules append after root rules, so the six agents' bare `"bash": "allow"` had **voided the git/gh denies** — every agent now repeats the full ordered deny map (verified: rule order via `opencode agent list`; `git status` under `--agent flow-master` rejected). The config's "compound forms are NOT matched" comment was wrong (tree-sitter decomposition proven) and was replaced with the two real footguns. (3) `tf-emit.sh` harness detection now honors `TF_HARNESS` first — the plugin must pass `CLAUDE_PROJECT_DIR` for the guards, which had mislabeled OpenCode emits as `claude-code`; additive, Claude path unchanged. (4) SCHEMA.md §4's "sessions is Claude-Code-only" gap note replaced with the plugin mechanism + the cumulative-snapshot dedupe rule. Deployment findings folded into OpenCode-Deployment-Guide §0: the large-repo hang is a `/mnt/c` 9p pathology (5-min timeout in the snapshot subsystem) not a Bun-in-WSL crash (same repo on ext4: 30s); WSL `opencode` resolves to the crash-prone Windows npm shim unless `~/.opencode/bin` leads PATH; the `opencode-go` auth key is portable. **Not yet done:** D-1 propagation to apps via `update-framework.sh`, framework-owned `.opencode/opencode.jsonc` split, S-1 vocabulary edits, routing.yaml. Git ban held throughout — the owner commits.

### 2026-08-20 (fourth pass) — every remaining design requirement closed; the harness-adapter design is fully implemented

(1) **Per-run telemetry (T-3):** new `.tfcore/hooks/session-pointer.sh` (SessionStart + UserPromptSubmit → `.tfcore/.session/claude-code.json`; wired in settings.json + all three scripts); `tf-emit.sh` gained `enrich_run()` — runs/gates records carrying `started`+`ended` are enriched with `tier/tier_model/model/models/routed/tokens_*/cost_usd/tokens_scope` from the harness's own store (Claude transcript window, scope `main`, cost always null; OpenCode `opencode.db` session-tree window, scope `tree`, real cost; fallback `none` — tokens never estimated). Live-tested on both real stores; SCHEMA §2.5 documents it. (2) **Routing bindings:** new `.tfcore/utils/tf-routing-bind.sh` — from `routing.yaml` `enabled: true` it generates Claude `/tf:<phase>` wrappers (`model:`/`effort:`) + `.claude/agents/tf-*.md`/`trblazeui`/`techierag`, and OpenCode `.opencode/opencode.json` (verified against the deployed binary: config files validate standalone, so command entries are complete; model-only agent entries deep-merge keeping prompt/permission). Manifest-driven: disabling removes exactly the 23 generated artifacts (round-tripped). `update-framework.sh` runs it per app. Flipping the experiment on is now a one-line edit in an app's routing.yaml + one `update-framework.sh` run. (3) **Context layer (C-1):** new `app-agents-md-tmpl.md` — day-1 writes the CONTENT into a committed `AGENTS.md`; `CLAUDE.md` became a thin gitignored `@AGENTS.md` wrapper with the Claude-only sections; both day-1 tasks updated. (4) **The rest:** evidence-discipline doctrine into `_smoke-test-policy.md` (C-2); `rm/rmdir/sudo: ask` in root + all six agent maps (P-1); `@`-sigil dual-form notes at five sites (I-1); ladder paths `$HOME`-ified (E-2); `*agent` transform left harness-specific by design (A-1); cosmetics N-4/5/7/9 fixed, N-1 kept-dual-form, N-3/N-8 acknowledged stale. Coupling-Points now shows every row FIXED/RESOLVED; Adapter-Design and Telemetry-Hooks carry final implementation-status headers. Propagated to all 18 repos, per-repo verified. A fifth-pass probe round then closed the UNVERIFIED list too: Claude `SubagentStop` carries `agent_transcript_path` at a deterministic `…/<session>/subagents/agent-*.jsonl` path → `tf-emit.sh` now globs it hook-free and Claude runs report `tokens_scope:"tree"` (live-tested); OpenCode routed-command model verified persisted on the session row; two `task` calls in one message verified CONCURRENT (35ms apart, ~6s overlap). The interactive-TUI sliver was then closed by the owner's own TUI test: the routed command's model STICKS for the session (the TUI does not re-impose its selection) — Adapter-Design §5.1/§5.4 corrected, rule surfaced in README/WORKFLOW §17b. Nothing in the design remains unverified. Also added: `.tfcore/utils/tf-routing.sh` (status|on|off|set-tier|set-model|bind — the one-command routing control) and README §17b + WORKFLOW.html §17b rewritten as task-oriented owner guides around it; routing enabled live on TechieBlog (pilot) with the first routed phase measured at $0.036 on the economy tier. On owner feedback ("docs still not clear; routing config too hard"), two dedicated owner guides were then authored: **`docs/TechieFlow-Routing-Guide.md`** (+`.html` per the shared render shell — complete phase AND persona/subagent tier tables, per-tier change recipes with examples for every tier, TUI visibility walkthrough incl. the verified default-agent-never-routed and model-sticks behaviors, under-the-hood file list, tuning loop, troubleshooting table) and **`docs/TechieFlow-Telemetry-Guide.md`** (+`.html` — the three questions, all four streams with annotated example records, how model/tokens/cost land on run records, reading `*metrics` output with real TechieBlog numbers, the trust rules, wiring table, FAQ). README §17/§17b slimmed to summaries linking the guides; WORKFLOW.html §17/§17b gained framework-repo guide pointers (propagated ×18); `tf-routing.sh status` now prints WHERE-YOU-SEE-IT guidance itself. Also observed: intermittent `opencode run` init hangs, recovered on retry — set `autoupdate: false` per the deployment guide. Git ban held — the owner commits.

### 2026-08-20 (third pass) — S-1 vocabulary fix, routing.yaml + tf-harness.sh shipped (OFF), docs aligned

(1) **S-1:** the three mandatory fan-out sites (`build-phase.md` §3 flow-master clusters, `verify-phase.md` §4 test-writer spawn, `fix-issues.md` §4 data/logic route) now name both harnesses' mechanisms — "`tf-builder`/`tf-test-writer` if the harness registers it, otherwise the harness's general subagent (Claude `Agent`/`general-purpose`, OpenCode `task`/`general`)" — and `SCHEMA.md`'s `runs.subagents` enum gained `general` + the `tf-*` names; mirrored byte-identical to `.claude/commands/TechieFlow/tasks/`. (2) **Routing, declared but OFF:** `.tfcore/routing.yaml` (three tiers with per-harness model ids, the full Adapter-Design §5.6 phase map, subagent tiers, effort map, `enabled: false`) + `.tfcore/utils/tf-harness.sh` (`detect|root|enabled|tier|model|effort|invoke|session` — all subcommands tested on both harness identities). `update-framework.sh` deploys `routing.yaml` once then PRESERVES it (per-project, like `core-config.yaml`); flipping `enabled: true` alone deliberately changes nothing — the harness bindings (Claude `/tf:<phase>` wrappers + `tf-*` agents, OpenCode `model` fields) are the enable-time step, not yet built. (3) **Docs aligned:** README §0 + WORKFLOW.html §0 gained "OpenCode in WSL — the primary path" with the Docker sections marked FALLBACK ONLY; `build-invocation-ladder.md` §E + `opencode-operating-contract.md` runtime classification updated; `TechieFlow-OpenCode-Gaps.md` supersession pointer; `Adapter-Design.md`/`Telemetry-Hooks.md` implementation-status headers; Coupling-Points S-1 → FIXED. All propagated to the 18 WSL repos (routing.yaml deployed `enabled: false` everywhere, tf-harness.sh answering, S-1 wording in canonical + mirror). One session slip caught and corrected: an earlier relative-path mirror copy had run in the wrong cwd (TechieBlog) — harmless there (same-content no-op), redone with absolute paths in the framework repo. Git ban held — the owner commits.

### 2026-08-20 (second pass) — D-1 propagation: the OpenCode bridge now deploys to every app

`update-framework.sh` §3c and both scaffolds' §4b deploy the bridge: `.opencode/plugin/*.js` (always refreshed) + a framework-owned `.opencode/opencode.jsonc` (transformed copy of the template's root config, always refreshed; OpenCode merges it AFTER the project's root `opencode.jsonc`, so the framework wins on conflicting keys — the root file stays preserved for project-specific agents/MCP/LSP, with a stale-`"bash": "allow"` warning). Trap caught by live verification, now baked into all three scripts: **`{file:...}` refs resolve relative to the config file's own directory** (and one bad ref hard-fails the whole config), so the deploy rewrites `./.tfcore/` → `../.tfcore/`. Ran on all **18** `.tfcore`-bearing repos under `/mnt/c/3AIGenCode` + `/mnt/c/1MyCode`; live-verified in TechieBlog: six framework agents load from the deployed config, flow-master's `git status` is blocked with `block-git.sh`'s own message via the deployed plugin, and a `sessions.jsonl` record landed with `app:"TechieBlog"`, `harness:"opencode"`, real `cost_usd`. Apps commit none of it (`.opencode/` sits in the managed `.gitignore` block). Also this pass: `~/.bashrc` gained `export PATH="$HOME/.opencode/bin:$PATH"` so WSL `opencode` resolves to the native Linux binary instead of the Windows npm shim. Coupling-Points D-1 marked FIXED. Remaining from the design: S-1 vocabulary edits, routing.yaml, Claude-side session pointer.

### 2026-08-19 — Harness-adapter design session (investigation + written design only; nothing implemented)

The owner asked where TechieFlow encodes Claude-Code-specific assumptions as framework logic, for the smallest abstraction that fixes them, for per-phase model routing, for the telemetry hook points, and whether OpenCode should move from Docker to WSL. Five new documents under `docs/` + a DECISIONS.md entry (2026-08-19): **`docs/Capability-Matrix.md`** (side-by-side matrix, every OpenCode row cited to the 1.18.18 source `file:line`, every Claude row to a docs URL, UNVERIFIED marked), **`docs/Coupling-Points.md`** (itemised, by severity; §0 is the six-step shortest path), **`docs/Adapter-Design.md`** (the "harness profile": guard bridge via a local `.opencode/plugin/techieflow.ts` that shells out to the *existing* guard scripts; framework-owned `.opencode/opencode.jsonc`; `.tfcore/routing.yaml` + `tf-harness.sh`; three vocabulary edits; per-phase routing via commands and subagents in both harnesses; starting tier map with the builders-on-standard challenge to the owner's hypothesis), **`docs/Telemetry-Hooks.md`** (per-run model/tier/tokens window on both harnesses; `sessions.jsonl` for OpenCode with real cost; additive schema fields), **`docs/OpenCode-Deployment-Guide.md`** (recommendation: run OpenCode in the same WSL distro as Claude Code; Docker demoted to fallback). Key factual corrections surfaced: OpenCode **does** load local plugin files without npm install (supersedes DECISIONS.md §3b's premise); OpenCode's bash permission parses compound commands with tree-sitter (the "compound forms not matched" comment in `opencode.jsonc` is probably wrong — UNVERIFIED at runtime); OpenCode ignores `CLAUDE.md` whenever an `AGENTS.md` exists up the tree; Claude skill/command `model:` is turn-scoped. **No framework file other than this one and DECISIONS.md was modified.** The git ban stopped the session from committing — the owner commits.

### 2026-08-15 — OpenCode Docker verification prerequisites fixed

Containerized OpenCode on Windows is a Linux container, not WSL: it cannot execute `cmd.exe` through Linux interop. The owner keeps `Dockerfile` and `opencode-docker.cmd` in `%USERPROFILE%\\.opencode-docker-config`, builds the reusable `my-opencode-dotnet` image once with `docker build --pull --no-cache -t my-opencode-dotnet .`, and invokes the launcher from `PATH` in each app folder. The image uses the Debian .NET 10 SDK and deliberately installs no MAUI workloads. Standard .NET apps build/test in the container; Windows MAUI Blazor Desktop builds go through the SSH-backed `/usr/local/bin/winrun` wrapper to the Windows host, while mobile/iOS/Mac Catalyst work runs natively on a Mac. `WORKFLOW.html` and `README.md` document the Windows OpenSSH setup, Docker environment variables (`TF_WINDOWS_SSH_HOST`, `TF_WINDOWS_SSH_USER`, and `TF_WINDOWS_APP_PATH`), and the limitation that an unavailable bridge makes only the Windows head `STATIC-ONLY`.

The OpenSSH setup was clarified after `Add-WindowsCapability` appeared to hang at `Operation [Running]`: that command is a Windows servicing/Windows Update operation, not Docker/OpenCode, and the original pasted block obscured that the following commands had not started. `WORKFLOW.html` and `README.md` now run the capability check/install separately, skip an already-installed capability, make the firewall/key setup retry-safe, and document Ctrl+C plus DISM/Optional Features diagnostics after roughly 10 minutes. The bridge is explicitly optional for projects that do not require a Windows-host build.

The key setup was then tested interactively. A missing `authorized_keys` file caused a harmless `Select-String` path warning, but exposed that matching the literal filename `opencode-docker` is incorrect because OpenSSH public-key lines contain the key and comment, not the source filename. Both guides now compare the complete public-key line and handle a missing file without emitting an error.

For copy/paste reliability, the normal-user key setup in both guides is now one PowerShell line: it creates the directory and `authorized_keys` explicitly, generates only when the private key is absent, and appends only when the complete public key is absent.

The SSH verification then exposed the Windows OpenSSH Administrator distinction: administrator accounts use `%ProgramData%\\ssh\\administrators_authorized_keys`, not the profile `authorized_keys`. Both guides now explain that a password prompt is not expected for the empty-passphrase bridge key, check the Administrators SID, and provide the secured administrator-key path when applicable.

The verification step now sets `BatchMode`, `PreferredAuthentications=publickey`, and `PasswordAuthentication=no`, so a setup error produces `Permission denied (publickey)` instead of confusing a new user with a Windows or VPS password prompt. `README.md` also separates elevated host setup from normal-user key setup rather than placing them in one copy-paste block.

The first Docker image build completed successfully but Docker linted `ENV TF_WINDOWS_SSH_KEY` as a possible secret. It is only a container path, not credential material; the declaration was removed from `docs/Dockerfile` because the launcher and expanded command already pass the path explicitly, eliminating the false-positive warning on the next rebuild.

The first containerized build-phase run exposed two stale/incorrect assumptions. The build ladder classified plain Linux Docker as generic native Linux instead of selecting its explicit §E bridge path, and it treated a mounted Windows NuGet.Config as usable even though Windows commonly stores its password with DPAPI encryption that Linux cannot decrypt. The ladder now detects Docker by `/usr/local/bin/winrun` plus `TF_WINDOWS_APP_PATH`, probes `winrun "dotnet --info"`, never checks for `maui-tizen`, and marks only the Windows head static-only when the bridge really fails. `opencode-docker.cmd` now mounts `%USERPROFILE%\\.opencode-docker\\nuget`; private-feed users must create that user-level config with a Linux-readable credential, never a repository PAT.

OpenCode-specific loading was also strengthened: `opencode.jsonc` now loads the build ladder as a global instruction, the build/day-1/verify tasks use `winrun` for Docker Windows heads instead of hardcoded `cmd.exe`, and the updater warns when an existing app's preserved `opencode.jsonc` lacks the ladder instruction. Existing apps must refresh that preserved config and restart OpenCode; otherwise the old agent session can continue selecting direct Linux builds.

OpenCode now also loads `.tfcore/templates/v4custom/opencode-operating-contract.md`. This OpenCode-only contract makes Docker bridge selection mandatory and defines `*build-phase` completion as build → self-smoke → inline verify → Checklist verdicts → PROJECT-STATUS/HTML → telemetry, rather than stopping at a green compiler. The updater warns when an existing preserved `opencode.jsonc` lacks this instruction. Claude Code does not load this file and its settings/permissions remain unchanged.

The next Docker run showed that the TechieRag/TrBlazeUI personas could still summarize “authenticated package access unavailable” without proving which NuGet failure occurred. The Docker launcher now exposes `NUGET_CONFIG_FILE=/root/.nuget/NuGet/NuGet.Config`; the OpenCode contract and both OpenCode-only library personas require `test -s`, `dotnet nuget list source --configfile`, and an explicit restore before reporting a block. They distinguish missing mount, missing source, and HTTP 401, and never fall back to Windows `C:\...` sources. Claude personas are untouched.

The first real Docker bridge probe then found the launcher defect: Docker Desktop's read-only `/root/.ssh` bind mount exposed the private key as mode `0777`, and OpenSSH rejected it; the same mount could not receive `known_hosts`. `docs/Dockerfile` now makes `winrun` copy the key to writable `/tmp/opencode-docker/opencode-docker`, chmods that copy to `0600`, and maintains host trust in a writable `/tmp` file. This is Docker-only launcher behavior; the Claude Code setup and its `.claude` files are not replaced.

NuGet authentication is now documented as user-level configuration, never a repository PAT: Windows `%AppData%\\NuGet\\NuGet.Config`, macOS/Linux `$HOME/.nuget/NuGet/NuGet.Config`, and the Docker read-only mount at `/root/.nuget/NuGet`. The TechieRag and TrBlazeUI personas no longer instruct agents to create credential-bearing project configs; they use project `nuget.config` only for non-secret source mapping.

Added `docs/TechieFlow-Library-Persona-Propagation.md`, which records the exact source files in the TrBlazeUI and TechieRag repositories, their `.targets` deployment mappings, and the consumer `.opencode/command/*.md` paths. This prevents framework snapshots from being mistaken for the NuGet package source of truth.

### 2026-08-13 — OpenCode-harness audit: 8 gaps fixed; the OpenCode mirror is gone for good; mirror parity is now Claude-only

A full audit of the OpenCode harness against the OpenCode 1.18.18 binary (evidence + fixes: `docs/TechieFlow-OpenCode-Gaps.md`) found and closed 8 gaps. The durable rule changes:

- **`.opencode/command/TechieFlow/` mirror deleted** — OpenCode registered those files as undescribed phantom *commands* (`TechieFlow/agents/flow-master`, `TechieFlow/tasks/*`), never as agents. OpenCode loads agents/tasks from `opencode.jsonc` `{file:./.tfcore/...}` references; the scaffold/update scripts no longer deploy the mirror, and §7 rule 1 now forbids re-creating it. **The byte-identity constraint between the two harness mirrors is ended** — Claude Code's `.claude/commands/TechieFlow/` mirror stays mandatory and byte-identical to `.tfcore/`.
- **`opencode.jsonc` gained a root `permission.bash` block denying git/gh** (OpenCode has no settings-level hooks; this is the mechanical "GIT IS MANUAL" gate there), replaced every deprecated `tools` block with `permission`, and registered `trblazeui`/`techierag` as `mode: all` agents with **inline prompts** (`{file:}` refs to NuGet-deployed personas hard-fail the whole config when absent). Their `.opencode/command/*.md` wrappers now carry only `description` + `subtask: true` + `agent:` — the only frontmatter keys OpenCode commands honor.
- **`instructions` now includes `AGENTS.md`** (tolerated when missing), and day-1 §7.2 emits a harness-neutral `AGENTS.md` at the app repo root — OpenCode auto-loads it, never `CLAUDE.md`.
- **Invocation references are dual-form** across `.tfcore/` (`/TechieFlow:agents:<x>` … `(OpenCode: /flow-<x>)`); the Claude-side mirror keeps the same dual-form text (harness-neutral prose).
- Verified post-fix: `opencode agent list` = 6 registered agents; `opencode debug config` = 29 commands, zero phantoms, git/gh denied. Claude side verified untouched: `.claude/settings.json` + hooks intact, agents mirror `diff`-clean.

### 2026-08-11 — the `commits.jsonl` design was answered instead of fixed; the hook now reconciles from `pre-commit`, and `.gitattributes` pins LF

**The owner pushed back on the 2026-08-10 answer, and was right to.** That entry established that an empty `commits.jsonl` is the seeded-then-waiting state and closed the item as *by design*. The owner's reply: he codes the same repos on more than one machine, so *"since we are not checking it into repository it would be a pain for me to collect data from them"* — and *"I think this feature increases the friction in data collection."*

The previous answer was correct about the mechanism and wrong about the question. `docs/metrics/` **is** tracked, so nothing is literally unversioned. But the *data* still failed to arrive, for three reasons that only bite on a multi-machine setup, and all three came from the hook appending exactly one record:

- **The newest record is always uncommitted.** `post-commit` fires after the commit is sealed, so its line lands in the *next* commit. Stop working on that machine and it is never pushed at all. The tree is also never clean — commit, and the file is dirty again immediately, forever.
- **A clone with no hook records nothing, silently.** `.git/hooks/` is not part of the repository, so every clone needs its own. The four Mac clones here (`AppStudio`, `TrSetup`, `TrStudio`, `TechieRag`) have `.tfcore/` from 2026-07-11 but no `docs/metrics/` and no `post-commit` — they have simply never been refreshed since telemetry landed, and nothing anywhere said so.
- **Commits made on the other machine were invisible here**, and vice versa. Two half-histories, neither complete, and merging the files by hand is how an append-only log loses records.

**The fix: the hook reconciles instead of appends.** On every commit it now writes a record for **every** commit reachable from HEAD that the stream does not already carry, skipping on `sha` — the same operation as `--backfill-commits`, which it delegates to (`--limit 500 --quiet`), falling back to the old pure-bash single-record path only when `python3` is absent. So after a pull, the next commit on *any* machine backfills everything the other one did. `git log` is itself an append-only log that push/pull already replicates everywhere; the stream was only ever a projection of it, and now it is maintained as one.

**The git ban is untouched.** This is still the owner's own `git commit`, still nothing added to the commit being made, still no agent path near it. `pre-commit` + `git add` — the "obvious" fix for the lag — stays rejected for the same reason as before. Reconciling from the log gets the completeness without putting version control inside an automated path.

**Two supporting changes so the gap cannot hide again:**

- `tf-metrics.sh --report` now prints **`⚠ no post-commit hook in THIS clone of: <app>`** — a filesystem read, never a git call, so it stays agent-safe. `metrics-report.md` tells the agent to surface it in §5 "What is missing" and explicitly **not** to act on it (installing is a refresh; backfilling is git). This is the one telemetry gap the owner could not see by reading the files.
- `tf-emit.sh` accepts a **JSONL batch** on stdin, not just one object, and resolves `project_type`/`harness`/`app` once per batch. Before this, a first reconcile of 400 commits meant 400 bash+python startups inside `git commit`. Single-object and pretty-printed input behave exactly as before; a malformed line is dropped on its own and the rest of the batch still lands.

**The second reported issue — GitHub on Windows warning "This file uses 'LF' line endings, but Git is configured to convert them to 'CRLF' the next time the file is checked out."** Same root cause as the 2026-07-11 incident, one layer out: the **framework repo** pinned `* text=auto eol=lf` back then, but **app repos never got the same treatment**. TrSetup / TrStudio / TechieRag carry the stock Visual Studio `.gitattributes` (`* text=auto`, nothing else); AppStudio has none at all. `text=auto` normalizes the committed *blob* to LF and still lets `core.autocrlf=true` smudge the *working tree* to CRLF on checkout — file on disk and file Git will next write disagree, and Git says so. On a `.jsonl` appended to by machine that is worse than cosmetic: mixed line endings inside a log.

The 8d `.gitattributes` block in all three scripts (added 2026-08-10 with `merge=union` only) now manages four lines:

```
* text=auto eol=lf
*.bat text eol=crlf
*.cmd text eol=crlf
docs/metrics/*.jsonl text eol=lf merge=union
```

`eol=lf` pins the **working tree**, which is the half `text=auto` never covered. `*.bat`/`*.cmd` are pinned back to CRLF — the Windows command processor requires it. The telemetry line is named explicitly as well as covered by `*` because it has to carry `merge=union` anyway. The legacy un-pinned `docs/metrics/*.jsonl merge=union` line (and its managed comment) is **removed** rather than left to shadow the new one — the only time this block edits an existing line. Verified idempotent across four repo states (no file / VS default / legacy line / legacy line with CRLF): re-runs report "already present", dry-run previews correctly, and the owner's own entries are never touched. `tf-emit.sh` also opens the stream with `newline="\n"` so a native-Windows python3 cannot write CRLF into it.

**Owner actions** (agents cannot do either — git is manual): run `update-framework.sh <repo>` per app repo **on each machine you use it from**, then once per repo `git add --renormalize . && git commit` so the committed blobs match the new rules. The scripts print that reminder when they add the block. The framework repo's own `.gitattributes` gained the `*.bat`/`*.cmd`/telemetry lines and needs the same one-time renormalize.

**Propagated to the four Mac repos, and the backfill proves the point.** The owner confirmed the Mac clones carry current code and asked for both the refresh and the backfill. All four (`AppStudio`, `TrSetup`, `TrStudio`, `TechieRag`) took the refresh cleanly: agent-artifact ignore entries added, `docs/metrics/` seeded, the reconciling `post-commit` installed, the four-line `.gitattributes` block written. `--backfill-commits` then recovered **64 commit records** that had been sitting in the logs all along — AppStudio 32 (back to 2025-12-15), TechieRag 22 (back to 2025-12-30), TrSetup 6, TrStudio 4 — LF-only, chronological, and enough for a commit-cadence figure in every repo. Nothing was "collected"; the history was already replicated, and the stream simply had never been asked for it. `--report` now shows no missing-hook warning on any of the four. (`subject_prefix` is `null` throughout — the owner does not use conventional-commit prefixes, which costs nothing but that one field.)

Run under an explicit owner instruction, and worth naming: `--backfill-commits` is documented **owner-run-only** because it invokes git. It is read-only (`git log`, `rev-parse` — no commit, tag, or mutation) and the owner asked for it directly. The doctrine stands; this was a delegation, not a loophole.

**Still not propagated:** the nine active app repos on the Windows machine (`/mnt/c/1MyCode`, `/mnt/c/3AIGenCode`) are unreachable from the Mac. The owner will refresh them there.

**The owner's follow-up found the hole in the reconcile design — duplicate shas — and it is now closed.** Asked *"won't I miss the last line added to `commits.jsonl` when I pull on a different machine?"*, the answer is no: that line describes a commit, the commit itself is pushed, and the other machine's reconcile rebuilds the record from `git log`. But the question exposed the symmetric case nobody had traced. Machine A records commit `N` locally; if that line is not yet pushed when B pulls `N` and reconciles, **both machines now hold a genuine record of `N`**, and `merge=union` — whose entire purpose is to never drop a line — keeps both. `--backfill-commits` skipped duplicate shas, but **`analyse()` did not**, so `commits`, `active_days` and `commits_per_active_day` would have drifted upward exactly in the multi-machine case the day's work was meant to serve. Union merge and de-duplication are two halves of one trade and only one half had been built. New `dedupe_commits()` collapses commit records on `sha` **per repo** (two repos may legitimately share a short sha), the report prints how many it collapsed rather than silently correcting, and only `commits` needs it — `runs`/`gates`/`sessions` describe events that happen on one machine and cannot be independently reconstructed, so union merge has no way to manufacture a second copy. Verified against a stream with a planted duplicate (4 lines → 3 counted, note printed) and against all four live repos (no change). Documented in SCHEMA §5, README §17, and two new WORKFLOW §15 FAQ entries — including the practical wrinkle that `commits.jsonl` always carries one pending line, so git refuses to pull when that file also changed upstream: commit the one-line diff first.

**Then the owner named the thing the reconcile fix had not touched, and the hook moved to `pre-commit`.** *"The wrinkle you are saying is exactly the issue I am saying — when I commit, a new line is added to the file after commit."* Correct, and the advice that came with the earlier fix (*"commit the one-line diff first, then pull"*) was circular: committing to clear the pending line creates a commit whose record is then pending in turn. `post-commit` cannot put its line inside the commit it describes, so **the working tree was never clean, permanently, with no reachable state where it was** — and on a repo worked from two machines that also blocked `git pull` every time the file had changed upstream. The 2026-08-08 design called this "the one-commit lag" and filed it as a caveat. It was a defect wearing a caveat's label; the lag is one commit either way, but "your tree is never clean" is not a lag.

Offered three options (pre-commit + stage · untrack it as a derived cache · leave it), the owner chose **pre-commit + stage**, which keeps the stream in the repo — the thing he asked for at the start — while making the tree clean. New `.tfcore/telemetry/pre-commit` reconciles from the log exactly as before, then `git add`s the one file so records ship *inside* the commit. `post-commit` is deleted from the framework, and `install-metrics.sh` **removes** an installed one (identified by the template's marker, so a hook the owner wrote is never touched) — `rsync --delete` on `.tfcore/telemetry/` retires the deployed template copies on the next refresh.

**This reverses a documented decision, so the risk it was rejected over is bounded rather than waived.** DECISIONS.md §3 had explicitly rejected `pre-commit` + `git add` as "putting version control inside an automated path"; that entry is now revised in place with both halves of the reasoning rather than rewritten. The bounds: it stages **exactly one path** (`docs/metrics/commits.jsonl` — never a directory, never `-A`, so it cannot pull source into a commit); on a **partial commit** (`git commit -- <paths>`, detected via `GIT_INDEX_FILE`) it writes the record and does **not** stage, so it can never add a file to a commit deliberately scoped down; and **every path exits 0**, because a pre-commit hook exiting non-zero *aborts the commit* and telemetry has no veto. Everything it skips — merges (git runs `pre-merge-commit` instead), `--no-verify`, rebase, cherry-pick, partial commits — is picked up by the next ordinary commit's reconcile, so skipping is never losing. Constraint 1 is untouched: *agents* still never run git, `block-git.sh` is unchanged, no agent path reaches the hook. What actually moved is the narrower habit that the framework's own git usage stays read-only — which was never a stated constraint, only an artifact of the post-commit design. `install-metrics.sh` also **never replaces a pre-commit hook it did not write** (unlike the old post-commit path, which backed up and replaced): losing someone's lint gate to a telemetry install is a worse outcome than not collecting commit records, so it warns and tells you how to chain instead.

Redeployed to all four Mac repos: `post-commit` removed, `pre-commit` installed, stale template swept, `--report` clean on every one.

**A classification limitation surfaced by the refresh, deliberately not "fixed".** `install-metrics.sh` auto-detected `TechieRag` as `project_type: app`, not `library` as SCHEMA §1 lists it. The detector is not wrong — the repo now hosts **two** products: the packable `src/TechieRag*` libraries *and* `apps/TechieDesk`, a head with screens, each with its own checklist (`docs/TechieRag-Checklist.md`, `docs/TechieDesk-Checklist.md`). `detect_type` takes `head -1` of the checklists, lands on TechieDesk, finds an executable head and says `app`. Since TechieDesk genuinely can fail the visual gate, `app` is the defensible value for the repo *as a whole* — `library` would claim a gate can never fire when it can. The real limitation is that `project_type` and `tf-emit.sh`'s `app` inference are both **per-repo, one value**, and this repo breaks the one-checklist-per-repo assumption: with two checklists present, `tf-emit.sh` falls back to the directory name, so every record here is stamped `app:"TechieRag"` even when the work was TechieDesk's. Left as-is and logged rather than patched — splitting the repo, or teaching telemetry about multi-product repos, is the owner's call, not a side effect of a line-endings fix.

**Also updated:** `SCHEMA.md` §5 (reconcile table, the `branch` field's honest limitation — a reconciled record carries the reconciling clone's branch, because `git log` does not retain per-commit branch membership, `merge=union` note) · the generated `docs/metrics/README.md` (new "Working on more than one machine" section, replacing the "it will sit empty indefinitely" paragraph) · `README.md` §17 (new "Working across several machines" subsection) · `WORKFLOW.html` (§15 two new FAQ entries — multi-machine collection, and the CRLF warning verbatim; §17 lag panel + a new multi-machine panel) · `DECISIONS.md` (the one-commit-lag decision revised in place, not overwritten) · `docs/TechieFlow-Telemetry-Runbook.md` · `metrics-report-template.md`.

### 2026-08-10 (3) — the artifact rule extended to the harness: `scripts-cluster-*` was the same defect one layer up

**The owner asked why `scripts/` and `scripts-cluster-*` folders were still appearing after the morning's artifact fix.** Investigation split the question in two, and only one half was a bug.

**`scripts/` (no hyphen) is legitimate and must never be touched.** Five repos have one: TrBlazeUI (`release-*.sh`, `run-demo.sh`), TechieExtensions (`create-release.ps1`, `setup-dev.ps1`), TechieCode (`test-cross-platform.ps1`, `test-performance.ps1`), TechieBlog (`Rename-Project.ps1` + its `.sh`, dated January 2026 — it predates the framework), and TrSetup (empty, a leftover from REQ-FN-034 deleting `publish.*`/`preflight-gate.*`). `document-project.md:166` even documents `scripts/` as a normal repo convention. Not litter.

**`scripts-cluster-*` was real, and it was the morning's own fix leaking.** TechieBlog alone had four — `scripts-cluster-b|f|g|i/`, dated 2026-08-09 — each holding a hand-written `smoke-cluster-*.mjs` (plus a `cleanup-cluster-i.mjs`) written by a §4/§4a role-cluster fan-out. The entry above diagnosed the original bug precisely — *"the doctrine was applied to the name the tool defaults to, not to the names the agents actually invented"* — and then fixed it by banning one more literal name family. So the ARTIFACT-LOCATION RULE pinned where the **tools write** and said nothing about where an agent puts the **scripts it authors**; the fan-out stopped creating `test-results-cluster-a/` and created `scripts-cluster-b/` instead. Same defect, different noun, four more untracked folders.

**Two findings that make this worse than a naming slip:**

- **The `outputDir` pin could never have reached these scripts.** They do `import { chromium } from 'playwright'` — the **library**, not the test runner. `playwright.config.ts` is never loaded, so `outputDir` does not apply and nothing wipes what they write. `scripts-cluster-b/smoke.mjs:10` hardcodes `const SHOTS = '/mnt/c/1MyCode/TechieBlog/test-results-cluster-b'` and `mkdirSync`s it — an absolute path to a banned directory, created by a script the ban's enforcement mechanism cannot see.
- **TechieBlog's `.gitignore` is the archaeology of chasing names**: a one-off `test-results-blogapp/` line from an earlier round, then the managed block with `tests/.artifacts/` + `test-results-*/` appended below it. Neither covered `scripts-*`.

**The fix — a rule about the class of thing, not the filenames.** `verify-phase.md` §1's rule now opens on *everything this run generates that is not a deliverable — what the tools output **and** what you write to drive them*, both under `tests/.artifacts/`, with an explicit instruction not to reason by the letter of the list (*any* new root-level `<name>-<slug>/` for a fan-out is the same defect). New bullets: **harness scripts go in `tests/.artifacts/harness/`**, named per cluster *inside* the folder; the project's own `scripts/` is off limits in **both** directions (never write a harness into it, never delete it); a promoted harness stops being scratch and becomes a real spec under `tests/verify/`; and a library-driving script must place its own captures under `tests/.artifacts/` and **never hardcode an absolute path**. §1's gitignore list and sweep, and §7 teardown, extended to `scripts-*/` — with the hyphen and root-anchor requirement stated at every site, plus a "if it's plainly not a run harness, report it and leave it" escape so the sweep can't eat something real. `_smoke-test-policy.md` + `build-phase.md` §6 bound identically.

**Ignore entry is `/scripts-*/`, deliberately** — leading slash so it matches only a root-level directory, hyphen required so the tracked `scripts/` can never match. Added to `GI2_LINES`/`GI2_PATS` in all three scripts (legacy-only, like the `test-results` pair: a compliant run writes none of them).

**Swept + propagated.** TechieBlog's four dirs deleted (backed up to the session scratchpad first); a full `maxdepth 2` scan of both repo roots confirmed no other app had any. 3 tasks mirrored ×2 (byte-identity verified), all three scripts `bash -n` clean, `update-framework.sh` re-run across all 9 `.tfcore` repos — each reported `added agent-artifact entries: /scripts-*/`, and TrBlazeUI/TechieBlog were spot-checked to confirm their real `scripts/` survived. WORKFLOW.html (§gitignore paragraph + a new one on the harness half + the FAQ entry, retitled to include the `scripts-cluster-b/` symptom) + README §mirrors updated.

### 2026-08-10 (2) — §4c, the performance gate: speed becomes gradeable, but only against a budget someone declared

**Where this came from.** While clearing the artifact litter (entry below), the throwaway dirs turned out to contain a hand-written load harness (`loadharness.py`, `ab.sh`) an agent had built from scratch to grade TechieBlog's REQ-NFR-001. That is the tell: §4a proves the data is present, §4b proves the screen is usable, and **nothing covered speed** — a page rendering every control perfectly in nine seconds passed every gate the framework had. An agent needing to invent tooling mid-verify is a missing framework feature, not a clever agent.

**The design constraint that shaped everything: this gate must not cry wolf.** A perf number on a dev box — WSL→Windows networking, sibling build agents, tiered JIT — is noisy. A gate that fails REQs on noise teaches the owner to ignore verdicts, and that costs far more than the defects it would have caught. Every decision below follows from that:

- **It never invents a budget.** §4c grades a REQ **only** if its acceptance criteria carry a machine-read line: `perf-budget: <p50|p95|max> <ttfb|load> <= <N>ms [@ concurrency <N>]`. No line → the gate does not run, `perf` is omitted from `gates_run`, nothing is reported. A REQ whose prose says "should be fast" is explicitly **not** gradeable — the run flags it in §8 as needing a number in the BRD, and moves on. Picking a sensible-sounding threshold would be the framework inventing a requirement.
- **Three bands, and the middle one is the point.** ≤ budget → `PERF-OK`. > budget but ≤ budget × 1.25 → `PERF-MARGINAL`, a dated remark that **never blocks `Verified`**, so drift is visible before it is a failure. > budget × 1.25 → `PERF-FAIL` → `Needs re-verify`.
- **Four refusals to grade**, each producing `PERF-UNMEASURED` with the reason: Debug build (no steady-state JIT — not evidence), `weak` sample (<20 behind the p95), any error or non-200 during the run (contaminated distribution, and the failure belongs to `acceptance`), visibly contended host.
- **MAUI native heads are not perf-gated at all.** The harness speaks HTTP; an Appium screen has no URL, and launch/frame timing is a different discipline. Same doctrine as `⚠ STATIC-ONLY` — unmeasured is unmeasured, never passed.

**New file `.tfcore/utils/tf-perf.sh`** — sibling of `tf-emit.sh`, python3-only, no new dependency. Samples TTFB (time to the first body byte) and full load over a declared path set at graded concurrency; discards warm-up requests; reports **p95, never the mean**; emits a `per_path` breakdown so the slow page is identifiable rather than just the slow screen; flags `weak` under 20 samples; stamps `build_config` and a machine fingerprint. **It produces numbers and never a verdict** — it does not read budgets, touch a checklist, or emit telemetry. A measurement tool that also judges is one you cannot audit when a number looks wrong. Exit codes: 0 measured · 2 unreachable · 3 bad args. Verified end-to-end against a synthetic server with injected 10/50/250 ms delays: measured p95s tracked the injections, `per_path` isolated `/slow`, and all three bands plus all four refusals fired correctly.

**Telemetry — the part that needed the most care.** `gate` gained `perf` (execution order now `build` → `acceptance` → `render` → `visual` → `perf` → `standards`; first-failing-gate semantics unchanged, so a REQ already `VISUAL-FAIL` still records `visual`). `failure_class` gained `slow-ttfb` / `slow-load` — **naming the metric only; the measured milliseconds are never emitted**, because a latency figure is a property of one machine on one day and these streams are pooled across hosts and months.

The real hazard is subtler and is now handled in code, not just prose: **a gate added mid-stream is structurally understated in the raw distribution.** Every record written before 2026-08-10 had zero chance of recording `perf`, and most records after it still will not run the gate. A `Counter` over `gate` therefore says almost nothing about how good the perf gate is. New **SCHEMA §3.5** states the correct denominator — `failures with gate="perf" ÷ records with "perf" in gates_run` — and `tf-metrics.sh` now carries a `LATE_GATES` table and prints coverage beside the count (`perf gate: ran on N records, caught M -> X%`, or `not yet run on this data (gate added 2026-08-10)`), with an explicit line that the share in the distribution above is **not** that rate. This is the same reasoning as the live/backfilled provenance rule arriving by a different route: a gate that ran on 6 of 400 records did not fail to catch things, it was not asked. Verified against synthetic data covering both branches and re-checked against TechieBlog's real 127-record stream (no regression; `MIN_N = 3` honored).

**A design bug the first real test caught — and the rule that replaced it.** The gate's original precondition list said *"`errors > 0` or any `non_200` → `PERF-UNMEASURED`"*, reasoning that failures contaminate a latency sample. Running it against TechieBlog's REQ-NFR-001 at `@ concurrency 100` produced **321 of 500 requests timing out** — and the gate would have shrugged and recorded *not measured*. That is exactly backwards: **requests timing out under load IS the perf result**, and a budget written `@ concurrency 100` is precisely a claim that this does not happen. The blanket rule made the gate useless at the concurrency levels it exists for.

Replaced with a rate-based branch, since only the rate separates the two situations: **`error_rate` ≥ 0.10 with timeout/connection error kinds → `PERF-FAIL`, `failure_class:"timeout"`** (the app did not serve the declared concurrency; report the completed/attempted split and label the p95 as survivors-only); **0 < `error_rate` < 0.10 → `PERF-UNMEASURED`** (a handful of failures means the sample is contaminated, not that the app is overloaded); **any `non_200` → `PERF-UNMEASURED` regardless of rate** (a 404 is a wrong path set, a 5xx belongs to the `acceptance` gate — neither is a latency verdict). A load-shed run can be both `weak` and a genuine `PERF-FAIL`; the load-shed verdict wins, because it does not depend on the latency distribution being trustworthy. `tf-perf.sh` now emits `attempted` and `error_rate` so the branch is mechanical rather than eyeballed, and `failure_class` gained **`timeout`** (SCHEMA §3.3 + the §6 status-mapping table, which now has separate PERF-FAIL rows for *slow* and *load shed*).

**Also edited:** `verify-phase.md` §0 (capture `perf-budget` into the working list) · §4c (new) · §5 · §6 verdict list + status-mapping table (`PERF-FAIL` / `PERF-MARGINAL` / `PERF-UNMEASURED` rows) · STRICT GATE clause (widened *only* for budget-carrying REQs) · §6a (`gates_run` honesty, verdict→record mapping, and the explicit rule that **`PERF-MARGINAL`/`PERF-UNMEASURED` are never emitted as failures** — a warning in a stream that counts catches would corrupt the one number the gate exists to produce) · §8 report line · Key principles · `_smoke-test-policy.md` (smoke may measure, may never write a perf verdict — ceiling stays `Implemented`) · `verifier.md` persona (two new core principles) · `split-brd.md` + checklist template (copy the budget line **verbatim**, never synthesize, never drop) · BRD template §11 (the grammar, as an agent-facing HTML comment, with "do not invent a budget" stated where it will be read).

Wired per the contract: 3 tasks + 1 agent mirrored ×2 (byte-identity verified), `.tfcore/utils/` already rsyncs to apps, no new command so `opencode.jsonc` is unchanged, WORKFLOW.html §7 + cheat-sheet + quick-ref + README mirrors, propagated to all 9 app repos.

### 2026-08-10 — Test artifacts confined to `tests/.artifacts/`; the empty `commits.jsonl` explained; the team edition linked

Three owner-reported items from one review pass. Two were questions with a documentation answer; one was a genuine defect.

**1. The artifact-litter defect — repo roots filling with screenshot dumps.**

The owner found directories named `test-results/`, `test-results-cluster-a/` … `-cluster-l/`, `test-results-tr054/`, `test-results-blogapp/` piling up at app repo roots, uncommitted and unignored. Two independent faults stacked:

- **The framework never pinned Playwright's `outputDir`.** `verify-phase.md` §1 said only *"ensure a minimal `playwright.config.ts` exists … If absent, create one with testDir/use/reporter"* — no `outputDir`, and no instruction to touch a config that already existed. Playwright's default is a repo-root `test-results/`, so that is what every project got, and a project whose config predated any change kept getting it forever.
- **Fan-outs invented suffixed siblings, which the ignore pattern could not match.** §4/§4a fan out by role-cluster; agents gave each cluster its own output directory via `--output test-results-cluster-x`. `test-results/` does **not** match `test-results-cluster-a/` — so every one of them landed untracked in the owner's commit view, and they accumulated one directory per run with nothing ever cleaning them up (Playwright only wipes the *configured* `outputDir`). TechieBlog reached fourteen.

The framework had the right doctrine already ("any machine-generated file a task introduces gets its `.gitignore` entry in the same step") and still lost, because the doctrine was applied to the *name the tool defaults to*, not to the *names the agents actually invented*. The fix is therefore a location rule, not another ignore pattern — patterns chase names, a pinned `outputDir` removes the choice:

- `verify-phase.md` **§1** — pins **`outputDir: './tests/.artifacts/test-results'`** in a full canonical config block, and now says *create if absent, **edit if present and unpinned*** (the old "if absent" wording is why existing projects never self-healed). Adds the hard **ARTIFACT-LOCATION RULE**: no repo-root `test-results/` or `test-results-<slug>/` sibling, ever; no `--output test-results-…`; isolation for a parallel run is permitted **only** as the subfolder `--output tests/.artifacts/<slug>`; and the statement of what these files are for — *failure evidence with a session lifetime*, cited by path in a §6 Remark during this run, never work product, never committed, not expected to survive the next run. Plus a one-time sweep: delete any repo-root `test-results*` directory found (root-level, name-matched only — `tests/`, `docs/screenshots/` and everything else are off limits).
- `verify-phase.md` **§3b / §5 / §6 / §7 / §8** — Appium captures land in the same place (the rule is driver-independent); §5 forbids a bare `--output`; the ledger + report evidence paths updated; **§7 teardown** must leave no artifact directory outside `tests/.artifacts/` (and must NOT delete `tests/.artifacts/` itself — the paths cited in §6 Remarks have to still resolve for the owner reading the report today).
- `_smoke-test-policy.md` + `build-phase.md` §6 — bound by the identical rule, so a self-smoke cannot reintroduce what the verifier stopped doing.
- All three scripts' ignore block (`GI2_LINES`/`GI2_PATS`) — gained `tests/.artifacts/` and the `test-results-*/` glob. **The bare `test-results/` and the glob are LEGACY-ONLY entries now**; a compliant run writes neither, and they exist purely to keep old trees quiet.

**What is deliberately NOT swept up:** `docs/screenshots/<APP>/` — the DevGuide's reviewed screen captures — are human work product, TRACKED on purpose, and untouched by any of this. The distinction that matters: a screenshot the owner reviewed and a doc references is a deliverable; a screenshot Playwright took because an assertion failed thirty seconds ago is a log line that happens to be a PNG.

**2. `commits.jsonl` empty in every repo — BY DESIGN, not a bug.**

Verified rather than assumed: `.git/hooks/post-commit` is installed in all nine app repos, and the stream demonstrably works — TechieBlog, the only repo actively committed to since telemetry landed, carries real records while every other repo sits at 0 bytes. That is the seeded-then-waiting state, not a failure. `install-metrics.sh` seeds all four streams so every repo has the same shape and no writer has to test for its file's existence; each then fills only when its own event occurs, and `commits.jsonl` is written **solely by the owner's own `git commit`** — no agent path can reach it (agents cannot run git at all). Empty files should be committed with everything else, so the first real record is a one-line diff rather than a file appearing from nowhere. Documented in three places a reader might land: the generated `docs/metrics/README.md` (inside `install-metrics.sh`), README §17, and a new WORKFLOW §15 FAQ entry.

**3. The team edition is now linked from here.**

The [AI-First Development Playbook](https://github.com/techierathore/AI-First-Playbook) already linked *back* to TechieFlow as the solo edition; the link was one-directional. Added README **§18 "Team edition — the AI-First Playbook"** — a solo-vs-team comparison table (unit of work: the run vs the feature; 5 compressed phases vs 10 steps/4 gates; Claude Code + `.tfcore/` vs OpenCode + BMAD personas; what each carries that the other doesn't) and a "which one do you want?" note — plus a header blockquote so the relationship is visible without scrolling. Mirrored to WORKFLOW.html §18 + its TOC. Also corrected the §3 repo-map line, which still claimed "16 sections" after §17 was added on 2026-08-08.

### 2026-08-08 — Development telemetry: the framework now keeps the evidence it was already producing

**The problem.** Every `*verify` run applied four separately-named gates (acceptance, §4a data-render, §4b visual-truth, standards-grep) to individually-identified requirements and wrote verdicts into the one checklist's Requirements Status table, plus `docs/.last-verify.json` as a run ledger. Then the next run overwrote the ledger, the table was mutated in place, and the history was gone. The framework could not answer three basic questions about any project built with it: **first-pass rate**, **gate catch distribution**, **escape rate**. Implemented from `docs/TechieFlow-Telemetry-Runbook.md`.

**The data model.** Four append-only, schema-versioned JSONL streams per app repo, under **`docs/metrics/` — TRACKED by version control on purpose**, since this is the one thing the framework cannot reconstruct after the fact (verified: no pattern in either managed `.gitignore` block catches it, and all three scripts now assert that at deploy time):

- **`gates.jsonl` — the primary stream.** One record per REQ verdict per verify run. Its `gate` field is **the FIRST gate that failed** (`build` → `acceptance` → `render` → `visual` → `standards`, in execution order — recording a later one would inflate the visual gate's apparent catch rate), `null` on a pass, or **`escaped`** when `*triage-issues` logs a defect a human found after every gate passed it. That one field produces both the gate catch distribution and the escape rate. `attempt` is *derived* by counting prior live records (`tf-emit.sh --next-attempt`), never guessed.
- **`runs.jsonl`** — one per framework command run, carrying `mode` (`build` vs FIX — the rework metric), `reqs_touched` (IDs only), `files_written`, `duration_s`.
- **`sessions.jsonl`** — written by a `SessionEnd` hook (event name verified against the installed Claude Code 2.1.226, not assumed). Token counts only: transcripts carry no per-message cost, so `cost_usd` is `null` and "cost per verified REQ" is reported **in tokens** — a rate-card multiplication would be an estimate presented as a measurement.
- **`commits.jsonl`** — written by the **owner's own `post-commit` hook**. Known limitation documented everywhere it matters: post-commit fires after the commit is sealed, so its record rides in the *next* commit — metrics lag reality by one commit. NOT worked around with pre-commit + staging, because that would put version control inside an automated path.

**Non-goal, stated so a future session doesn't add it:** cycle-time-per-feature. The owner hands a checklist to a phase and runs it; the unit of work is **the run**, not the ticket.

**The git ban is untouched.** `block-git.sh` is byte-identical. No agent path runs git. The only file in the framework containing version-control commands is `.tfcore/telemetry/tf-metrics.sh`, whose `--backfill-commits` / `--backfill-gates` modes are **owner-run only**; its read-only `--report` / `--rollup` modes run no git and are what the `*metrics` task invokes.

**New files:** `.tfcore/telemetry/SCHEMA.md` (canonical schema — every field, enum, and known limitation; read before emitting), `.tfcore/utils/tf-emit.sh` (**the single append primitive** — takes a stream name + JSON on stdin, validates, appends one line, injects `v`/`ts`/`project_type`/`app`, and **exits 0 unconditionally**: missing dir, malformed JSON, absent `python3`, full disk — the event is dropped and the caller continues, same fail-open posture as `guard-status.sh`/`guard-verify.sh`), `.tfcore/telemetry/install-metrics.sh` (per-repo setup, idempotent, auto-invoked by all three scripts), `.tfcore/telemetry/post-commit`, `.tfcore/telemetry/tf-metrics.sh`, `.tfcore/hooks/metrics-session.sh`, `.tfcore/tasks/_metrics-emit-gate.md` (**the doctrine doc — sibling of `_status-update-gate.md`**, carrying the nine constraints), `.tfcore/tasks/metrics-report.md`, `.tfcore/templates/v4custom/metrics-report-template.md`.

**Edited:** `verify-phase.md` **§6a** (the highest-value edit — per-REQ gate emission, first-failing-gate captured, `attempt` derived; §0 now stamps the run start) · `build-phase.md` §0 + **§7a** (run record with the `mode` build-vs-FIX distinction) · `fix-issues.md` §6a · `triage-issues.md` §0 + **§6a** (the `gate:"escaped"` records — this is what makes escape rate computable at all) · `_status-update-gate.md` **item 10** (the status gate is now also the telemetry trigger) · nine further tasks gained a run-emit step (`handoff-phase`, `split-brd`, `amend-docs`, `refresh-status`, `devguide`, `productguide`, `mockups`, both `day1-*`) — low value individually, but the run stream is only honest if it is complete · `flow-master.md` (**`*metrics`** command + help row + REQUEST-RESOLUTION + dependency).

**`project_type` and why gate metrics are segmented.** Auto-detected ONCE (packable `.csproj` → `library`; `.razor`/`.xaml` → `app`; no source → `docs`; this repo → `framework`), printed loudly, and written to `core-config.yaml → metrics.project_type` — a **preserved** file, so the classification survives every `update-framework.sh` and is never re-guessed. Correct a wrong guess with `.tfcore/telemetry/install-metrics.sh . --type <type>`. Gate-catch distribution is only meaningful where the same gates *could* fire: a `library` (TechieRag, TrBlazeUI) has no screens, so it can never fail the §4b visual gate, and pooling it understates that gate's real catch rate.

**The provenance rule — enforced in code, not just prose.** Two separations, the same rule twice: (a) records not written at the moment of the event carry `backfilled:true` and never pool with live records; (b) records never pool across `project_type`. **No report may print a combined first-pass rate, gate catch distribution, or escape rate crossing either boundary — not as a merged figure, not as a "total" row, not as an "overall" summary line.** `tf-metrics.sh` has no code path that produces one, which is deliberate: the task must make the tempting number impossible, not merely forbidden. Backfilled data appears in an adjacent labelled column with a footnote naming the inferred fields. Additionally, **any REQ carrying backfilled history is excluded from the live first-pass rate** — its live `attempt` numbering restarts at 1, so counting it would import the guess. Commit-derived metrics are exempt from both separations (the commit log is a real append-only log, and commit volume is comparable across types). The reasoning, recorded because it will be questioned: a merged first-pass rate cannot be defended when someone asks how attempts were counted, since backfilled attempts are inferred from a mutable status table that never recorded them — and one indefensible figure contaminates every other number on the page.

**Backfill — know what the data is worth.** `--backfill-gates` parses existing `<APP>-Checklist.md` Requirements Status tables + dated Remarks (`⚠ visual:` / `⚠ render gate:` / `⚠ UAT bug` markers classify the gate; only the *classification* is stored, never the remark text). Every record carries `backfilled:true` and `inferred:[...]`. **`attempt` is inferred on all of them, always** — the status table is a mutated-in-place snapshot, so a REQ that failed three times then passed is indistinguishable from one that passed first try. Backfilled gate data is **context and volume, never evidence for a published rate**. `--backfill-commits` is different and genuinely reliable.

**Privacy.** Records carry IDs, counts, durations, verdicts and file paths *at most*. `failure_class` is a closed enum (`blank-data`/`zero-rows`/`overlap`/`clipped`/`offscreen`/`exception`/`assert-fail`/`naming`/`build-error`/`other`) precisely so no free text can leak; commit **subjects** are discarded on the spot and only a `feat|fix|docs|chore|refactor|test|build` prefix is kept. This framework runs on employer projects and the files are append-only — a leaked field is not something you fix later.

**Wirings (contract §7):** all edited tasks + `flow-master.md` mirrored byte-identically to `.claude/commands/TechieFlow/` and `.opencode/command/TechieFlow/` (`diff -rq` clean); `opencode.jsonc` gained `techieflow:tasks:metrics-report` + the flow-master description mention; **the `SessionEnd` hook was added to the canonical `.claude/settings.json` block in ALL THREE scripts identically** (the updater force-refreshes `settings.json`, so a block present in only two of three silently reverts on the next update) plus the framework's own settings.json; `update-framework.sh` gained `telemetry` in `FRAMEWORK_SUBDIRS`; both scaffolds create + seed `docs/metrics/`; all three assert no ignore rule swallows it. `WORKFLOW.html` + `README.md` gained **§17 "Development telemetry"** (nav entry, §14 quick-ref row, "Last revised" bumped). Decisions + the Playbook schema mapping recorded in `DECISIONS.md`.

**No separate install step (design correction, same day).** The first cut put telemetry setup behind an owner-run `install-metrics.sh`, reasoning that installing a git hook must stay outside an automated path. Wrong twice: `update-framework.sh` is the owner's command, and **installing a hook is a file copy, not a git operation**. Setup now runs inline in `update-framework.sh` + both scaffolds, locating `.git/hooks` by reading the filesystem (`.git/` dir, or the `gitdir:` pointer when `.git` is a file) and **never invoking the git binary** — so it behaves identically whoever triggers the refresh, needs no permission prompt, and `block-git.sh` is untouched. `install-metrics.sh` survives only as the auto-invoked implementation (one source of truth instead of the same block pasted into three scripts) and as the manual reclassifier `--type`. Related: **the `post-commit` hook is optional** — it is the only component with a known defect (the one-commit lag) and the only one whose data reconstructs perfectly without it, since `tf-metrics.sh --backfill-commits` reads a log that is itself append-only. Delete the hook and backfill on demand and nothing is lost; that is precisely the opposite of `gates.jsonl`, whose history is unrecoverable, which is why the rest must be written as it happens.

**Owner actions:** re-run `update-framework.sh` per app repo — that is the whole of it (see `DECISIONS.md` §5 for the list and the `project_type` the detector will choose); check the classification line it prints and correct with `--type` if the heuristic guessed wrong; restart open sessions so the new `SessionEnd` hook loads.

### 2026-07-16 — `*triage-issues`: the ANALYZE-ONLY bug front door (UAT/production bugs get logged, not auto-fixed)
Owner report from **AstroLyfe UAT**: human-found bugs (the kind that surface at UAT or in production) had **no analyze-and-plan activity** in the flow. The owner told flow-master in free text to *analyze* the bugs, *re-verify the other features*, and *update the checklist* — deliberately NOT asking for fixes — and the agent matched the request to `*fix-issues` (the only bug command) and **started fixing code afterwards anyway**. Two defects: (a) no command whose *deliverable* is the bug analysis/paper-trail, (b) nothing told fix-issues that "analyze" is not "fix".
**New command:** **`*triage-issues {AppName} {evidence} [verify]`** on flow-master (task `.tfcore/tasks/triage-issues.md`). Evidence = a screenshots folder (same channel as fix-issues) **and/or a written bug list** (UAT reports often arrive as prose). It boots the app itself (smoke policy rules apply), reproduces each issue live with Playwright (data-render + visual-truth gates applied observationally), maps it to the owning screen + REQ via the DevGuide, echoes a triage table whose action column is a **checklist action, not a builder**, then writes the deliverables: regression inside an existing REQ's scope → Status `Needs re-verify` + dated `⚠ UAT bug {date}: {symptom} (evidence; kind)` remark (demotions never trip guard-verify); defect with no owning REQ → new `Planned` row (next free ID, right prefix) + acceptance detail bullet (genuinely new *scope* flags `*amend-docs` instead of inventing BRD text); could-not-reproduce → dated remark + owner question, never a guess. Optional literal **`verify`** EXECUTES verify-phase inline scoped to the demoted REQs' sibling REQs (same screens/services) — a real run that writes the `docs/.last-verify.json` ledger; without it no verdict promotions at all. Ends on the status gate (PROJECT-STATUS .md + .html; a Verification-log row ONLY if `verify` ran) with **next command = `*fix-issues {AppName} {Folder}`** naming the REQ IDs (fallback `*build-phase` when there's no evidence folder). **HARD SCOPE RULE stated twice in the task:** read-only on `src/`/`tests/`, no builder sub-agents (`/trblazeui`/`/techierag`/general-purpose fixers), no "obvious one-liner" fixes, never writes `Verified` itself; "if you changed a source file, this task FAILED".
**Root cause hardened where agents read:** `fix-issues.md` gained **§0 Scope guard** — if the owner's words ask only to analyze/triage/log/document (no fix asked), STOP and execute triage-issues.md instead; "'Analyze these bugs' is NEVER permission to edit code" — plus a matching Hard-rules bullet and an intro cross-pointer. `flow-master.md` gained the **ANALYZE IS NOT FIX** core principle (docs-only deliverable on analysis requests; fixing code nobody asked to touch is overreach), REQUEST-RESOLUTION examples ("analyze/log these bugs" → `*triage-issues`, "fix these bugs" → `*fix-issues`), the command + help-template entries, `whenToUse` mention, and `triage-issues.md` in dependencies.
**Wirings (contract §7):** task + fix-issues + flow-master mirrored byte-identically to `.claude/commands/TechieFlow/` and `.opencode/command/TechieFlow/` (`diff -rq` clean); `opencode.jsonc` gained `techieflow:tasks:triage-issues` + the flow-master agent description mention; WORKFLOW.html gained **§7.12** (after §7.11, anchor `#triage-issues`), the A/B "Process at a glance" flowcharts gained the `UAT/prod bugs — log, don't fix yet` edge (`G → T → X`), §5 handoff panel + §7.11 cross-pointers, agent-roster row, §14 quick-ref row, §15 FAQ entry, task-file list, "Last revised" bumped; README mirrored at every equivalent spot. Propagated to the WSL-side apps via `update-framework.sh`. Session memory: new `triage-issues-command.md` + index line; `fix-issues-frontdoor.md` cross-linked. **Not changed:** `_status-update-gate.md` ladder (bug rows logged by triage are ordinary build-tier rows; the triage task itself states that their front door is `*fix-issues`).

### 2026-07-11 — Windows-side pull of the Mac commits CRLF-smudged the scripts AND the guard hooks; `.gitattributes` now pins `eol=lf`
The owner pulled the Mac-portability commits onto the Windows/WSL clone and `update-framework.sh` died with `/usr/bin/env: 'bash\r': No such file or directory`. Root cause: the Windows-side checkout (`core.autocrlf=true`, `filemode=false`, `ignorecase=true` — a Git-for-Windows-style clone) smudged every file the pull rewrote to CRLF, because the repo's old `.gitattributes` said only `* text=auto` — that normalizes committed blobs but leaves working-tree endings to `core.autocrlf`. Blast radius: all 6 `*.sh` (3 scripts + the 3 `.tfcore/hooks` guards) plus 13 pulled text files (README, WORKFLOW.html, WorkFlow-Context, LICENSE, verify-phase/_smoke-test-policy ×3 mirror copies, build-invocation-ladder, both sample coding-standards docs). **The nasty part: a CRLF hook script fails bash parsing with exit 2, so the PreToolUse guards BLOCKED the agent's own Bash (block-git) and Write/Edit (guard-verify) — a total tool lockout.** The unblock had to come from the owner: one in-session `! find /mnt/c/3AIGenCode/TechieFlow -type f -name '*.sh' -exec sed -i 's/\r$//' {} +`. The agent then stripped CRLF from the remaining 13 files (harness-mirror byte-identity re-verified with `diff -rq` — clean), replaced `.gitattributes` with `* text=auto eol=lf` + `*.sh text eol=lf` + image `binary` entries (working tree LF-pinned on Windows/WSL/macOS — `core.autocrlf` can never smudge this repo again), and ran `update-framework.sh` on all 7 WSL-side apps (AppManager, AppStudio, TechieRag, TrBlazeUI, TrSetup, TrStudio, AstroLyfe@`/mnt/c/1MyCode`) — each app also picked up the missing agent-artifact `.gitignore` entries, and every app's deployed hooks verified LF + `bash -n` clean afterward. **Owner actions:** run `git add --renormalize . && git commit` once in this repo (agents never run git); restart any open Claude Code sessions on the refreshed apps. Standing lesson for agents: if a PreToolUse hook error mentions `\r` or `$'\r': command not found`, the line endings are the emergency — nothing else is diagnosable until the hooks parse again, and the fix must come from the owner via a `!` command because the broken hooks block Bash and Write/Edit alike.

### 2026-07-11 — Framework moved to macOS natively; scripts made portable (self-locating TEMPLATE, BSD-safe realpath/sed, exec bits)
The owner moved the framework repo (and the app portfolio) from Windows/WSL to a Mac, natively — new home `/Volumes/MacD/MyCode/TechieFlow`. Three portability defects fixed in the scripts, all verified by running the real flows on macOS:
- **Hardcoded WSL path:** all three scripts carried `TEMPLATE="/mnt/c/3AIGenCode/TechieFlow"` — dead on any other machine. Now self-locating: `TEMPLATE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"`, so the repo works from wherever it lives (WSL, macOS, Linux). Usage headers de-WSL-ed to `/path/to/TechieFlow/…`.
- **GNU-only `realpath -m`** (scaffold-greenfield): macOS's BSD realpath has no `-m`. The script now `mkdir -p`s the target first, then resolves with plain `realpath` (the template-refusal branch `rmdir`s the empty dir it may have just created).
- **BSD-hostile `sed -i -e`** (update-framework's legacy `.bmad-core` migration): BSD sed reads `-i -e` as backup-suffix "-e", littering `core-config.yaml-e`. Replaced with sed-to-temp + `mv`.
- **Exec bits:** the Windows→Mac copy dropped `+x` on `*.sh` and `.tfcore/hooks/*.sh` (scripts failed with `permission denied` when invoked directly). Restored via `chmod +x`; app-side hooks are invoked as `bash "$…"` per settings.json so they were never broken.
- **Verified on macOS** (stock zsh environment, Apple openrsync protocol 29): `scaffold-brownfield.sh` onto a fake app with existing `src/` → full deploy incl. settings.json + gitignore block; `update-framework.sh --dry-run` itemizes and the real run refreshes; `scaffold-greenfield.sh` onto a not-yet-existing path works (the old `realpath -m` failure mode). openrsync supports every flag the scripts use (`-a`, `-ain`, `--delete`, `--ignore-existing`).
- **Docs:** `WORKFLOW.html` §16 + `README.md` §16 gained a **"Moving an existing project (or this framework repo) from Windows/WSL to a Mac"** panel: Finder hides dot-folders by default (Cmd+Shift+. toggles; Terminal `ls -la` always sees them); an app moved via **git** lacks `.tfcore/`/`.claude/`/`.opencode/`/`CLAUDE.md`/`WORKFLOW.html` BY DESIGN (gitignored since 2026-07-05) — re-deploy with `update-framework.sh` when `.tfcore/` survives, `scaffold-brownfield.sh` when it doesn't (safe: `--ignore-existing`); per-project gitignored work product (`CLAUDE.md`, `core-config.yaml` customizations, `settings.local.json`) only survives a folder copy — else copy from the old machine or regenerate; exec-bit chmod; restart Claude Code after redeploy. "Last revised" bumped in both; the footer's hardcoded WSL memory path made machine-neutral.
- **NOT done / owner actions:** per-app `update-framework.sh` runs from the Mac (do as each app lands — this also force-syncs the updated WORKFLOW.html into them); the old WSL session memory (`~/.claude/projects/-mnt-c-3AIGenCode-TechieFlow/memory/`) did NOT travel — the Mac session memory starts fresh under `~/.claude/projects/-Volumes-MacD-MyCode-TechieFlow/memory/` (copy the old dir over from the Windows machine if its history is wanted). No task/agent/template/`opencode.jsonc` change → no mirror or propagation needed beyond the standard per-app refresh.
- **Follow-up (same day) — Mac commands placed ADJACENT to the WSL commands throughout the doc body**, not only in §16. Owner: the main sections still showed only WSL/Windows commands. `WORKFLOW.html` + `README.md` now show both, labeled, at every command site: header Root pill lists both repo paths; §0 (WSL bootstrap) gained an "on macOS: skip this whole section" panel (`npx playwright install chromium` is the only prerequisite); §0b gained a Mac-native panel (everything on the one machine, all endpoints `http://localhost:4723`, skip mirrored networking) + a second labeled macOS variant of the `runtimeVerification.appium` YAML example + the curl check; §3 scaffold/updater blocks show the WSL and macOS invocations side by side (+ zsh alias variant); §11 retitled "MAUI builds & runs — from WSL (bridged) or macOS (native)" with a native `dotnet build`/`-f net9.0-maccatalyst`/`net9.0-android` block adjacent to the `winrun` block, and the Appium head table gained a "macOS-native reaches it via" column (all localhost; MAUI Windows head = n/a on Mac); §12 settings panel + §14 quick-ref rows (update-framework, MAUI build+test) show both forms. Anchors (`#wsl-runtime` etc.) unchanged; tag balance verified. Owner refinement, same day: the two platforms' commands must NOT share one copyable code window (copying grabs both variants) — every mixed block split into **separate, self-contained per-platform blocks** (each includes its own `cd`; shared follow-on commands like the greenfield `dotnet new` sequence get their own "Then, on either machine" block), labeled by a small bold caption OUTSIDE the block. Applies to: §0b appium YAML (two blocks), §3 brownfield/greenfield/updater, §11 MAUI build blocks, §12 settings panel — in both WORKFLOW.html and README.md. Standing rule for future doc edits: **never mix two platforms' commands in one `<pre>`/fence; label outside, keep each block copy-paste-runnable for one machine.** Owner follow-up, same day: "skip §0 on a Mac" left no positive setup path — new **§0a "macOS bootstrap — DO ONCE, EVER"** added (WORKFLOW.html between §0 and §0b, nav "One-time" group entry `#mac-bootstrap`; mirrored in README): Xcode CLT + license (explicitly noted as the python3 provider the guard hooks depend on), Homebrew, `brew install dotnet-sdk node`, MAUI workload only-if-MAUI, then per-project Playwright (`npm i -D @playwright/test` + `npx playwright install chromium`; no apt libs on Mac, browser cache shared under `~/Library/Caches/ms-playwright`), a Verify panel (`dotnet --info` / `python3 --version` / `npx playwright --version`), and a pointer into §0b for the Appium leg (all-localhost on Mac). §0's macOS note now points to §0a instead of just "skip"; §16 macOS quick-start step 1 now points to §0a. Owner follow-up: presenting the per-project Playwright install as an owner step was wrong — **the framework already self-provisions Playwright** (`verify-phase.md §1` self-healing env: `npm init -y` → `npm i -D @playwright/test` → `npx playwright install chromium` → minimal `playwright.config.ts`; smoke runs ride the same step). §0a's Playwright block rewritten to "nothing for you to do — Node is the only machine prerequisite" in both WORKFLOW.html + README. Two task files made platform-aware and re-mirrored ×2 (`diff` clean): **`_smoke-test-policy.md`** §"banned excuse" intro now covers WSL §0 / macOS §0a and adds "**Never ask the owner to install Playwright** — verify-phase §1 self-provisions per project (browser binary is a shared per-machine cache)"; **`verify-phase.md §3`** boot rung no longer hardcodes `~/.dotnet/dotnet` — on macOS/native Linux plain `dotnet run` (ladder §A; `~/.dotnet/dotnet` doesn't exist there), WSL keeps rung #2, and the nohup example is labeled "WSL form — substitute plain `dotnet` on macOS/Linux". Propagation to apps rides the standard per-app `update-framework.sh` runs already queued. Owner follow-up: agent-provisioned npm/Playwright files were landing in `git status` for him to triage — fixed at both layers. **(a) Scripts:** all three now manage a SECOND append-only `.gitignore` block, `# TechieFlow agent artifacts — machine-generated test harness & logs, never commit`: `node_modules/`, `/package.json`, `/package-lock.json`, `test-results/`, `playwright-report/`, `.verify/`, `logs/`, `/docs/.last-verify.json`, `.DS_Store` (root-anchored file entries so a genuine nested frontend package.json isn't swept; `playwright.config.ts` deliberately stays TRACKED — committed suites depend on it; same CRLF-safe tolerant matching; tested live: pre-existing unslashed `node_modules` respected, second run idempotent). **(b) Tasks:** `verify-phase.md §1` gained "Gitignore what you provision — in this same step, never later" (checks/appends the same entries whenever it provisions, self-healing pre-block projects) plus the generalized rule "any machine-generated file a task introduces gets its .gitignore entry in the same step that creates it"; `_smoke-test-policy.md` states the same in its Playwright bullet. Both re-mirrored ×2 (`diff` clean); WORKFLOW.html §3 + README document the second block. **Owner one-time action per app:** ignore rules don't untrack — `git rm -r --cached node_modules package.json package-lock.json test-results playwright-report .verify logs` (whichever are tracked) once, then commits stay clean.
- **TrSetup refreshed on the Mac (same day):** first app updated from the Mac framework copy — `update-framework.sh /Volumes/MacD/MyCode/TrSetup` ran clean (tasks/agents/hooks/WORKFLOW.html/settings.json all current; harness mirrors diff-verified; `.gitignore` gained the 3 missing agent-artifact entries `playwright-report/`, `logs/`, `.DS_Store` — its own earlier entries incl. `/package.json`, `/playwright.config.ts` were respected). TrSetup lives at **`/Volumes/MacD/MyCode/TrSetup`** now. ⚠ Its root **`CLAUDE.md` is MISSING** (gitignored work product that didn't survive the move) — regenerate via `*refresh-status TrSetup` / day-1, or copy from the Windows machine. Owner must RESTART any open Claude Code session on TrSetup to load the refreshed defs. Remaining 6 apps (AppManager, AppStudio, AstroLyfe, TechieRag, TrBlazeUI, TrStudio) still pending their Mac refresh as each is moved.
- **macOS workload installs need sudo (same day):** owner hit `Inadequate permissions. Run the command with elevated privileges.` on `dotnet workload install maui` / .NET updates — on macOS the SDK lives in root-owned `/usr/local/share/dotnet`, so ALL `dotnet workload install/update` + SDK updates need `sudo` (WSL's `~/.dotnet` is user-owned; no sudo there). Fixed in: **`build-invocation-ladder.md §A`** (agent-facing — `sudo dotnet workload install maui`; the error is "a sudo signal, NEVER a project blocker"; when the user reports it, hand them the exact sudo form), **WORKFLOW.html** §0a step 4 (`sudo` + explanatory comment), §16 quick-start / ladder paragraph / comparison table, **README** same spots (native-Windows mention left sudo-less — not needed there). Template not mirrored (relative-path load); TrSetup re-refreshed same day so its ladder + WORKFLOW.html carry the fix. **Machine note (resolved):** on this Mac `python3` was the Xcode stub erroring until the Xcode license was accepted — owner accepted it same day (`python3 --version` → 3.9.6), so the python3-backed guard-status/guard-verify hooks are ACTIVE again (they fail open without python3).

### 2026-07-10 — `Verified` verdicts enforced MECHANICALLY (guard-verify hook), `<APP>.App` naming banned, cross-publish guides disambiguated
Three owner reports in one session. **(1) The TrSetup build agent skipped the verifier and wrote the verdicts itself** — its own admission: "Build-phase §6b says to execute verify-phase.md inline (scope all) as a distinct gate — I did my own smoke and wrote the verdicts myself." Same cure as git/status: a **mechanical guard**. New PreToolUse hook **`.tfcore/hooks/guard-verify.sh`** (matcher `Write|Edit|MultiEdit`, python3-backed, fails open) blocks any Write/Edit/MultiEdit to a `*-Checklist.md` that **introduces** a `Verified` status cell (regex on status-cell form; counts new-vs-old so demotions/moves/Remarks-prose like "VERIFIED 2026-07-07" never trip it) unless `docs/.last-verify.json` (next to the checklist) exists with `date` = today. That ledger is written ONLY by an executed **verify-phase §6** run (new step: `{"date","app","scope","booted","gates","evidence"}` written immediately before verdicts, after §3–§5 actually ran — writing it without running them is declared "falsifying the audit record"), or by **refresh-status §3** in `"mode":"reconcile"` (only for surfacing a row's PRE-EXISTING dated verdict that the Status column lagged). The block message is the on-the-spot recipe (verifier path / orchestrator-ceiling-is-`Implemented` path / reconcile path). 10-case test suite (introduce w/o ledger, fresh ledger, stale ledger, demotion, remark prose, full Write, other file, move, malformed JSON fail-open, bold+dated cell). Prose hardened where agents actually read: **build-phase §6b** ("Chaining = EXECUTING, not summarizing… the build orchestrator NEVER writes `Verified`… STOP — you are self-attesting"), **`_smoke-test-policy.md`** new §"Smoke is NOT verify — a self-smoke's ceiling is `Implemented`", **flow-master persona** new principle SMOKE IS NOT VERIFY, **refresh-status §3** reconcile-mode paragraph. Wired into `hooks.PreToolUse` in the framework `.claude/settings.json` + all THREE scripts' heredocs (`bash -n` + JSON validated). **The damage repaired:** TrSetup's checklist rows FN-031/FN-034/NFR-007/UI-006 — all "VERIFIED" 2026-07-09 by the orchestrator — demoted to `Needs re-verify` with a dated DEMOTED remark; PROJECT-STATUS (.md + .html) corrected (29 Verified · 8 UAT · 4 Needs re-verify · 1 Planned · 6 N/A of 48; phase back to Build; one Verification-log row).
**(2) `<APP>.App` naming banned.** Owner: "All the main apps should be named directly as the Product" — the TrSetup MAUI head was scaffolded `TrSetup.App`. Standard baked in: the **primary executable head project is named exactly `<APP>`** (`src/<APP>/<APP>.csproj`); a single-head product's one head IS `<APP>`; secondary heads take *descriptive* suffixes (`.Api`, `.Desktop`, `.Cli`); satellites unchanged (`.Core`, `<APP>UI`, `.Core.Tests`). Locations: coding-standards canonical block (**day1-brownfield §4** new "Project & solution naming" — greenfield inherits it), **build-phase §3** scaffold-time rule (existing `<APP>.App` → log a rename REQ, never propagate), WORKFLOW.html §10 + README standing-standard panels. TrSetup (the only `.App` in the portfolio) gained **REQ-FN-035** (`Planned`): rename dir/csproj/sln/namespaces/harness+docs paths; bundle id + AutomationIds unchanged; next command `*build-phase TrSetup`.
**(3) The MAUI-vs-plain-.NET cross-publish confusion.** Owner read TrStudio's BuildAndRun guide as claiming the MAUI app cross-publishes as an ARM Mac app; the TrSetup agent (correctly) explained Catalyst needs Xcode — but earlier "gave the excuse it can't build the Mac app", ignoring the guide's owner-run on-Mac path. Fixes: **TrStudio-BuildAndRun-Guide** (.md + .html) — intro gained "the one rule": `-r osx-arm64` cross-publishes plain .NET ONLY; §3 Option B gained "⚠ these two commands are the complete list — `TrStudio.Desktop` is deliberately NOT here… by design, not a missing feature"; the stale pointer to TrSetup's guide §1b (deleted 2026-07-09) removed. **Agent-facing HTML comments** ("a host limitation never cancels a cross-OS deliverable — prepare source + exact on-Mac commands; a locally-buildable substitute is a complement, never a silent replacement") added to TrStudio's + TrSetup's guides, and the same rule added to **`build-invocation-ladder.md` §"What NOT to do"** (with the `NETSDK1005`/`NETSDK1139`/TFM-guard error signature so it reads as rung-selection, not failure). Note: TrSetup's Web-head retention itself was legitimate — REQ-FN-034 carries an "OWNER-AMENDED end-state" (test-only headless smoke host), so only the verdict process, not the scope call, was at fault.
**Mirrors + propagation:** 5 tasks (`verify-phase`, `build-phase`, `_smoke-test-policy`, `refresh-status`, `day1-brownfield`) + `flow-master` agent copied byte-identically to both harness dirs (`diff -rq` clean); templates (`build-invocation-ladder`) not mirrored (relative-path loads); no new command → no `opencode.jsonc` change. `update-framework.sh` run on all 7 apps (AppManager, AppStudio, AstroLyfe, TechieRag, TrBlazeUI, TrSetup, TrStudio) — hook + settings.json + tasks verified present in each. WORKFLOW.html §12 (third-hook panel + JSON) + §10 naming panel + README mirrored; "Last revised" bumped. **Restart any open session against these apps to load the refreshed defs + settings — especially the TrSetup session that self-attested.**
**Follow-up (same day) — the guard WORKED, but the ladder had a UAT gap.** The owner re-ran `*build-phase TrSetup` in a restarted session: the agent genuinely executed the whole pipeline this time (REQ-FN-035 rename done incl. namespaces/WinUI shim/manifest/scoped-CSS + all doc paths; verify-phase executed inline — run ledger `docs/.last-verify.json` dated 2026-07-10, fresh desktop+mobile screenshots in `test-results/` (04:25–04:26), renamed `TrSetup.exe` built 04:21 + booted with today's Serilog log written, 7/7 specs, 126/126 unit; evidence audited from this session — NOT self-attested). But it ended by suggesting `*handoff-phase` ("optional") while 8 owner-run UAT rows were still open — the owner rejected that as the same "rushing to done" smell. Root cause: the Build→Verify→Handoff ladder had NO tier for "all agent-verifiable rows terminal, only documented owner-run UAT rows open", so agents freelanced handoff suggestions into the gap (TrSetup's handoff had already run twice). Fix: a **UAT tier** inserted into the ladder (`_status-update-gate.md` item 5 + `build-phase.md §7`): phase `UAT`, next command = the owner-run UsageGuide-UAT pointer naming the REQ IDs; `*handoff-phase` is NEVER suggested (not even "optional") when a READY-FOR-UAT handoff already ran this cycle — it leads exactly once, when it hasn't; no "done"/"nothing left"/`Released` language while non-terminal rows remain (`Released` is the owner's call). `handoff-phase.md` gained a matching **run-once-per-UAT-cycle guard** (declines a repeat unless owner-invoked or substantive post-handoff changes landed). TrSetup's PROJECT-STATUS (.md + .html) corrected — the "Optional: *handoff-phase" line removed, owner UAT pointer names the 8 REQ IDs. 3 tasks re-mirrored ×2 + re-propagated to all 7 apps (verified).

### 2026-07-09 — PROJECT-STATUS shape enforced MECHANICALLY, Serilog file logging made a standing .NET default, TrSetup simplified to a single MAUI desktop app
Three owner reports in one session. **(1) Agents still convolute PROJECT-STATUS** despite the 2026-06-30 crisp-shape prose rules — repeat friction, owner has to keep correcting it. Applied the same cure that fixed the git problem: a **mechanical guard**. New PreToolUse hook **`.tfcore/hooks/guard-status.sh`** (matcher `Write|Edit|MultiEdit`, python3-backed, fails open without python3) blocks any Write/Edit/MultiEdit to a file named `PROJECT-STATUS.md` that: adds an H2 outside the template's fixed section set (Where I am / Next command to run / Open requirements / Known blockers / Verification log / Library feedback summary / Standards compliance / Deferred / future / Recovery note — trailing dates/qualifiers tolerated), has a heading naming a command run (`*verify`/`*build-phase`/`*fix-issues`/`*refresh-status`), stuffs `current_phase:` past ~120 chars, or full-file-Writes more than 120 lines. The block message is the reshape recipe (overwrite sections in place, ONE Verification-log row, detail → checklist Remarks). Tested with an 11-case suite (good/bad H2s, Recovery-note allowance, MultiEdit `edits[]`, oversized write, template passes, malformed-JSON fail-open). Wired into `hooks.PreToolUse` in the framework's `.claude/settings.json` + the settings heredocs in all THREE scripts (`scaffold-greenfield.sh`/`scaffold-brownfield.sh`/`update-framework.sh`; JSON re-validated, `bash -n` clean). `_status-update-gate.md` + `app-project-status-tmpl.md` now state the mechanical enforcement (and warn against sneaking narrative past the guard in split edits). `WORKFLOW.html` §12 + README document the second hook + config.
**(2) Serilog file-based logging is now a STANDING default for every .NET app type** (web, API, MAUI, desktop, console, background service) — the owner should never have to ask for it again. Baked in end-to-end: `app-brd-tmpl.md` §11 carries a standing Observability NFR line (comment marks it non-droppable); `day1-greenfield.md` ALWAYS emits it; `day1-brownfield.md` emits it as met/`Done (pre-existing)` when the §5 scan finds an equivalent structured file-logging stack, else `Planned`; the canonical coding-standards block (day1-brownfield §4) gained a **§Logging** recipe (rolling file sink `logs/<app>-.log` daily/14-retained, startup wiring per host type incl. `MauiProgram` + per-app data dir for MAUI/desktop, unhandled-exception + `CloseAndFlush`, libraries reference abstractions only, `ILogger<T>` in app code, gitignored logs/); `app-architecture-tmpl.md` §5 states the mandate; and `build-phase.md §3` says any cluster that CREATES a new executable head wires Serilog at scaffold time even if no REQ names logging (legacy checklists: wire + note in Remarks). `WORKFLOW.html` §10 + README document the standing standard.
**(3) TrSetup: owner withdrew the CLI + Web heads** — "one single MAUI desktop app should be fine; CLI/Web is over-complication" (the three-head shape came from the original TrSetup-Plan). Docs-only pass (code removal deliberately left to the next build run): checklist Goal rewritten; FN-011/012/013/032/033 + NFR-006 → `N/A` (WITHDRAWN 2026-07-09, history preserved in Remarks + detail bullets); FN-031 rescoped `Needs re-verify` (guide rewritten MAUI-only, publish scripts withdrawn); **new REQ-FN-034** (decommission: delete `src/TrSetup.Cli`, `src/TrSetup.Web`, `tests/unit/TrSetup.Cli.Tests`, sln entries, `scripts/publish.*` + `preflight-gate.*`, migrate `playwright.config.ts` off the Web head; keep App/UI/Core/Core.Tests; build green rung #4) + **new REQ-NFR-007** (Serilog in the MAUI head per the new standing NFR — TrSetup had NO file logging) + **new REQ-UI-006** (owner follow-up same day: the configuration surface — endpoint values like the Mac IP, settings-file path, profile details — was CLI-only (`trsetup config`/`--mac-ip`/SetupPrompt) and dies with the CLI; a GUI Settings screen (`/settings`, BRD-56, UIDesign §Settings control map) moves it into the MAUI app); FN-019/FN-020 status columns reconciled to their own "VERIFIED 2026-07-07" evidence (were stuck `Implemented`). PROJECT-STATUS rewritten to the crisp shape (was itself a pre-guard append-log): 29 Verified · 8 Implemented(UAT) · 1 Needs re-verify · 2 Planned · 6 N/A of 46; next command **`*build-phase TrSetup`** (ladder: Planned REQs exist → build leads). BRD (BRDs withdrawn + 2 appended, §4 table, F-catalog), Architecture (single-head table, superseding ADR, diagrams, §5 Serilog line), DevGuide (Non-GUI-heads section retired), UsageGuide (CLI/Web run steps stripped, UAT plan restated against the MAUI board), BuildAndRun-Guide (Option A/B framing collapsed to MAUI-only) all rewritten + re-rendered via the shared shell; `TrSetup-AgentMode.md` archived to `docs/OldDocs/`. Session memory `trsetup-plan.md` updated. **Known follow-up:** `AppStudio-BuildAndRun-Guide.md` cited TrSetup's old Option B CLI as its Mac bootstrapper — cross-reference updated the same day.
**Mirrors + propagation:** 4 tasks (`_status-update-gate`, `day1-greenfield`, `day1-brownfield`, `build-phase`) copied byte-identically to both harness dirs; templates (`app-brd-tmpl`, `app-architecture-tmpl`, `app-project-status-tmpl`) not mirrored (relative-path loads); no new command → no `opencode.jsonc` change. `update-framework.sh` run on all 7 deployed apps (AppManager, AppStudio, AstroLyfe, TechieRag, TrBlazeUI, TrSetup, TrStudio) — each got the new hook + settings.json + tasks. **Restart any open session against these apps to load the refreshed defs + settings.**

### 2026-07-05 — Deployed framework copies were committable in app repos; `.gitignore` is now managed by the scaffold/update scripts
Owner report: `.opencode/` (and other framework files) were not gitignored everywhere — and the review that should have caught it hadn't. Audit findings: the 4 committed apps already ignored the three dirs (AppManager/TechieRag/TrBlazeUI fully; AppStudio missed `/CLAUDE.md` + `/WORKFLOW.html`; AstroLyfe missed `/WORKFLOW.html`), TrStudio (not yet a git repo) had no entries, and **the TechieFlow framework repo itself had NO `.gitignore` at all** — `.claude/settings.local.json` (per-machine approvals) was tracked. Fixes:
- **Policy, stated where it can't rot:** everything the framework deploys into an app is a **copy** (source of truth: this repo, or the NuGet package for library personas) and must never be committed in the app repo. All three scripts (`scaffold-greenfield.sh` §9, `scaffold-brownfield.sh` §8, `update-framework.sh` §8) now END by ensuring the app's `.gitignore` carries the framework block: `.tfcore/`, `.claude/`, `.opencode/`, `/CLAUDE.md`, `/WORKFLOW.html`, `/opencode.jsonc`, `/.tf-scaffold-note.txt`. Append-only + idempotent: any existing anchored/slash variant of an entry is respected (matching strips `\r` first — the app `.gitignore`s are CRLF, which silently defeated a naive `$`-anchored grep in dry-run testing), user content is never rewritten, and `--dry-run` prints what WOULD be added.
- **Framework repo `.gitignore` created** — but the OPPOSITE policy applies here and the file's header comment says so: `.tfcore/`, the `.claude/`+`.opencode/` harness mirrors, `WORKFLOW.html`, `opencode.jsonc` are the framework's *source* (the scripts rsync FROM them), so they stay tracked; only per-machine/local files are ignored (`.claude/settings.local.json`, `.opencode/session/`, `*.bak`, `tmpclaude-*`, OS junk).
- **All 6 apps refreshed** via `update-framework.sh`; each `.gitignore` now carries the full block. Git-index audit (read-only parse — agents can't run git): AppManager/AppStudio/AstroLyfe clean; owner must once run `git rm --cached` for the 3 stragglers still tracked from before their ignore entries existed — TechieFlow `.claude/settings.local.json`, TechieRag `.tf-scaffold-note.txt` + `opencode.jsonc`, TrBlazeUI `opencode.jsonc`.

### 2026-07-04 — Template mandates leaked into rendered HTML (fixed as comments + render-strip) and library persona files fixed at their source repos
Two owner reports. **(1) Every new HTML doc showed a "Depth mandate" header + other authoring statements.** Root cause: the BRD / Architecture / DevGuide templates carried the drafting-agent instructions ("Depth mandate — read before drafting", "Mermaid mandate") as **visible markdown blockquotes** — unlike the ToC hints, which were already HTML comments — so agents copied them into every generated doc and they rendered for the human reader. Fixes:
- **Templates** (`app-brd-tmpl`, `app-architecture-tmpl`, `app-devguide-tmpl`): all agent-facing mandate blockquotes converted to a single `<!-- AGENT-ONLY AUTHORING NOTES … -->` HTML comment that explicitly says "NEVER emit as visible text". Reader-facing blockquotes (DevGuide verification banner + Purpose, UsageGuide test-user rules, UIDesign/ProductGuide intros) untouched.
- **`html-render-shell.md` — new §6b** "Agent-facing authoring notes NEVER render as visible text": a doc generated from a pre-2026-07 template that still carries a visible mandate blockquote gets it **stripped at render** (omission reported in the output summary); §9 checklist gained the matching item. `generate-html.md` + `render-workflow-docs.md` reference §6b in their shell-section lists; `generate-html.md` §2.6 carries the full rule + a new Output-Checklist item. Existing app docs therefore self-heal on their next HTML render; their `.md` gets the comment-conversion whenever an agent next edits it.
- **Bonus gap:** the top-level `/generate-html` wrappers (`.claude/commands/generate-html.md`, `.opencode/command/generate-html.md`) were stale June-13 copies that still said checklist docs are renderable (banned 2026-06-26). Regenerated from the current task.
- **Mirrors:** `generate-html.md` + `render-workflow-docs.md` copied byte-identically to both harness dirs; templates not mirrored (relative-path loads). Propagated to the 6 `.tfcore` apps via `update-framework.sh`.

**(2) Owner asked whether the framework wrote anything custom into the trblazeui/techierag agent files (NuGet re-deploys overwrite them).** Yes — and the arrangement was broken: during the 2026-06-13 rebrand the framework's copies were hand-edited (removed "Powered by BMAD" header; trblazeui's `.bmad-core/{type}/{name}` → `.tfcore/…`), but both libraries' MSBuild targets re-copy the packaged file over `.claude/commands/<lib>.md` on **every consumer build**, so those edits never survived in any app — and the framework copy of trblazeui.md had also gone stale (missing the library's newer PortalHost-interactivity + scoped-CSS guidance). Per owner direction (supersedes the old §6 "libraries are NOT modified by this repo" stance), fixed at origin:
- **TrBlazeUI repo:** `docs/skills/claude-code-trblazeui.md` — BMAD header removed, `.bmad-core/` → `.tfcore/`. OpenCode variant was clean.
- **TechieRag repo:** `src/TechieRag/build/content/techierag-claude-command.md` — BMAD header removed; now byte-identical (modulo CRLF) to the framework copy. OpenCode variant was clean.
- **Framework snapshots refreshed** from the updated sources (`.claude/trblazeui.md`, `.claude/commands/trblazeui.md`, `.opencode/command/trblazeui.md`) — picking up the library's newer content too.
- **Feedback files updated:** TR-001/TR-RAG-001 marked ✅ resolved (verified both `.targets` now deploy straight to `.claude/commands/` — the old shim is a harmless no-op); new **TR-002/TR-RAG-002** document the futile-consumer-edit trap and the remaining owner action: **repack + republish TrBlazeUI.Components and TechieRag** so consumers get the corrected personas (until then every `dotnet build` re-stamps the old BMAD-branded file).
- **Standing policy (also in session memory):** persona/skill customizations for library agents go in the LIBRARY repos and ship via the package — never edit a consumer repo's copy.

### 2026-07-02 — Two persistent behavior failures fixed at the ROOT: git ban made mechanical (deny + hook), run-it-yourself moved into always-loaded surfaces, and native-head window-binding discipline added
The owner reported that despite the existing prose rules, (1) agents **still ran git during PROJECT-STATUS updates** — and when the owner denied the prompt, they broke the status file with convoluted writes; (2) agents **still asked the owner to run the app for verification** and claimed "can't run from Linux" despite the Playwright/MAUI/Appium harness being fully set up; and (2b) during MAUI automation, **agents typed into a DIFFERENT window than the app under test**. Diagnosis: all three were structural, not prose-strength, problems:

- **The framework itself still instructed agents to use git.** `build-phase.md §3` told every builder sub-agent to "commit each REQ `[REQ-UI-N]`" (×3 clusters), `fix-issues.md §4` said "each sub-agent commits with the `[REQ-*]` tag", and the CLAUDE.md template's line 20 mandated "reference REQ IDs in commit messages" — so git felt sanctioned no matter what the ban said. `opencode.jsonc`'s refresh-status description also still said "ground truth (git + working tree …)".
- **The ban was ask-not-deny, and prefix rules leak.** `settings.json` had `Bash(git *)` under `ask` — but that's a literal prefix match, so compound forms (`cd src && git log`) sailed through the bare `Bash` allow with NO prompt at all; simple forms hit the owner as deniable prompts, and a mid-gate denial left the agent flailing (→ the convoluted status writes).
- **The rules lived only in task-include prose** (`_smoke-test-policy` / `_status-update-gate`), which loads only when a task runs and is skimmable; the agent PERSONAS (except verifier's "zero manual steps") and the always-loaded app CLAUDE.md carried neither the git ban nor the run-it-yourself rule. And **library sub-agents (`/trblazeui`, `/techierag`) never read `.tfcore/` tasks at all**, so no rule ever reached them unless the orchestrator's prompt carried it — it didn't.
- **No window-targeting rule existed anywhere.** verify-phase §3b even scoped the MAUI Windows head "out of scope"; nothing banned global keyboard injection (FlaUI `Keyboard.Type`, `SendKeys`, coordinate clicks), which types into whatever window has focus — the exact observed failure.

**Fixes:**
1. **Mechanical git ban.** `settings.json`: `Bash(git *)`/`Bash(gh *)` moved **ask → deny** (+ bare `Bash(git)`/`Bash(gh)`), and a new **PreToolUse hook `.tfcore/hooks/block-git.sh`** blocks git/gh as a command word ANYWHERE in a Bash command (catches `cd x && git log`; regex-tested: `.gitignore`, `github.com`, paths stay legal). The hook's block message is the on-the-spot recovery recipe (checklist tables + working-tree files + fresh build; "use Write/Edit if your file merely mentions git") — converting the old flail-inducing denial into instruction. Updated in all THREE canonical sources: the framework's own `.claude/settings.json` + the heredocs in `scaffold-greenfield.sh`/`scaffold-brownfield.sh`/`update-framework.sh` (JSON validated; `bash -n` clean). `hooks` added to `update-framework.sh`'s `FRAMEWORK_SUBDIRS` so the hook force-syncs to apps. The owner still runs git manually (separate terminal or `!git …` — bang commands bypass agent permissions).
2. **Pro-git prose purged.** `build-phase.md §3`: sub-agents now "tag work `[REQ-*]` in the checklist Remarks (NEVER a git commit)", and the section defines **"the two standing rules"** (no-git + smoke-it-yourself) that every sub-agent prompt MUST carry **verbatim** — with an explicit "library agents don't read `.tfcore/` tasks, the rules reach them only through you" and "a sub-agent that git-commits or returns un-smoked code is the orchestrator's failure — reject and re-prompt". `fix-issues.md §4` same. CLAUDE.md template: commit-message line → checklist-Remarks tagging. `opencode.jsonc` refresh-status description de-git-ed.
3. **Always-loaded surfaces now carry both rules.** All four agent personas gained core principles: **GIT IS MANUAL** (all 4), **RUN IT YOURSELF** (flow-master + analyst; verifier already had zero-manual-steps), **SUB-AGENTS INHERIT THE RULES** (flow-master), **BOUND-WINDOW INPUT ONLY** (verifier). The CLAUDE.md template gained a **"Hard rules (non-negotiable)"** section right under Required-reading: git-manual (+ the enforcement pointers), run-it-yourself (+ the banned excuses), window-binding; its Permissions paragraph now says git/gh are DENIED.
4. **Window-binding & input discipline** (new, the 2b fix): `verify-phase.md §3b` — bind the session to the app under test **by identity** (launched **PID → top-level window handle** via `appium:appTopLevelWindow`/FlaUI `Application.Attach(pid)` on Windows; package/bundle id on mobile), **element-scoped input ONLY via `AutomationId`**, global keyboard/mouse injection BANNED, focus verified before every input burst, handles re-resolved after dialogs, missing `AutomationId` = logged coding-standard defect (never a coordinates fallback). The §3b intro now states the discipline applies IN FULL to the MAUI **Windows** head (was "out of scope"). Short versions added to `_smoke-test-policy.md` (new "input goes to the app's OWN window" paragraph) and `build-invocation-ladder.md §D`; verifier persona principle as above.
5. **Docs:** `WORKFLOW.html` §12 rewritten (deny + two-layer explanation + new config JSON incl. hooks; §11 window-binding panel; "Last revised" bumped) and `README.md` mirrored. This doc's §4 refresh-status line updated (git now harness-denied).
6. **Gate prose reinforced** for the denial-flail path: `_status-update-gate.md` §"Never run git" now says a blocked git call means *use the local evidence path* and is NEVER a reason to guess or pad the file — the checklist table alone suffices for every section; `_smoke-test-policy.md` §"Git is manual" mentions the mechanical enforcement + the sub-agent-prompt carry rule.

**Mirrors:** 5 tasks (`build-phase`, `fix-issues`, `verify-phase`, `_smoke-test-policy`, `_status-update-gate`) + all 4 agents copied byte-identically to `.claude/commands/TechieFlow/` + `.opencode/command/TechieFlow/` (verified `diff -rq`). Templates (`app-claude-md-tmpl`, `build-invocation-ladder`) not mirrored (relative-path loads). No new command → no `opencode.jsonc` key change (one description fixed). **Propagated to the 6 `.tfcore` apps** via `update-framework.sh` (each app got the new settings.json + hook + tasks/agents; per-app details below). **Existing apps' CLAUDE.md files** (preserved by the updater) were hand-patched once with the new Hard-rules section + the commit-line fix, mirroring the template change. **Restart any open session against these apps to load the refreshed defs + settings.**

### 2026-06-30 — Removed §13 "Document templates" (~36% of WORKFLOW.html) — agent-facing duplication
Owner questioned why the human workflow doc reproduces the full doc *templates* (§13, 13.1–13.13). Confirmed it's agent-facing duplication: 698 lines (~36% of WORKFLOW.html) of every artifact's raw markdown schema, while the canonical copies live in `.tfcore/templates/v4custom/` (drift risk + bloat of the doc the owner re-reads). §6 "Doc artifacts & audiences" already gives the human-facing catalog. Owner chose **remove entirely** (over slim-to-pointer). Done:
- **`WORKFLOW.html`** — deleted the whole §13 block (banner + 13.1–13.13). **Renumbered** §14→13, §15→14, §16→15, §17→16 (headings + the one visible `§15`→`§14` cross-ref). Removed the nav "Doc templates" entry. **Repointed all inbound links** to the deleted §13 anchors — `#t-devguide`/`#t-productguide`/`#t-html-render` and bare `§13.x` refs → `#docs` (§6, the doc catalog) or §10 (coding standards) as fit. Verified: **zero** remaining `§13` refs and **zero** broken `#t-*`/`#templates` hrefs; sections now a clean 0,0b,1–16. File 1965 → 1266 lines. (The "Process at a glance" flowcharts + mermaid runtime added earlier this day are above §13, untouched.)
- **`README.md`** — same removal + renumber + repoint (markdown form; 729 lines removed; zero `§13` refs remain; headings 1–16).
- **Propagated:** `WORKFLOW.html` force-synced to all 6 `.tfcore` apps (byte-identical verified). README framework-repo only.
- Repo-map (§3) line updated: "16 sections; opens with Process-at-a-glance flowcharts; templates live only in `.tfcore/templates/`".

### 2026-06-30 — WORKFLOW.html + README gain "Process at a glance" quick-reference flowcharts
Owner: re-reading the whole `WORKFLOW.html` to re-grok the process flow is time-consuming — asked for a few simple command-bearing flowcharts for quick reference. Added a prominent **"Process at a glance — flowcharts"** section at the very top of `WORKFLOW.html` (new nav group "Quick reference" → `#ataglance`, before §0) with **5 mermaid flowcharts**, each box = the exact command: **A** Greenfield (scaffold → day1-greenfield → mockups → approve → split-brd → build-phase → verify ⇄ fix-issues → handoff), **B** Brownfield (scaffold-brownfield → day1-brownfield → split-brd? → build-phase → verify ⇄ fix-issues → handoff), **C** the **Build→Verify→Handoff next-command ladder** (pick by weakest open REQ), **D** recovery (`*refresh-status`), **E** library docs (`*day1-brownfield` → `*split-brd` → `*devguide --update`; UI=component-by-component, service=service-by-service).
- **Mermaid runtime wired in:** `WORKFLOW.html` had ZERO live diagrams (its only `mermaid` mentions were example source inside `<pre><code>` template blocks). Added the mermaid CDN `<script>` + `mermaid.initialize({startOnLoad,theme:'dark',securityLevel:'loose'})` before `</body>`, a `pre.mermaid` card style, and — critically — **fixed the copy-button JS from `querySelectorAll('pre')` → `pre:not(.mermaid)`** (injecting a copy `<button>` into a `pre.mermaid` is the exact 2026-06-17 "Syntax error" bug; see [[mermaid-quote-labels]]). All diagram/edge labels are double-quoted, no `end` node ids (§5.5 safe).
- **README.md** mirrored — same 5 charts as fenced ```mermaid blocks (GitHub renders them) in a new top "Process at a glance" section.
- **Propagated:** `WORKFLOW.html` is in the updater's force-sync set → ran `update-framework.sh` on all 6 `.tfcore` apps; verified byte-identical in every app. (README is NOT force-synced — framework-repo only, fine.) No task/agent/template/`opencode.jsonc` change.

### 2026-06-30 — Library DevGuide mode generalized to TWO sub-types (TechieRag is a service/SDK lib, not a UI-component lib)
Right after the component-by-component fix below, owner asked the sharp follow-up: TechieRag's DevGuide is screen-by-screen of its sample app — is *that* right, or does it also need correcting? Investigation: TechieRag ships **0 package `.razor` components** (all 15 `.razor` are in `samples/TechieRagWeb`); its public surface is `AddTechieRag(...)` (DI extension) + the `ITechieRag` facade + service classes. So it is a **service/SDK library**, a *different kind* than TrBlazeUI (a UI-component library) — and the component-by-component mode added below doesn't fit it. Worse, the §0 detection I'd just written keyed on `src/*.Components`, so TechieRag fell through to plain application/screen-by-screen mode — i.e. the existing (suboptimal) state. **TechieRag's current DevGuide documents the sample app's screens, not the package a consumer codes against** — a real gap.

Fix: generalized DevGuide **library mode into two sub-types** (`devguide.md §0`, intro, §1; `app-devguide-tmpl.md`; `WORKFLOW.html`/`README.md` §13.12):
- **UI-component library** (TrBlazeUI) → **component-by-component**; API surface = `[Parameter]`/`EventCallback`/`@typeparam`.
- **Service/SDK library** (TechieRag) → **service/API-by-service**; API surface = the `AddXxx(...)` DI registration + options + facade/interface method signatures; internal flow = the call pipeline (ingest→chunk→embed→upsert; query→embed→vector-search→prompt→LLM) + external deps + resilience/token tracking; modules = Ingestion/Embedding/Query-RAG/LLM/Resilience+Token/Admin.
- Both share the core rule, stated explicitly with a ⚠ callout: **a library is documented by its PUBLIC SURFACE, never by its sample app's screens** (the sample is the exerciser you boot for screenshots + usage snippets, not the unit). Detection picks the sub-type by what `src/` actually ships, not by the sample.
- **Follow-up (same day) — "does the SAMPLE also need updating?"** Owner's next question. Answer: it depends on **coverage**, and the DevGuide regen is the diagnostic. Added a **coverage cross-check** to `devguide.md §0` (step 3): the library-mode pass maps each public component/service to where the demo/sample exercises it and **flags any uncovered public API `⚠ no demo coverage` → logs it to the checklist as a sample gap** + reports a covered/uncovered tally in §8. So: complete coverage → only the DevGuide changes; uncovered public API (e.g. a "bring-your-own `IEmbeddingProvider`/`ILlmProvider`/`IVectorStore`" extension point with no example) → the sample needs a small showcase page/snippet so the item is demonstrable + verifiable. **The sample app is part of a library's deliverable, not a backdrop.** Quick check of TechieRag: the 10 sample screens cover the main `ITechieRag` facade well (Ingest/Text/Chat/Ask/Stats/Tokens/Tools/LLM/Vector-admin); the likely thin spots are the pluggable provider interfaces (custom implementations). Mirrored in `app-devguide-tmpl.md` + `WORKFLOW.html`/`README.md` §13.12; re-propagated byte-identical to all 6 apps.
- Mirrored ×2 + propagated byte-identical to all 6 apps (devguide.md + template). No command/agent change.
- **Answer to the owner: yes, TechieRag's DevGuide should be regenerated** in service/API-by-service mode. Teed up (run in the TechieRag repo, restart session first): `*devguide TechieRag --update` (now that §0 classifies it as a service/SDK lib, it'll re-organize around `AddTechieRag`/`ITechieRag`/the public services, using the `samples/TechieRagWeb` screens as per-service demos). Its existing PROJECT-STATUS/BRD/Checklist already exist — only the DevGuide shape needs the regen.

### 2026-06-30 — Libraries are first-class projects: component-by-component DevGuide mode (the TrBlazeUI gap)
Owner asked why TrBlazeUI has no PROJECT-STATUS when it (like TechieRag) is a NuGet library — "they're still projects and should have all the necessary documents including DevGuides explaining the flow of every component code with help of a Sample Application." Correct: it was an **inconsistency, not a principle**. TechieRag was run through the full TechieFlow flow (PROJECT-STATUS + BRD + Architecture + Checklist + Coding-Standards + a screen-by-screen DevGuide built from `samples/TechieRagWeb` + UsageGuide); TrBlazeUI grew up *outside* the flow with its own library-native docs (`CHANGE_LOG`/`RELEASE`/`THEMING`/`CONTRIBUTING`/`TrBlazeUI-Doc`/`-AI-Reference`) + 4 `demos/` projects, and the framework then *codified the gap* ("TrBlazeUI is a library with no PROJECT-STATUS / app-flow"). That exemption is retired. Owner decisions (via question): DevGuide for a UI library is **component-by-component**; **framework fix + tee up** (don't run the heavy TrBlazeUI generation from the framework session).

**The nuance:** a pure UI **component** library's natural unit is the component, not a screen — so a straight screen-by-screen DevGuide doesn't fit. Fix = a **component-library mode** in the DevGuide.
- **`devguide.md`** — new **§0 "Detect the mode — application vs component library"**: a packable lib (`<IsPackable>`/`PackageId`, a `src/*.Components` project, no app screens, a `demos/`/`samples/` showcase) → map **component-by-component**. Per component: **Purpose · Public API surface** (every `[Parameter]`/`EventCallback`/`@typeparam` at file:line — the consumer's contract) **· Internal flow** (render/state/lifecycle/JS interop) **· Demo & usage** (the showcase page + a screenshot from the booted demo app + a copy-paste `<Component …/>` snippet) **· Known issues**. The OBSERVE pass boots the **demo/sample app**; split is by component **category** (Inputs/Layout/Overlays/Data/Feedback/Icons). Everything else (fan-out, render-truth, no-hallucinated-paths, defect→checklist, HTML render) applies unchanged. Intro + §1 also state a library is a first-class project that is never skipped for "it's just a library".
- **`app-devguide-tmpl.md`** — "How to use" gained a **component-library variant** note (component catalog by category; the per-component block shape).
- **`WORKFLOW.html` §13.12 + `README.md` §13.12** — a **Component-library mode** paragraph (libraries first-class; component-by-component; boot the demo/sample).
- **Mirrors + propagation:** `devguide.md` mirrored ×2 and (with the template) propagated byte-identical to all 6 `.tfcore` apps (verified). No new command / agent / `opencode.jsonc` change (rides inside the existing `*devguide`).
- **NOT done (per "tee up"):** TrBlazeUI's actual doc set. **Next step for the owner — run inside the TrBlazeUI repo** (`/mnt/c/3AIGenCode/TrBlazeUI`, restart the session there first to load the new defs): `*day1-brownfield TrBlazeUI` (reverse-docs + PROJECT-STATUS + Coding-Standards; migrates any plan into `docs/TrBlazeUI-Checklist.md`; auto-runs the **component DevGuide** via §7.6) → if no plan was migrated, `*split-brd TrBlazeUI` then `*devguide TrBlazeUI --update`. The DevGuide OBSERVE pass boots `demos/TrBlazeUI.Demo.Server` to screenshot each component.

### 2026-06-30 — PROJECT-STATUS bloat fix: enforce a CRISP, fixed-shape, OVERWRITE-not-APPEND snapshot (+ git ban in the status gate)
The owner reported that despite the 2026-06-26 "crisp + command-not-prose" fix, AstroLyfe's `PROJECT-STATUS` had again become a 286-line convoluted mess — and that deleting + regenerating it reproduced the same mess. Two root causes, both in how agents *wrote* status (the template was already crisp; nothing stopped agents from ignoring its shape):
1. **No "overwrite, don't append" rule.** Every `*verify`/`*fix-issues`/`*build-phase` run **appended a new dated H2** (`## *verify all — formal coverage matrix (DATE)`, `## *fix-issues — …`, `## *build-phase — …`) and stuffed `current_phase:` with a paragraph of run detail. AstroLyfe had ~10 such sections stacked up. The gate (`_status-update-gate.md`) listed *what* to update but never said PROJECT-STATUS is a **fixed-shape snapshot you OVERWRITE** — so the file only ever grew.
2. **Agents reached for `git grep` to compute the status update** (owner kept denying the prompt mid-gate — a likely contributor to broken writes). The "git is manual" rule lived in `_smoke-test-policy.md`/`refresh-status.md`/`devguide.md` but **not** in the status gate that every checklist-executing agent reads.

**Fixes (framework-only):**
- **`_status-update-gate.md`** — two new sections: (a) **"PROJECT-STATUS is a CRISP, FIXED-SHAPE snapshot — you OVERWRITE it, you NEVER append to it"** — enumerates the exact allowed sections, bans per-run dated H2s (a run records ONE Verification-log row + checklist Remarks, full stop), bans paragraph-stuffed `current_phase`/`Where I am`, mandates trimming an already-bloated file back to template shape, with a "grew by more than one log row = you're appending narrative" smell test; (b) **"Never run git to update status"** (no `git grep`/`diff`/`log`/`status` for the status write — reconstruct from checklist tables + working-tree files + a fresh build, all local). Minimum-item 2 (`current_phase`) tightened to "ONE short line, never a paragraph".
- **`app-project-status-tmpl.md` template** — a prominent top-of-body HTML comment block restating the OVERWRITE-not-APPEND rule + the banned per-run-H2 / paragraph-`current_phase` patterns (points at the gate).
- **`refresh-status.md` §4** — a reconstruction must rebuild to the CRISP template shape and NOT copy forward the old file's per-run dated sections / paragraph `current_phase` (this is the "regenerate also produced a mess" path the owner hit).
- **`verify-phase.md`** final gate — explicit "record this run as ONE Verification-log row + checklist Remarks; do NOT add a `## *verify all — coverage matrix` H2 to PROJECT-STATUS" line.
- **Mirrors:** the 3 edited tasks (`_status-update-gate`, `refresh-status`, `verify-phase`) copied byte-identically to `.claude/commands/TechieFlow/tasks/` + `.opencode/command/TechieFlow/tasks/` (verified `diff -q`). Template not mirrored to harness dirs (loaded by relative path); no new command → no `opencode.jsonc`/agent change.
- **AstroLyfe `PROJECT-STATUS.md` cleaned as the concrete fix** — rewrote the 286-line append-log down to the ~78-line crisp template shape (same true state: Verify, 70/88 terminal, 18 open, REQ-UI-022 the lone code-fix, rest env/external/human-blocked), dropped the ~10 stacked dated sections + the paragraph `current_phase` + the stale 06-27 Recovery note (history preserved in the checklist Remarks + `.verify/`). `PROJECT-STATUS.html` re-rendered via the shared shell. **The other apps were already crisp** (~75-81 lines, 0 appended dated sections — they simply hadn't accumulated AstroLyfe's run count yet; the fix is prophylactic for them).
- **Propagated to all 6 `.tfcore` apps** (`update-framework.sh`): AstroLyfe, AppManager, TechieRag, TrBlazeUI, AppStudio, TrStudio — the 4 changed files (3 tasks ×3 dirs + template) verified byte-identical to canonical in every app. SpinSins + TrTools are still legacy `.bmad-core` layout (not TechieFlow) — skipped. **Restart any open session against these apps to load the refreshed defs.**

**Follow-up same day — Next-command must follow the Build → Verify → Handoff ladder (not freelance to `*verify all`).** Owner found AppStudio's PROJECT-STATUS set "Next command = `*verify all`" while the app still had ~12 PARTIAL/unbuilt REQs + service gaps (a brownfield app whose functionality is clearly not built). Root cause: the next-command rule wasn't *state-derived* — `build-phase §7` was binary (all-verified→handoff, else→build) and an agent rationalized jumping to verify after a wiring pass; nothing forbade leading with verify on a half-built app. Fix: a single **state-derived ladder** added to `_status-update-gate.md` item 5 and `build-phase.md §7` — pick the lead command by the *weakest* open REQ: **any REQ unbuilt (`Planned`/`In Progress`/`PARTIAL`/`NOT-IMPLEMENTED`) → `*build-phase` (build is never skipped for verify); all built but some unverified → `*verify all`; all `Verified` → `*handoff-phase`.** "When in doubt, build." `day1-brownfield.md §6` got an explicit **"NEVER set `*verify all` as the day-1 next command"** (day-1 verified nothing; a fresh brownfield checklist almost always has unbuilt features → next is `*split-brd` or `*build-phase`). The 3 edited tasks (`_status-update-gate`, `build-phase`, `day1-brownfield`) re-mirrored ×2 and re-propagated to all 6 apps (byte-identical verified). **AppStudio's PROJECT-STATUS corrected** in place: next command `*verify all` → **`*build-phase AppStudio`** (+ `current_phase` qualifier), HTML re-rendered. Also fixed AppStudio's **`.gitignore`** stale `.bmad-core/` → `.tfcore/` (the owner hit the stale-layout warning; same TechieRag/TrBlazeUI precedent).

**Audited every other app's next-command against the ladder (same day).** Verified all 5 remaining `.tfcore` repos: **AstroLyfe** (Verify tier — everything built bar the one code-fixable defect → targeted `/trblazeui` REQ-UI-022 + `*verify REQ-UI-022`) ✓; **AppManager** (4 `PARTIAL` + 1 `In Progress` genuinely unbuilt → `*build-phase`) ✓; **TrStudio** (`Discovery`, no checklist yet → `*split-brd`) ✓; **TrBlazeUI** (the UI *library* — no PROJECT-STATUS/checklist yet; at the time treated as N/A, but **superseded same day** — see the next-newer log entry: libraries are now first-class and TrBlazeUI's missing docs are a known gap to fill via the flow) ⚠; **TechieRag** (31 `Done` + 6 `Needs re-verify`, **0 unbuilt** → strict ladder says *verify*, but its 6 open REQs are backend/RAG items `NOT-OBSERVABLE` until a **test harness is written**, so its `*build-phase` "add the remaining tests" is the correct human call) ✓. TechieRag exposed a ladder gap, so the **build tier was refined**: a built-but-`NOT-OBSERVABLE` REQ (no test/harness yet) is **build** work (`*build-phase` adds unit tests) and leads until the verifier has something to assert — verify leads only when built REQs are *built AND observable*. Refined `_status-update-gate.md` item 5 + `build-phase.md §7`, re-mirrored + re-propagated (byte-identical verified all 6). Net: every app's next-command is now ladder-correct (only AppStudio needed an actual fix; the rest were already right).

### 2026-06-29 — Appium runtime bridge: data + visual gates now reach MAUI Android / iOS / Mac Catalyst
The owner asked whether the WSL Windows/MAUI bridge (which lets the verifier observe the MAUI **Windows** head) could be extended to **Android** (Android SDK on the Windows host) and **iOS** (a LAN Mac with Xcode + .NET) — "tested once per WSL like Playwright + the Windows bridge." It could, and it filled a real blind spot: the §4a data-render + §4b visual-truth gates only ever reached **Blazor** (Playwright) and the **MAUI Windows** head (FlaUI/Appium-Win). Android, iOS, **and Mac Catalyst** were **build-only** — a MAUI mobile/desktop app could pass every gate while its screens overlapped, clipped, or rendered blank. After a design pass (owner reviewed `Mobile-Runtime-Bridge-Design.md`, then locked: include Mac Catalyst, enable Win11 mirrored networking, add an AutomationId coding standard, emulator-default for Android), implemented the bridge.

**Core insight:** **Appium is the native analogue of Playwright** — same WebDriver protocol, returns a `base64` screenshot (→ §4b) and an element tree with `rect`/text/page-source (→ §4a). So the gates' logic is unchanged; the verifier just gains a third render engine. The WSL side only ever talks to an **HTTP endpoint** — no `adb`/emulator/Xcode in WSL: Android runs on an emulator + Appium on the **Windows host** (reached via `localhost` thanks to mirrored networking; the verifier boots it itself via a `launch` command), iOS Simulator + Mac Catalyst run on the **LAN Mac** (one Appium server, `xcuitest` + `mac2` drivers). An absent/unreachable endpoint degrades that head to `⚠ STATIC-ONLY` (reusing the existing "stack down" pattern), never a faked `Verified`. Builds are **unchanged** — this is purely a *runtime-observe* leg appended after a green build. The MAUI Windows head keeps FlaUI/Appium-Win; Blazor keeps Playwright (owner scope: add Android+iOS+Catalyst, don't disturb the working paths).

**Changes:**
- **`core-config.yaml`** — new (commented, per-app opt-in) `runtimeVerification.appium` registry block (android/ios/maccatalyst endpoints + avd/simulator/launch). Not mirrored (loaded from `.tfcore/`).
- **`build-invocation-ladder.md` (template)** — new **§D "Runtime-observe leg"**: per-head driver table (Playwright / FlaUI / Appium uiautomator2·xcuitest·mac2), endpoint discovery, boot-it-yourself, STATIC-ONLY fallback, `§0b` pointer. Build rungs untouched. Not mirrored.
- **`verify-phase.md`** (+ mirror ×2) — new **§3b "MAUI native heads — drive with Appium"** (endpoint resolution, boot emulator/Appium itself, per-head STATIC-ONLY); §3 routes native heads to §3b; §4a/§4b each gained a "for MAUI native heads, same assertions over the Appium session" paragraph; §7 tears down the Appium session + any emulator it booted; §0 loads the registry; Local-only policy clarified that local device hosts are NOT cloud.
- **`_smoke-test-policy.md`** (+ mirror ×2) — added the Appium mobile/desktop bridge as a permanent capability; new banned-excuse ("it's Android/iOS, can't run from WSL"); new "boot the emulator + Appium yourself" paragraph (iOS/Catalyst Mac is a genuine session dependency → STATIC-ONLY).
- **`devguide.md`** (+ mirror ×2) — §5a OBSERVE notes that MAUI native apps are driven over the verifier's Appium endpoint (same observations/screenshots; unregistered/unreachable head → STATIC-ONLY).
- **`verifier.md` agent** (+ mirror ×2) — `whenToUse` + a new core-principle name the per-head render engines.
- **AutomationId coding standard** — new "MAUI UI testability" subsection in the canonical coding-standards block (`day1-brownfield.md §4`, which `day1-greenfield §4` reuses; day1-brownfield mirrored ×2) + both reference samples (`docs/AppManager-Coding-Standards.md`, `docs/AstroLyfe-Coding-Standards.md`). Stable `AutomationId` = the native analogue of a stable DOM id for Playwright.
- **`WORKFLOW.html`** — new **§0b "Device-host bootstrap"** (mirrored-networking `.wslconfig` step + Android/Mac setup + registry + verify), TOC entry, **§11** mobile/desktop runtime-observe subsection (+ driver table), §17 per-platform table row + intro tweak, new **pain-point card #10**. **`README.md`** — mirrors all of these (§0b, §8 pain card, §11 subsection+table, §17 row+intro).
- **Design doc** `Mobile-Runtime-Bridge-Design.md` at repo root (the approved spec).

**Mirrors:** verify-phase, _smoke-test-policy, devguide, day1-brownfield (tasks) + verifier (agent) copied byte-identically to `.claude/commands/TechieFlow/` + `.opencode/command/TechieFlow/` (verified `diff -q` — all identical). Templates (`build-invocation-ladder`, coding samples), `core-config.yaml`, and root docs are not mirrored. No new command → **no `opencode.jsonc` change** (the bridge rides inside verify-phase/smoke/devguide).

**Propagated to all four existing apps (2026-06-29):** ran `update-framework.sh` on **AppManager / TechieRag / TrBlazeUI** (`/mnt/c/3AIGenCode/<app>`) + **AstroLyfe** (`/mnt/c/1MyCode/AstroLyfe`). Verified byte-identical to canonical: the 5 tasks/agent across all 3 dirs (`.tfcore/` + both harness mirrors) + the `build-invocation-ladder.md` template + `WORKFLOW.html`, in every app (4 apps × [5 files × 3 dirs + ladder + WORKFLOW.html] = 68 files, all match). Per the updater's preserve-list, each app's **`core-config.yaml` (preserved — per-project)** did NOT receive the commented `runtimeVerification.appium` example, and each app's **`docs/` (preserved)** keeps its existing coding-standards doc (the AutomationId rule reaches *new* apps via the force-synced `day1-brownfield §4` block; an existing app's coding-standards doc gets it only on regenerate/`*amend-docs`). `README.md` is not in the updater's force-overwrite set (apps carry `WORKFLOW.html`). **To actually USE the bridge an app must (a) add its `runtimeVerification.appium` block to `core-config.yaml` and (b) the host must complete the §0b one-time setup** — both per-app, opt-in. **Restart any open session** against these apps to load the refreshed task/agent defs.

### 2026-06-26 — MAJOR redesign: one checklist, dissolved build phases, mockups, the visual-truth gate, and `*fix-issues`
The owner reported the framework's central failure: **the verifier passes and the DevGuide says every control "renders," yet the running UI is completely broken — controls overlapping, off-screen, invisible.** Root cause (a designed-in blind spot, not agent laziness): the 2026-06-24 runtime-grounding loop only ever checked that **controls render their DATA** ("rows present AND non-empty"); it never looked at **layout/pixels**, so a screen with all its data but overlapping/clipped controls passed every gate. The owner also asked for greenfield mockups (the broken UI had no design to build against), a single bug-fix entry point, and — after discussion — collapsed the two checklists into one and dissolved the separate build commands. Five framework changes (all five owner decisions locked in the planning conversation):

1. **ONE checklist replaces the two.** `<APP>-UI-Checklist.md` + `<APP>-Functional-Checklist.md` → a single `docs/<APP>-Checklist.md` with all `REQ-UI/FN/NFR/RAG-*` in one Requirements Status table. Prefixes still route work and scope `*verify ui|functional|all` (which now FILTER the one table, not pick a file). New template `app-checklist-tmpl.md`; the two old templates deleted. Every reader/writer updated: `split-brd` (emits one file), `verify-phase`, `devguide §6a`, `refresh-status`, `amend-docs`, `day1-greenfield`/`day1-brownfield`, `_status-update-gate`, `handoff-phase`, `render-workflow-docs`, `generate-html`, `TOKEN-GUIDE`, templates `app-brd-tmpl`/`app-project-status-tmpl`/`app-claude-md-tmpl`.
2. **Build phases dissolved → one `*build-phase` (flow-master).** Deleted `build-ui-phase.md` + `build-rag-phase.md` (+ their opencode entries); renamed `build-functional-phase.md` → **`build-phase.md`**, reframed as the single unified build: clusters ALL open REQs and **calls `/trblazeui` (REQ-UI-*, from the mockups) and `/techierag` (REQ-RAG-*) as sub-agents** while building FN/NFR itself; self-smokes (data + visual); chains the verifier. trblazeui/techierag are now **skills parent agents call**, not standalone phase commands.
3. **Greenfield mockups.** New `*mockups` task (analyst) + `app-uidesign-tmpl.md`: reads the TrBlazeUI catalog FIRST, then produces `docs/<APP>-UIDesign.md` (per-screen `region → TrBlazeUI control` map) + rendered `docs/mockups/*.html` styled to look like TrBlazeUI — **replicable by construction**. Auto-run at greenfield day-1 (§3.6), approved with BRD + Architecture before build, used as the build contract AND the verifier's visual baseline. **Brownfield gets the counterpart:** `devguide §5a` OBSERVE now captures a screenshot of every screen to `docs/screenshots/<APP>/`, embeds them in the DevGuide, and §5b runs an owner visual-review gate ("what needs to change?") — which also positions the DevGuide to become a user guide.
4. **VISUAL-TRUTH gate.** New `verify-phase §4b` (parallel to the §4a data-render gate) + a VISUAL-TRUTH section in `_smoke-test-policy.md`: Playwright asserts no control overlap, every control in-viewport & non-zero size, at desktop + mobile widths, inspects a screenshot, and diffs vs the mockup when one exists. **A REQ is `Verified` only if acceptance passes AND data renders AND the screen looks right** (`VISUAL-FAIL → Needs re-verify`, remark prefixed `⚠ visual:`). This is the actual fix for the false passes. verifier agent + devguide banner updated.
5. **`*fix-issues {AppName} {folder}` (flow-master).** The bug-fix front door — the answer to "the verifier passed but the UI is broken, who do I call?" is **flow-master, not the analyst**. Owner drops screenshots (+ optional description) in a folder; it reproduces with Playwright, triages (layout/data/logic/RAG), fans the fix to the right builder (trblazeui / its own subagents / techierag — **it calls them; the owner never invokes a builder agent**), re-smokes (data+visual) + re-verifies, updates DevGuide + checklist + PROJECT-STATUS.

**Mirrors/wiring:** all edited/new `.tfcore/tasks/*.md` + `.tfcore/agents/*.md` copied byte-identically to `.claude/commands/TechieFlow/` + `.opencode/command/TechieFlow/` (verified `diff -rq` — identical); `build-ui-phase`/`build-rag-phase`/`build-functional-phase` removed from all three dirs. `opencode.jsonc`: removed the two build keys, renamed `build-functional-phase`→`build-phase`, added `mockups` + `fix-issues`, updated flow-master/analyst descriptions, split-brd/verify-phase descriptions (validated JSONC parses, 23 command keys). Agents `analyst` (+`*mockups`, dep), `flow-master` (`*build-phase`, `+*fix-issues`, deps `build-phase`/`fix-issues`/`mockups`/`verify-phase`/`_status-update-gate`), `verifier` (one checklist + visual-truth). `WORKFLOW.html` + `README.md` rewritten to the new flow (0 stale refs). Memory: new `[[one-checklist-model]]`, `[[build-phase-unified]]`, `[[visual-truth-gate]]`, `[[greenfield-mockups]]`, `[[fix-issues-frontdoor]]` + index.

**Existing apps migrated (one-time, doc-only, same day) — AppManager, TechieRag, AstroLyfe** (TrBlazeUI had no checklists, skipped). Ran `update-framework.sh` on each (propagated the new tasks/templates, removed the old build phases + two checklist templates — verified byte-identical to canonical). Then merged each app's two checklists → one `<APP>-Checklist.md`, **lossless** (AppManager 24 UI + 50 Fn = 74 rows; TechieRag 13 + 24 = 37; AstroLyfe 38 + 50 = 88), every Status/%/Remarks verdict + anchor preserved (all Details links verified resolvable); originals archived to `docs/OldDocs/`; PROJECT-STATUS/BRD (+ AstroLyfe DevGuide) references repointed and the human HTML re-rendered. No app source touched, no git run.

**Each app's PROJECT-STATUS updated with the post-migration next step (2026-06-27)** — the unifying message to every owner: *prior `Verified`/`Done` verdicts all predate the visual-truth gate, so nothing has been visually confirmed.* Each got a dated `## Framework migration note` (merged checklist + dissolved build commands + visual-truth gate + `*fix-issues` + restart-session) and a discipline-correct "Next command" (HTML re-rendered): **AppManager → `*build-phase AppManager`** (build the open/stubbed REQ-FN-* incl. ⚠ SECURITY, then it self-smokes data+visual + chains verify); **TechieRag → `*build-phase TechieRag`** (adds the remaining tests; live sample UI verify still needs the TrBlazeUI GitHub Packages token, externally blocked); **AstroLyfe → `*verify all AstroLyfe`** (re-grade the just-migrated ~62-file TrBlazeUI UI under the new data+visual gate, then `*fix-issues` with screenshots for any ⚠ visual screen). TrBlazeUI is a library with no PROJECT-STATUS / app-flow — it only needed the framework propagation (done). **Restart any open session against these apps to load the refreshed defs.**

**Follow-up (2026-06-27) — end-user Product Guide + DevGuide screenshots clarified.** Owner asked whether greenfield DevGuides also get UI screenshots (they do — the DevGuide OBSERVE pass §5a captures a screenshot of every screen whenever there is built code to boot, which includes a greenfield app's post-build/handoff DevGuide; only a not-yet-built greenfield repo has none), and asked for a command to turn that into **external-user product documentation**. Clarified the devguide wording (screenshots are universal, not brownfield-only) and added a new **`*productguide {AppName} [scope] [--update]` on flow-master** (`productguide.md` + `app-productguide-tmpl.md`): the user-facing sibling of the DevGuide — same screen inventory + the **same captured screenshots**, re-told as plain-language "what each screen is for / how to do it" for external users; fans out per role, auto-splits for large apps into `docs/productguides/`, and **always emits MD + HTML** (re-shoots any missing screenshots via the verifier render sweep). Owner on whom-owns: flow-master (the doc/status hub that already owns devguide + the screenshot capture + generate-html), not analyst (early-discovery, can't boot apps). Mirrored byte-identical; `opencode.jsonc` entry added (24 keys); handoff §5 points owners at it; WORKFLOW.html/README + memory updated. Propagated to all four apps via `update-framework.sh`.


### 2026-06-26 — Two PROJECT-STATUS complaints: agents wrote technical to-do prose, and updated only the `.md` (not the `.html`)
The owner reported two recurring problems with how agents touch `PROJECT-STATUS`: (1) the "what to do next" content was being written as a **technical narrative** ("wire up the X service, fix the Y binding, then add validation…") instead of **which command to run against which checklist**; (2) agents updated **only `PROJECT-STATUS.md`** and left `PROJECT-STATUS.html` stale — and the owner reads the HTML. Both are reinforcements of rules that already existed but were too easy to skip. Fixes (all framework-only):
- **`_status-update-gate.md` (the shared rule every checklist/phase task includes):**
  - **Headline** now defines "update PROJECT-STATUS" as **BOTH the `.md` AND the re-rendered `.html`** — a markdown-only update is explicitly an *incomplete* update that fails the gate (the owner reads the HTML).
  - **Item 5 (Next command)** rewritten: the next step is expressed **only as a command pointed at a checklist/scope** (optionally naming target REQ IDs) — a **prose description of the technical work is banned**; the *what* lives in the checklist REQ rows, PROJECT-STATUS only says *which command against which checklist*.
  - **Item 8 (HTML refresh)** elevated to MANDATORY/same-turn with forceful wording (was a soft "re-render … defeats the point").
- **Template `app-project-status-tmpl.md`:** "Where I am" annotated as a state snapshot (not a to-do list); "Next command to run" got a comment block with explicit GOOD (command + one line naming the checklist/REQs) vs BAD (technical narrative) examples, plus OpenCode-form hint.
- **Build/verify final-gate lines** (`build-ui-phase` §6, `build-rag-phase` §6, `build-functional-phase` §7, `verify-phase` final gate): the "Updating `PROJECT-STATUS.md` is the LAST action" line — which named only the `.md` and reinforced md-only thinking — now reads "**BOTH `PROJECT-STATUS.md` AND re-rendered `PROJECT-STATUS.html`**; a markdown-only update is incomplete". `verify-phase` also gained the command-against-checklist-only reminder inline.
- **Mirrors:** the 5 edited tasks (`_status-update-gate`, `build-ui-phase`, `build-rag-phase`, `build-functional-phase`, `verify-phase`) copied byte-identically to `.claude/commands/TechieFlow/tasks/` + `.opencode/command/TechieFlow/tasks/` (verified `diff` — 15 files all match). Template not mirrored (loaded by relative path). No new command, no agent/`opencode.jsonc` change (command surface unchanged). `WORKFLOW.html` (§13.5 schema annotations + a new §13.6 paragraph "PROJECT-STATUS.html is re-rendered on every status update") + `README.md` (same two) updated.
- **Propagated to all four existing apps (same day):** ran `update-framework.sh` on **AstroLyfe** (`/mnt/c/1MyCode/AstroLyfe`), **AppManager**, **TrBlazeUI**, **TechieRag** (last three at `/mnt/c/3AIGenCode/<app>`). Verified the 5 tasks across all 3 dirs (`.tfcore/` + both harness mirrors) **+ the `app-project-status-tmpl.md` template** byte-identical to canonical in every app (4 apps × [5 tasks × 3 dirs + 1 template] = 244 files checked, all match). Each run preserved `docs/`/`src/`/`tests/`/`core-config.yaml`/`opencode.jsonc`/library agents; `settings.json` already-current on all four. **AstroLyfe was previously held back from the 2026-06-25 syncs (live DevGuide session) — this run also caught it up** on `_smoke-test-policy`/`day1-brownfield`/`devguide`/`verify-phase`. Restart any open session against these apps to load the refreshed defs.

### 2026-06-25 — Two DevGuide-session complaints: agent asked the owner to run the app, and ran `git diff`
While generating AstroLyfe DevGuides the agent (a) asked the owner to start the app for the OBSERVE pass and only ran it himself after being scolded, and (b) tried to run `git diff` to inspect code — both of which the owner had to deny. Root causes + fixes (framework-only; the AstroLyfe session was left untouched per the owner):
- **(a) Multi-service boot was treated as a license to escalate.** `devguide.md §5a` OBSERVE reuses the verifier to boot the app, but AstroLyfe is a multi-process stack (AstroAPI + AstroBlazeWeb + Postgres + LM Studio + AppManager). The old §5a gave an easy out — *"If the app genuinely cannot be booted (**dependent stack down** and the user can't bring it up now) → STATIC-ONLY"* — so a down dependency became a reason to hand the boot to the owner, contradicting the 2026-06-24 Follow-up-4 precedent (the agent should boot the missing services itself). **Fix:** §5a OBSERVE now has a **"Boot the WHOLE stack yourself — a multi-service app is NOT an excuse"** clause (read each dependent project's `appsettings*.json`/launch settings, start services in dependency order DB/LLM→API→web via the ladder + `verify-phase §3a` escalation); the "cannot be booted" paragraph was rewritten to fire **only after the full escalation (incl. asking the user) genuinely fails**, and a new **hard rule** restates "boot the app yourself, incl. multi-service; asking the user is the LAST resort". The same fix was added to the shared **`_smoke-test-policy.md`** (new banned-excuse bullet "it's multi-service / the API/DB/LLM is down" + a "bring the dependent services up yourself" paragraph) so EVERY build/verify/smoke task inherits it, not just devguide.
- **(b) No shared "git is manual" rule existed.** Only `refresh-status.md` was de-git-ed (2026-06-14); nothing stopped the devguide/verify/build agents from running `git diff`/`log`/`blame` to chase a defect except the CLAUDE.md permission gate (which is why the owner got the prompt). **Fix:** added a **"Never run git"** hard rule to `devguide.md` and a new **"Git is manual — NEVER run git to inspect code"** section to the shared `_smoke-test-policy.md` (investigate by reading working-tree files at `file:line`, never a diff).
- **Mirrors:** `devguide.md` + `_smoke-test-policy.md` copied byte-identically to `.claude/commands/TechieFlow/tasks/` + `.opencode/command/TechieFlow/tasks/` (verified `diff`). No agent/`opencode.jsonc`/template change (command surface unchanged).
- **Propagated to existing apps (same day):** ran `update-framework.sh` on **AppManager / TrBlazeUI / TechieRag** (all at `/mnt/c/3AIGenCode/<app>`); all three picked up the two fixed tasks, verified **byte-identical to the canonical in `.tfcore/` + both harness mirrors** (3 apps × 2 files × 3 dirs = 18 copies, all match). Each run preserved `docs/`/`src/`/`tests/`/`core-config.yaml`/`opencode.jsonc`. **AstroLyfe was deliberately NOT synced** — the owner has a live DevGuide session open there; a mid-session framework swap is the owner's call (they'll sync + restart when that session finishes). Restart any open session against the three updated apps to load the refreshed defs.
- **Incidental cleanup:** the updater flagged a stale-layout pointer in **TechieRag's `.gitignore`** (`.bmad-core/`); corrected it to `.tfcore/` (matching the 2026-06-17 TrBlazeUI `.gitignore` precedent) — the stale-layout warning is now gone. One more pre-rebrand line (`/_bmad`, line 31, not flagged) was left as-is (it ignores a path that no longer exists — harmless; remapping it would be a guess).

### 2026-06-25 — DevGuide now AUTO-RUNS at brownfield day-1 (was handoff-only)
The owner ran `*day1-brownfield` on the **TechieRag** app and the screen-by-screen **DevGuide was not produced** — it had been wired to auto-run only at handoff (`handoff-phase §3a`), even though a brownfield repo already has built code to map on day-1. Per the owner's earlier intent, the DevGuide should be created at brownfield day-1 too (greenfield day-1 correctly has no equivalent — no code exists yet). Fix:
- **`day1-brownfield.md`:** new **§7.6 "Generate the Developer Guide"** — after the §7.5 HTML render, run `.tfcore/tasks/devguide.md` for `{AppName}` end-to-end (fan-out per role, LANDING/RENDER-TRUTH, real-path verification, markdown + sibling HTML, single vs split auto-decide). It handles three realities explicitly: **(a) self-guard** — if the repo is genuinely doc-only the devguide task HALTs itself and §7.6 records "skipped — no built code yet" (not an error); **(b) OBSERVE pass** — the app's stack is usually not up at day-1, so the guide is commonly stamped **`⚠ STATIC-ONLY`** (expected; don't block day-1 booting a stack); **(c) defect logging (§6a) depends on the checklists existing** — if §3.5 migrated a dev plan the devguide logs defects into the checklists, otherwise findings stay in DevGuide prose and the §8 message tells the user to run `*devguide {AppName} --update` right after `*split-brd` so day-1 findings reach the new checklists before the verifier trusts them. Updated the task's opening description, §8 summary (new item 9, HTML renders → item 10), the closing fixed message (mentions the DevGuide + the `--update`-after-split caveat), and the Output Checklist (new §7.6 item).
- **Mirrors:** `day1-brownfield` copied byte-identically to `.claude/commands/TechieFlow/tasks/` + `.opencode/command/TechieFlow/tasks/` (verified `diff`). No agent/`opencode.jsonc`/template change (command surface unchanged; devguide task already registered). `WORKFLOW.html` + `README.md` updated (the day-1 brownfield step blurb, §13.12 DevGuide "When" line now names brownfield day-1, the §15 quick-ref row label).
- **Not yet propagated to existing apps:** `update-framework.sh` will carry §7.6 to AstroLyfe/AppManager/TrBlazeUI on the next sync; their existing DevGuides were generated at/after handoff so nothing is missing there. The owner can re-run `*day1-brownfield TechieRag` (or just `*devguide TechieRag`) to get TechieRag's DevGuide now.

### 2026-06-25 — Multi-part DevGuide clutters `docs/` → split guides now live in a `docs/devguides/` subfolder
The owner reported that for a big multi-role app (AstroLyfe: 8 area files + index; AppManager: 5 + index) the DevGuide's per-role split **fills the `docs/` folder** with a dozen+ `*-DevGuide-*.md/.html` files. Fix: a **multi-part (split) DevGuide is now written into a dedicated `docs/devguides/` subfolder**; a **single-doc** DevGuide stays at `docs/{AppName}-DevGuide.md` (small apps, unchanged). The index + every role file are **co-located in `docs/devguides/`**, so the index's relative links (`./{AppName}-DevGuide-{Role}.md`) resolve unchanged and HTML renders in place beside each `.md`.
- **`devguide.md`:** output line + §3 (single vs split decision: split now `mkdir docs/devguides/` and writes the whole set there; added a **structure-switch cleanup rule** — on a re-run that flips single↔split, move/delete the stale-location copy so there's never both `docs/{App}-DevGuide.md` and `docs/devguides/{App}-DevGuide.md`), §4 `--update` (locate the existing guide in either layout first), §6 render-in-place note, §7 PROJECT-STATUS path, §8 report Files line, + a new Output-Checklist item.
- **`app-devguide-tmpl.md`:** §"How to use" + §"Roles and menu map" note that a split guide's files live together in `docs/devguides/`.
- **Glob references updated** (`docs/{AppName}-DevGuide*.md` would have missed the subfolder): `handoff-phase.md` (re-render set + §3a output), `verify-phase.md` (×3 — DevGuide-as-test-map load, §4a, §6b write-back), `_smoke-test-policy.md` — each now reads "`docs/{AppName}-DevGuide.md`, or the split set under `docs/devguides/`".
- **Mirrors:** `devguide`, `handoff-phase`, `verify-phase`, `_smoke-test-policy` copied byte-identically to `.claude/commands/TechieFlow/tasks/` + `.opencode/command/TechieFlow/tasks/` (verified `diff`). Templates not mirrored (loaded by relative path). No agent/`opencode.jsonc` change (command surface unchanged). `WORKFLOW.html` (tree, doc-table, §13.12) + `README.md` (same three) updated.
- **Applied to the three apps:** ran `update-framework.sh` on AstroLyfe / AppManager / TrBlazeUI to propagate the framework change; then **relocated the existing split guides** — AstroLyfe (index + 8 area files, `.md`+`.html`) and AppManager (index + 5 area files) moved from `docs/` into `docs/devguides/`; TrBlazeUI has no DevGuide yet. Relative index links were already co-located so they still resolve; external pointers (PROJECT-STATUS DevGuide line) updated to the new path.

### 2026-06-24 — Four owner-reported challenges (Developer Guide, no-HTML-checklists, theme toggle, token efficiency)
Addressed four challenges the owner hit after using the framework for a few days.

1. **No doc lets a HUMAN developer understand/verify the generated code.** Architecture/DB-design/checklists don't show the actual code path for a screen, so developers couldn't chase bugs or catch AI hallucination after the verifier passed. **New screen-by-screen Developer Guide.** New template `.tfcore/templates/v4custom/app-devguide-tmpl.md` + new task `.tfcore/tasks/devguide.md` (registered `*devguide {AppName} [scope] [--update]` on **flow-master**). It documents the code **as built**, per user role, screen by screen: route + Razor file, a **flowchart** of the screen's controls + data flow, a **Controls** table, and the full **Data lineage** (`Razor component → service method → data-access method → stored proc / query`), plus business rules and known issues. Output `docs/{AppName}-DevGuide.md` (+ sibling `.html`); **large apps split per role** into `docs/{AppName}-DevGuide-{Role}.md` + an index (auto when ≥3 roles / >12 screens / any role >8 screens; `--single`/`--split` to force). It **fans out per role** to bound tokens, **verifies paths are real** (flags `{unresolved — TODO}` rather than inventing proc names), and supports **`--update`** (changed-screens-only). **Auto-run at handoff** (new `handoff-phase §3a`); `amend-docs` points code-flow changes at it. It is a human doc → rendered to HTML.

2. **HTML versions of checklists weren't wanted** (checklists are AI-agent docs). **Removed all checklist→HTML rendering** from `day1-greenfield`, `day1-brownfield`, `split-brd`, `handoff-phase`, `refresh-status`, `amend-docs`, `_status-update-gate`, `generate-html`, `render-workflow-docs` — each now states the rule explicitly. Policy added to the HTML shell §0 + render checklist: **only human docs (BRD/Architecture/UsageGuide/DevGuide/PROJECT-STATUS) render to HTML; checklists stay markdown.**

3. **HTML docs were dark-only and the dark wasn't soothing.** **Added a light/dark theme toggle** to the shared shell (`html-render-shell.md`): converted the hardcoded dark palette to themed CSS variables (`:root` = warm off-white light default; `html[data-theme="dark"]` = softened near-black), a flash-free `<head>` init script that picks the **default by time of day** (light 07:00–19:00 local, dark at night) with the user's explicit choice remembered in `localStorage` (`tf-theme`), a floating `#themeToggle` button, and Mermaid's theme following the resolved theme (toggle reloads to re-render diagrams). Fixed the PROJECT-STATUS CTA box in `render-workflow-docs.md` to use theme vars instead of hardcoded hex.

4. **Token utilization.** New **`.tfcore/TOKEN-GUIDE.md`** (for owner + team): where tokens go and the levers — don't load whole docs/repos (load the section the task names), checklists markdown-only, fan out to subagents, incremental updates (`*amend-docs` / `*devguide --update`), recover with `*refresh-status` instead of re-running a build, keep `devLoadAlwaysFiles` lean, watch `/cost`. Ships with every app via the `.tfcore/` rsync. The owner chose "guide + baked-in defaults": #2 (no checklist HTML), the DevGuide's per-role fan-out + `--update`, and the existing "load resources at runtime" principle are the baked-in part.

**Mirrors:** ten tasks (`devguide` [new], `render-workflow-docs`, `generate-html`, `_status-update-gate`, `split-brd`, `refresh-status`, `handoff-phase`, `amend-docs`, `day1-brownfield`, `day1-greenfield`) + `flow-master` were copied byte-identically to `.claude/commands/TechieFlow/` + `.opencode/command/TechieFlow/` (verified by `diff`). `opencode.jsonc` got the `devguide` task entry + the flow-master description update. Templates (`app-devguide-tmpl`, `html-render-shell`) are **not** mirrored (loaded by relative path). `.tfcore/TOKEN-GUIDE.md` is a root-level `.tfcore/` file: scaffold rsyncs all of `.tfcore/` so new apps get it, but `update-framework.sh` only syncs named root files — so it was **added to the `update-framework.sh` top-level `.tfcore/*.md` overwrite loop** (line ~231) + its header comment, then propagated to all three existing apps. **Applied to the three existing apps (2026-06-24):** ran `update-framework.sh` on AstroLyfe (`/mnt/c/1MyCode/AstroLyfe`), AppManager, TrBlazeUI; all picked up the new shell/tasks/template/TOKEN-GUIDE (their `docs/`, `src/`, `core-config.yaml`, `opencode.jsonc` preserved). First AstroLyfe DevGuide generated via the per-role fan-out (8 area files + index, ~93 screens, all HTML-rendered with the theme toggle); the mapping surfaced real code-path issues (no-op Save, missing `[Authorize]` on Admin Document pages, route mismatches, unwired Numerology stats, a 405 GET/POST) — proving the feature's purpose.

**Follow-up (same day) — bugs must reach the checklists + AppManager DevGuide.** The owner asked whether the DevGuide-found bugs were added to the checklists (they weren't — devguide was doc-only). So: (a) **added `devguide §6a`** — confirmed defects found during mapping are now logged into the owning checklist Requirements Status tables (Remarks + `Needs re-verify`, never `Verified`; security items prefixed `⚠ SECURITY`; markdown only), with a matching hard-rule + output-checklist item + the §7 PROJECT-STATUS note carve-out; mirrored + synced into all three apps. (b) **AstroLyfe** findings logged: 11 UI + 9 Functional REQs flipped to `Needs re-verify` with dated DevGuide remarks. (c) **Removed the stale `*-UI-Checklist.html` / `*-Functional-Checklist.html`** from AstroLyfe + AppManager (now banned — checklists are markdown-only). (d) **AppManager DevGuide generated** (index + 5 area files, 51 screens, all HTML) and its defects logged: 12 UI + 16 Functional `Needs re-verify`, with **⚠ SECURITY flags on REQ-NFR-002/FN-010/FN-027/FN-024/FN-001** (SQL injection in Reports RunReport, XSS in Issue description, missing `[Authorize]` on all Settings+Billing+Issues pages, plaintext "encrypted" DB connection string). AppManager turned out to be heavily stubbed (settings/billing/promo/subscription/report-download handlers, emails never sent) — a strong demonstration that the DevGuide catches what the verifier missed. (e) **`update-framework.sh` gap fixed**: new root-level `.tfcore/TOKEN-GUIDE.md` wasn't in the named-root-file sync loop — added it (+ header comment) so it propagates on update (scaffold already rsyncs all of `.tfcore/`).

**Follow-up 2 (same day) — DevGuide hallucination caught in owner testing → LANDING-TRUTH + RENDER-TRUTH hardening.** The owner test-read the AstroLyfe **Admin** DevGuide and found (i) it implied SysAdmin lands on the Admin Dashboard after login, when `LoginPage.razor.cs:57/:84` redirect **every** role to `/Index` (the mapper inferred the landing from the folder name instead of reading the redirect), and (ii) the real landing page `Index.razor` had two live bugs the verifier AND the mapping both missed — the Ruling-Planet **Details** table renders empty (`RpCalcEngine.GetCurrentRps(...)` returns null/empty, `Index.razor.cs:62`) and the Kundali list shows its count but **blank rows** (grid binds an **undeclared `pagination`**, `KundaliList.razor:22`, + `"GetMyClients"`→`UserAstroData` column-mapping / computed `BirthDate`/`BirthTime` getters). Root cause = the mapper verified that *methods exist*, not that *data actually reaches/renders*, and inferred routing. **Framework fix (`devguide.md` §4 + hard rules + output checklist + `app-devguide-tmpl.md` depth mandate):** added **LANDING-TRUTH** (post-login landing / "reached via" MUST be read from the redirect code and cite file:line — never inferred from folder/role) and **RENDER-TRUTH** (verify guards, column→property mapping, throwing computed getters, and undeclared required component params; tag each control renders / renders-empty-suspected-defect / unresolved; log suspected defects via §6a). Mirrored + synced to all three apps. **AstroLyfe doc fixed:** added an accurate "Home (Index.razor)" screen block + landing correction to the Admin guide, a landing note + bug bullet to the index, and logged the two bugs to **REQ-UI-009** (kundali list blank) + **REQ-FN-014** (RP details empty); HTML re-rendered. Lesson recorded because it is the exact failure the framework exists to prevent — confident-but-wrong docs instead of found bugs.

**Follow-up 3 (same day) — owner pushback: the real fix is RUNTIME GROUNDING + a closed loop, not "read harder".** The owner correctly called Follow-up 2 a band-aid: static reading (even with LANDING/RENDER-TRUTH) can't see that a table renders blank or a calc returns empty at runtime — only running the app can, and it must be a loop, not a one-shot. Redesigned the DevGuide into a **3-pass, runtime-grounded, closed verification loop** (owner decisions: reuse the verifier as the runtime engine; strict render gate; land framework first then run live):
- **`devguide.md` §5a OBSERVE + RECONCILE:** after the static MAP, boot the app (build ladder) and **observe every control's actual render** by reusing the verifier's render sweep, then reconcile — render-status becomes an OBSERVED fact; deviations are logged to the checklist as runtime-confirmed defects. If the app can't boot, the guide is stamped **`⚠ STATIC-ONLY`** (never a faked "renders ✓"). Every index + area file carries a **verification-status banner**.
- **`verify-phase.md` §4a render gate + §6b loop-back:** the DevGuide is now the verifier's **per-control test map**. For every in-scope screen the verifier asserts each control actually renders its data (rows present AND non-empty, chart non-empty, value non-blank). **A REQ is `Verified` only if acceptance passes AND all its controls render** (`RENDER-FAIL` → `Needs re-verify`). Crucially, **`Done (pre-existing)` is no longer auto-skipped** — it was the hole the Index bugs fell through (a migrated "done" claim, never runtime-verified); it now gets the render sweep. After verifying, the verifier **writes its runtime observations back into the DevGuide** (§6b), closing the DevGuide ⇄ Checklist ⇄ Verifier loop.
- **`_smoke-test-policy.md`:** added ""it runs" means the CONTROLS RENDER their data, not HTTP 200" — a blank table / count-over-zero-rows is a FAILED smoke.
- **`app-devguide-tmpl.md`:** added the verification-status banner at the top.
- Mirrored (`devguide`, `verify-phase`, `_smoke-test-policy`) + synced to all three apps; existing AstroLyfe + AppManager DevGuide indexes stamped **STATIC-ONLY** pending the live pass (owner will run `*verify all` once the stacks are up). WORKFLOW.html/README updated. **Net:** "verified" now requires the UI to actually render its data, continuously — the band-aid is replaced with the stitch.

**Follow-up 4 (same day) — FIRST LIVE RUN of the loop validated it.** Ran the runtime render-gate pass against a live AstroLyfe (owner had the stack up). Learnings: (1) the loop **works and immediately paid off** — logging in as `astrologer1@astrolyfe.com` and observing the Home page caught the Ruling-Planet **Details table rendering EMPTY** at runtime (chart renders on the same data; details card had only its header, 0 rows) — a real defect the OLD verifier would never have caught because that screen was `Done (pre-existing)` and got skipped. Written back to the checklist (REQ-FN-014 runtime-confirmed) + the DevGuide Home block (observed render-status) + a PARTIALLY-RUNTIME-VERIFIED banner. (2) Practical: "stack up" was incomplete — only **AppManager** (:5100/:5101) + Postgres + LM Studio were running; **AstroAPI was down**, so I booted AstroAPI (:6392, its config already targets the live AppManager/DB/LLM) + AstroBlazeWeb (:5099) myself; `AstroBlazeWeb/Program.cs` accepts the dev cert in Development, so WSL TLS was a non-issue. (3) The **UsageGuide test creds were partly wrong** vs the running AppManager (`admin@astrolyfe.com` fails "incorrect"; `astrologer1` works) — reconciled that table; the "16-charts-but-blank" kundali bug (REQ-UI-009) stays **runtime-pending** because no data-bearing account could log in. AstroLyfe's PROJECT-STATUS got a "▶ RESUME HERE" note with the exact stack bring-up recipe + open bugs for the owner's next (separate) session. **All three apps verified byte-identical to the canonical devguide/verify-phase/_smoke-test-policy/app-devguide-tmpl (this session's framework changes are fully propagated).** No further framework edits pending. `WORKFLOW.html` + `README.md` updated (§5 tree, §6 audiences, §7 handoff, §13.6 HTML rendering, new §13.12 DevGuide schema, §14 cheat sheet, §15 quick-ref, §16 FAQ, footer date). **Existing apps pick this up via `update-framework.sh`; restart any open session to load refreshed defs.**

### 2026-06-18 — Four owner-reported issues (persona rename, mandatory smoke-testing, test-user discipline, day-1 UsageGuide)
Fixed four issues the owner hit using the framework.

1. **Flow-master persona renamed `Krishn` → `Madhav`.** Updated the canonical `.tfcore/agents/flow-master.md` (name/identity/help-display/tip) + both harness mirrors (byte-identical), `opencode.jsonc` description, `README.md` + `WORKFLOW.html` current-state rows, and this doc's §3 roster + §5-2026-06-13 persona list. Memory (`techieflow-rebrand.md`, `MEMORY.md`) updated. **Historical log entries that named "Krishn" at the time of a past action (§5 lines for the 2026-06-13 rebrand) were left as-is** — they record what was true then; only current-state references were changed.

2. **Agents kept excusing themselves from smoke-testing** ("can't run on Linux / it targets Windows / it's MAUI / Playwright needs a GUI"), dumping the smoke on the owner — even though the one-time setup (`WORKFLOW.html §0`) already installed headless Playwright + the Windows/MAUI bridge. New shared rule **`.tfcore/tasks/_smoke-test-policy.md`** (mirrored, like `_status-update-gate.md`): smoke-testing every change before the verifier is mandatory and **the agent runs it itself**; those five phrases are now a **banned-excuse list** (same treatment the build ladder gives wrong-rung errors); escalating to the user is the LAST resort (only after rung #4 + `verify-phase §3a` genuinely fail), never the first. Wired a reference into the self-smoke sections of `build-functional-phase §6a`, `build-ui-phase §4a`, `build-rag-phase §5a`, and `verify-phase §4`, plus an Output-Checklist box in each, and added it to the `flow-master` + `verifier` dependency lists. Two new pain-point cards in `WORKFLOW.html §2`.

3. **Smoke/verify runs polluted the DB with random throwaway test users.** Same `_smoke-test-policy.md` defines the **test-user discipline**: resolve credentials in order — (1) the `docs/{AppName}-UsageGuide.md` Test-users table, (2) look them up in the **database** via the connection string in `appsettings*.json`, (3) if neither yields an account, **STOP and ask the owner** to provide/confirm before creating any; (4) only after confirmation, create + **record the account back in the UsageGuide**. NEVER invent throwaway users. `verify-phase §4` carries the same rule and threads chosen creds into its test subagents.

4. **No day-1 "uses" document.** Folded the old handoff-only `app-usage-testing-tmpl.md` into a richer **`app-usageguide-tmpl.md`** → output `docs/{AppName}-UsageGuide.md`: a **Test-users table at the top** (username/password/role/`Created?`) + a **screen-by-screen / menu-by-menu test plan** + setup/test/smoke/known-limitations. It is now **always created at day-1** (new `day1-greenfield §7.4` / `day1-brownfield §7.4`, rendered to HTML in each §7.5, in the §8 summary + Output Checklists) and **finalized (not recreated) at handoff** (`handoff-phase §1` reconciles the table with the live DB, §3 re-renders it). Renamed every reference from the old `<APP>-Usage-Testing-Setup.md` across `README.md` + `WORKFLOW.html` (incl. the §13.10 schema) and the `flow-master`/opencode handoff descriptions. The old template was deleted (templates aren't mirrored — they load by relative path).

**Follow-ups the owner asked for after reviewing the first AstroLyfe UsageGuide:**

5. **Flowcharts for complex flows.** Added a "Flowchart any complex flow" callout to `app-usageguide-tmpl.md` (Mermaid for multi-step/multi-actor/branching flows, per the §5.5 authoring rules) — the new template was the only doc template with no diagram mandate. Demonstrated it on the AstroLyfe UsageGuide with 4 diagrams (auth/token `sequenceDiagram`; AI predict→validate→confidence-recalc loop; ingestion→embedding→rollback; onboarding→approval), re-rendered to HTML (4 `.diagram` toolbars, labels quoted, no `end` ids, copy-button selector `pre:not(.mermaid)`).

6. **`*amend-docs` — incremental day-1-doc updates.** The framework had no documented way to evolve the day-1 docs as a project changes (only additive `author-brd`, or wholesale re-run of `day1-*` which archives + regenerates). New task **`.tfcore/tasks/amend-docs.md`**: surgically amends BRD + Architecture **in place** from a described change — append-only `BRD-N` (modified edited in place, removed struck through, never renumbered), updates §4/§9/§10, ripples new/changed REQs into the checklists (appends rows / flags modified for re-verify — never blindly re-splits), re-renders HTML, runs the status gate; one confirmation gate then bulk-apply; never archives to OldDocs (that's day-1's job). Registered `*amend-docs {AppName} {change}` on **analyst** (owner) **and flow-master** (hub), added to both dependency lists + `opencode.jsonc`. Documented as new **WORKFLOW.html §7.9 "Evolving the day-1 docs"** (+ §15 quick-ref row + updated the architecture-update FAQ) and the **README §7.9** mirror.

**Mirrors:** the three agents (`flow-master`, `verifier`, `analyst`) and nine tasks (`_smoke-test-policy` [new], `amend-docs` [new], `build-functional-phase`, `build-ui-phase`, `build-rag-phase`, `verify-phase`, `day1-brownfield`, `day1-greenfield`, `handoff-phase`) were copied byte-identically to `.claude/commands/TechieFlow/` + `.opencode/command/TechieFlow/` (verified by `diff`). Templates + root docs not mirrored. The `_smoke-test-policy` include needs no `opencode.jsonc` entry (loaded by path, like `_status-update-gate.md`); `amend-docs` got one (it's a user-invokable command). **Restart any open Claude Code / OpenCode session against an app to pick up refreshed defs; existing apps get these via `update-framework.sh`.**

### 2026-06-17 — Mermaid diagrams STILL erroring — real root cause found (the 2026-06-14 fix was a misdiagnosis)
The owner reported that AstroLyfe's regenerated Architecture + BRD HTML **still** show the bomb-icon **"Syntax error in text — mermaid version 11.15.0"** on flowcharts, despite the 2026-06-14 "Mermaid authoring rules" work. Investigated empirically (headless Chrome + the `mermaid@11.15.0` parser, the exact CDN version the shell loads) instead of by inspection. Findings:
- **Every diagram parses and renders fine in isolation** — quoted *and* unquoted labels, with literal `<br/>`. The 2026-06-14 belief that **unquoted special characters** broke flowcharts was **wrong**: I never reproduced the failure before declaring it fixed. Unquoted labels are not the cause.
- **Actual root cause: a bug in the HTML shell's own JavaScript.** The "copy buttons on every `<pre>`" loop ran `document.querySelectorAll('pre')` and `appendChild`ed a `<button>copy</button>` into **`pre.mermaid` too** — *before* `mermaid.initialize`. Mermaid reads the `<pre>`'s `textContent` as the diagram source, so `copy` got glued onto the diagram's last line. On a **flowchart** that corrupts a node/edge id → "Syntax error". On a **sequence diagram** the trailing `copy` lands in freeform message text → still valid. **That** is why "only flowcharts broke, sequence diagrams were fine" — the signature the 2026-06-14 entry misattributed to label quoting. Reproduced the exact error and the fix in a Chrome A/B (`pre` vs `pre:not(.mermaid)`).
- **Fix:** scoped the selector to **`document.querySelectorAll('pre:not(.mermaid)')`** in `html-render-shell.md` §7 (+ explanatory comment, + the §1 feature bullet). Mermaid blocks already get copy/export from their own `.diagram` toolbar. Template only — **not mirrored** (templates load by relative path; harness mirrors cover agents/tasks only).
- **Patched the already-generated AstroLyfe HTML in place** (the owner needs them working now, not after a re-render): `AstroLyfe-Architecture.html`, `AstroLyfe-BRD.html`, `AstroLyfe-Functional-Checklist.html`, `AstroLyfe-UI-Checklist.html` — same one-line selector change. Verified by re-rendering in headless Chrome: **0 "Syntax error", 8/8 Architecture + 7/7 BRD diagrams render as real SVGs.**
- **The 2026-06-14 §5.5 authoring rules + quoted example diagrams are kept** — they're harmless good practice and defensive, just not the thing that was breaking renders.
- **Recurrence prevention (each app carries its OWN `.tfcore` copy of the shell).** Fixing only the framework template would let a re-render reintroduce the bug from an app's stale local copy. New apps inherit the fixed shell at scaffold time; existing apps stay in sync via `update-framework.sh` (it rsyncs `templates/`). AppManager/TrBlazeUI had no mermaid-bearing *generated* HTML yet (only `WORKFLOW.html`, 0 diagrams), so no output needed patching there — only their shell copy, to protect the next render.
- **Propagated to existing apps with `update-framework.sh` (the proper sync, not a hand-patch).** Ran it on **AstroLyfe → AppManager → TrBlazeUI**; all three now have the shell **byte-identical to the fixed framework** (verified `diff` + 0 buggy selectors). The runs preserved `docs/`/`src/`/`core-config.yaml` (so AstroLyfe's already-patched, verified HTML stayed intact) and reported `settings.json` already-current on all three. NOTE the script's own reminder: **restart any open Claude Code / OpenCode session** against an updated app so the refreshed task/agent defs load.
- **Cleaned up a pre-existing, unrelated stale-layout flag the updater surfaced on TrBlazeUI.** `update-framework.sh` flags (never edits) per-project files still referencing the old `.bmad-core`/`BMad:` layout. In TrBlazeUI that was `.gitignore` (`.bmad-core` → `.tfcore`) and `docs/TrBlazeUI-Update-plan.md` (paths `.bmad-core/…`→`.tfcore/…`, namespace `BMad:`→`TechieFlow:`, brand "BMAD"→"TechieFlow", orchestrator→`flow-master (Krishn)`). Two refs pointed at structures the rebrand **removed**, so they were corrected rather than blindly reprefixed: the `dev.md` example agent → `architect.md` (dev was trimmed), and the `agent-teams/team-*.yaml` registration mechanism → the real TechieFlow surface (mirror the agent into `.claude/commands/TechieFlow/agents/` + `.opencode/command/TechieFlow/agents/`, key it in `opencode.jsonc`). These are TrBlazeUI's own work-product docs — not framework files, nothing to mirror. Re-running the updater dry-run confirms the stale-layout warning is gone.

### 2026-06-14 — Three owner-reported issues (Mermaid, BRD dev-status, cross-platform)
Fixed three issues the owner hit while using the framework on AppManager.

1. **Broken Mermaid diagrams in rendered BRD/Architecture HTML** (context/component/deployment flowcharts threw "Syntax error in text — mermaid v11.x"; sequence diagrams were fine). Root cause: **unquoted special characters in flowchart node labels** (`/`, `()`, `&`, `:`, …) — flowcharts carry real app text, sequence diagrams don't, which is why only flowcharts broke. The framework had **no Mermaid authoring rules** and the template example diagrams themselves used the unquoted style agents then copied. Fix:
   - New **§5.5 "Mermaid authoring rules"** in `html-render-shell.md` (quote every node/edge/subgraph label, quotes inside shape brackets, no `end` node id, `<br/>` for breaks, one statement per line, self-check). Added a matching item to the shell §9 render checklist.
   - **Rewrote every example diagram** in `app-brd-tmpl.md` (§6/§8/§9) and `app-architecture-tmpl.md` (§2/§6/§8) to the quoted style, and added a "Mermaid mandate" callout to both templates' depth-mandate headers.
   - Added a **Mermaid validity pass** to `generate-html.md` §3 + `render-workflow-docs.md` §6 (and both checklists): the renderer scans each diagram and quotes any bare label rather than emitting a diagram it knows will error.
   - Added the authoring-rule pointer to the day1-brownfield/greenfield "Mermaid mandate" bullets.
2. **BRD had no "what's built vs pending" view.** Added a new **§4 Development status** section to `app-brd-tmpl.md` (after §3 Scope, per owner's stated placement preference), renumbering old §4–§14 → §5–§15 and updating the TOC. It's a feature-level **snapshot** table (one row per §9 feature-catalog F-code: Phase / Status / % / Notes) explicitly labeled NOT a competing source of truth — live per-REQ status stays in PROJECT-STATUS + the checklist Requirements Status tables. day1-brownfield populates it from the migrated dev-plan / code scan; day1-greenfield emits all-`Planned` (build roadmap). Updated all BRD section cross-refs in day1-brownfield, day1-greenfield, and author-brd (ledger §9→§10, feature catalog §8→§9, NFR §10→§11, diagrams §5/6/7→§6/7/8).
   - **Follow-up (owner Q): keep §4 current after day-1, both automatically and on-demand.** The table is a *derived* rollup of the checklists, refreshed at two kinds of touch point: (a) **live** — the **status gate** (`_status-update-gate.md` item 9, run at the end of every build/verify/handoff phase) re-derives the feature-level rows from the checklists and re-renders `BRD.html`; (b) **on-demand** — **`*refresh-status`** does the same as part of recovery (new §4.5), and is the ONE place allowed to **insert** the §4 section into a **legacy BRD** that predates it (e.g. AstroLyfe) — it adds the `## 4. Development status` H2 after Scope, renumbers following H2s +1, regenerates the TOC, then fills the table. Rollup rule (shared, defined in gate item 9): feature → its `Requirements: BRD-…` line → the `REQ-*` those split into → per-REQ Status; all-terminal → `Done`, mixed → `Partial`, started-but-unverified → `In progress`, none → `Planned`. Build phases that hit a legacy BRD (no §4) do NOT do section surgery — they tell the user to run `*refresh-status` once. refresh-status's "never touch the BRD" guardrail was narrowed to allow ONLY the §4 table. Mirrored `_status-update-gate.md`, `refresh-status.md`, `handoff-phase.md`.
3. **Everything was WSL-on-Windows-coded; owner asked how to run on Mac / native Windows.** The framework is portable (Markdown agents/tasks/commands, bash scripts) — only `dotnet` invocation + the Playwright/MAUI bridge are environment-specific. Fix:
   - **Generalized `build-invocation-ladder.md`** with a §0 platform-detection probe (`uname` → macOS / WSL / native Linux / native Windows) and split it into **§A macOS/Linux** (dotnet in PATH, native MAUI via Xcode/Android SDK), **§B WSL** (the existing interop ladder, now scoped), and **§C native Windows** (dotnet in PATH, native MAUI, iOS needs a paired Mac). The old hardcoded WSL paths/`cmd.exe`/`winrun` are now clearly one platform's ladder, not the universal one.
   - New **WORKFLOW.html §17** and **README.md §17** ("Running on macOS / native Windows / Linux") — self-contained, with a per-platform table and macOS / native-Windows / native-Linux quick-starts. (A standalone `PLATFORM-GUIDE.md` was drafted then removed per owner preference — the content lives in the workflow doc + README only, not a separate file.)
   - Scaffold scripts: portable `rsync`-missing message (Linux/macOS/Windows; **removed backticks that would have run `sudo apt-get` via command substitution** — a latent bug).
   - **Mirrored** the five edited tasks (day1-brownfield, day1-greenfield, generate-html, render-workflow-docs, author-brd) byte-identically to `.claude/` + `.opencode/`. Templates and root docs are not mirrored (loaded by relative path / shipped by scaffold).

### 2026-06-14 — `*refresh-status` de-git-ed (design bug fix)
`refresh-status` was the **only** agent task that actively invoked git (`git rev-parse`/`log`/`status`/`diff` in its step-1 evidence gathering). That contradicts a core framework rule: **git is manual, agents never touch it** — the shipped CLAUDE.md gates `git`/`gh`/`sudo` behind an *ask*. In practice the owner (correctly) denied the permission prompt, which knocked out an evidence leg the task depended on. Root cause: the task was authored on the generic-git assumption that "committed vs uncommitted" is the strongest interruption signal — untrue here, where nothing is ever committed by an agent. Fix (owner decision: **remove git entirely**, no opt-in flag):
- Rewrote `.tfcore/tasks/refresh-status.md` evidence model to **checklist Requirements Status tables + working-tree files (presence + mtimes vs PROJECT-STATUS `last_updated`) + fresh `dotnet build`**. The "committed-but-unrecorded" signature became **"worked-but-unrecorded"** (files newer than `last_updated`, or files present for still-open REQs); "in-flight uncommitted" became **"in-flight/partial"** (partial files / build errors). Added an explicit "No git" callout + a guardrail forbidding `git`/`gh`.
- Mirrored byte-identically to `.claude/commands/TechieFlow/tasks/` and `.opencode/command/TechieFlow/tasks/`.
- Updated the flow-master `refresh-status` command description (+ both mirrors), `WORKFLOW.html` §8a recovery paragraph and §15 quick-ref row, and this doc's §4.
- **Left untouched (correct as-is):** `WORKFLOW.html` §8b "clean cold resume" step 2 (`git log && git status`) and the §15 "resume cold project" row — those are **human** manual steps, fully consistent with git-is-manual. The `git clone` lines in `handoff-phase.md` / `app-usage-testing-tmpl.md` are human-facing deployment-doc instructions, not agent actions.

### 2026-06-13 — Full rebrand to **TechieFlow**
The framework was fully re-branded from its prior third-party origin to **TechieFlow**, the owner's own customized framework. Goal: the shipping surface carries the TechieFlow identity only — every agent, task, template, command, path, script, and doc. (The previous `docs/OldDocs/` archive of original stock files was removed entirely.) What changed:
- **Core folder** renamed to `.tfcore/` (all in-file path strings updated too). The knowledge-base data file is now `techieflow-kb.md`; the doc-template util is `techieflow-doc-template.md`.
- **Command namespace** is now `TechieFlow` — harness mirrors at `.claude/commands/TechieFlow/` & `.opencode/command/TechieFlow/`, `slashPrefix: TechieFlow`, so commands are `/TechieFlow:agents:<name>` and `/TechieFlow:tasks:<task>`. OpenCode keys: agents `flow-*`, commands `techieflow:tasks:*`.
- **Super-agent merge:** the two former super-agents (a task-executor + an orchestrator) collapsed into a single hand-authored **`flow-master.md` (Krishn)** — union of both command sets + dependencies, keeps the BRD-coverage protocol, and gains `*run-workflow {AppName}` to drive the whole pipeline (day-1 → split-brd → build → verify → handoff) across parallel subagents.
- **Indian personas:** Architect **Ravi**, Analyst **Chanakya**, Verifier **Vidur**, Flow-Master **Krishn** (renamed to **Madhav** on 2026-06-18 — see the §5 log entry of that date). Agent IDs unchanged except the merge.
- **Markers/prose:** the old "Powered by …" marker header removed from every file; all prior-brand prose → `TechieFlow`; the template path → `/mnt/c/3AIGenCode/TechieFlow`; the scaffold note → `.tf-scaffold-note.txt`. Dormant stock guide docs' broken installer/community links (a side effect of the rename) were re-pointed to the scaffold scripts + WORKFLOW.html.
- **Scripts:** all three rebranded; `update-framework.sh` gained a **one-time migration** that converts a project still on the legacy layout to `.tfcore/` (rename core, patch core-config slashPrefix/paths, delete the old mirror dirs, rename the scaffold note, back up & replace a stale `opencode.jsonc`) before the normal refresh — so old apps upgrade cleanly and the old folders are deleted. (The migration's detection strings necessarily name the legacy paths — the only place the old layout is referenced, and unavoidably so.)
- **Docs:** `WORKFLOW.html` scrubbed + title → "TechieFlow: Solo-Dev Delivery Workflow", §14 roster now six agents with one merged flow-master row; **new `README.md`** (markdown of WORKFLOW.html) for repo browsers; the two `docs/*-Feedback.md` files renamed to the `TechieFlow-` prefix.
- Harness mirrors regenerated byte-identical; the only prior-brand strings left in the whole repo are the legacy-path detectors in `update-framework.sh`'s migration block (functionally required — they name the old folders so the script can find and delete them).

### 2026-06-13 — Two gaps the owner hit
1. **AI-agent context doc** — created **this file** (`WorkFlow-Context.md`) by evolving the old `WorkFlow-Issues.MD` into a durable orientation + living maintenance log, so any agent can get current context from one file instead of crawling the whole framework. Renamed; references in the two `docs/TechieFlow-*-Feedback.md` files updated.
2. **`*refresh-status` recovery command** — new task `.tfcore/tasks/refresh-status.md`, registered on **flow-master** (`*refresh-status {AppName} [verify]`), mirrored to `.claude/` + `.opencode/`, added to `opencode.jsonc`, and documented in `WORKFLOW.html` (§8a recovery branch, §15 quick-ref row). Closes the "session broke mid-phase and I don't know the state" gap.

### 2026-06-12 — Full framework audit (37 issues, all resolved)
A complete audit against `WORKFLOW.html`, `.tfcore/`, the harness mirrors, the three shell scripts, and sample docs fixed **37 issues** (I-01…I-37) plus follow-throughs I-38/I-39. Headlines:
- **Verifier chain** (I-01…I-04): `verify-phase` rebuilt around named scopes (`ui`/`functional`/`all`/REQ-list) instead of the obsolete BRD-phase model; build tasks now state the chain mechanism explicitly.
- **Status conventions** (I-05…I-11): `Done (pre-existing)` status added to legends; standardized on `Blocked`; HTML re-render folded into the status gate; gate-minimum fields enforced everywhere.
- **Library agents** (I-12…I-14): killed the stale legacy trblazeui copies; NuGet personas shimmed into `.claude/commands/`; rsync excludes fixed so mirrors self-heal.
- **Scripts** (I-15…I-22): removed banned `docs/qa/`; corrected idempotence claims; `settings.json` documented as force-refreshed; assorted path/dry-run fixes.
- **WORKFLOW.html** (I-23…I-25): replaced the old checklist schema with the real Requirements Status table; purged all dated-qa references; documented field-prefix as a per-project decision.
- **Vanilla remnants** (I-26…I-31): fixed `core-config` stale paths; `author-brd` made `{AppName}`-aware; pruned undefined orchestrator commands; tidied dependency lists.
- **Policy/cosmetics** (I-32…I-37): archived `-v2`/issuedocs to `OldDocs`; normalized `TR-NNN`; corrected templates + session memory.
- **I-38 stock trim:** removed unused stock agents (`dev/pm/po/qa/sm/ux-expert`), 14 story-flow tasks, 5 stock templates/checklists, `workflows/` + `agent-teams/` (originally archived; the archive was removed entirely during the 2026-06-13 rebrand).
- **I-39 per-library feedback:** replaced the combined feedback file with one file per library.

Full per-issue detail is preserved in [§8 Audit appendix](#8-audit-appendix-2026-06-12-issue-register).

## 6. Known open items

| Item | Status |
|------|--------|
| **Library-side NuGet deploy path** (root cause of I-13) — ✅ resolved in both libraries: their `.targets` now deploy straight to `.claude/commands/<lib>.md` (verified 2026-07-04; TR-001/TR-RAG-001 closed). The framework shim remains as a harmless no-op. | Resolved |
| **Library package republish pending** — the persona-source fixes of 2026-07-04 (BMAD header removed; trblazeui `.bmad-core`→`.tfcore`; see TR-002/TR-RAG-002) live in the TrBlazeUI/TechieRag repos but reach consumers only when the owner **repacks + republishes** both NuGet packages; until then every consumer `dotnet build` re-stamps the old persona over `.claude/commands/`. | Open (owner action) |
| **Windows app repos not yet carrying the 2026-08-11 telemetry/line-ending fixes** — the reconciling `post-commit` hook and the `.gitattributes` LF+union block reach a repo only through `update-framework.sh`, and the hook lives in `.git/hooks/`, so it must be run **once per repo per machine**. The four Mac clones were refreshed + backfilled on 2026-08-11; the nine repos under `/mnt/c/1MyCode` and `/mnt/c/3AIGenCode` were unreachable from that session and are the owner's to refresh there. Follow each with one `git add --renormalize . && git commit`. | Open (owner action) |
| **`TechieRag` hosts two products in one repo** (the packable `src/TechieRag*` libraries and `apps/TechieDesk`, a head with screens), so it carries two checklists. Telemetry is per-repo, single-valued: `project_type` is `app` (defensible — TechieDesk can fail the visual gate) and `tf-emit.sh` stamps every record `app:"TechieRag"` because the one-checklist inference cannot resolve two. Not a defect to patch blind — either split the repo or teach telemetry about multi-product repos, owner's call. | Open (owner decision) |
| **Miss telemetry not yet consumed downstream** — the framework emits `docs/metrics/misses.jsonl` as of 2026-08-28, but neither reader is built: **TfLens** needs the fifth stream (two record kinds on one file, two new tables, the attribution/cost exclusions, a `/misses` page, and a `tf-metrics.sh --rollup` counterpart before its figures are quotable under BRD §13 parity), and the **AI-First-Playbook** needs its own OpenCode-only version. Both are fully specified in `docs/Miss-Telemetry-TfLens.md` and `docs/Miss-Telemetry-AI-First-Playbook.md`. No coordination window is needed — TfLens ignores stream files it does not know. | Open (separate repos) |
| **Apps not yet carrying the miss stream** — `misses.jsonl` reaches an app only through `update-framework.sh <repo>`, which seeds it and refreshes the tasks that write it. **TfLens refreshed 2026-08-28**; the rest are pending, and each must also be run **once per machine** (the commit hook lives in `.git/`, which is not part of the repository). | Open (owner action) |
| **Greenfield-born `docs` classifications elsewhere** — the 2026-08-28 upgrade path corrects a `docs` label on the next `update-framework.sh`, but only for repos that actually get refreshed. A survey on 2026-08-28 found TfLens the only casualty of 20; re-run the survey after the rollout rather than assuming. Records written before a correction keep the old value by design — `--report` names the split. | Open (owner action) |
| **Stale YOLO flags** — the 2026-08-28 (6) scan found live `.tfcore/.session/yolo.json` flags in five repos (this one, `AppManager`, `TechieBlog`, `TrBlazeUI`, `TfLens`). All five cleared that session, and **no repo carries one now**. `TfLens` was held back initially in case a run was live, then cleared at the owner's instruction once its `goal-done.json` showed the handoff had completed at 14:51 with no activity since 15:08. All 19 repos now carry the 24 h expiry, so a forgotten flag stops granting on its own. | Resolved |
| **Two owner-owned docs still name the pre-2026 framework layout** — `update-framework.sh` reports them and deliberately never edits them: `AI-First-Development-Playbook.html` + `AI-First-Development-Team-Guide.html` in `AI-First-Playbook-Source-PRIVATE`, and `docs/TechieFlow-TrBlazeUI-Feedback.md` in `TrBlazeUI`. All still reference `.bmad-core` / `/BMad:` / `bmad-master`. Fix the pointers or regenerate (`*render-workflow-docs`). Pre-existing, surfaced by the 2026-08-28 (6) propagation. | Open (owner action) |
| **The two new gates were propagated to all 19 repos on 2026-08-31** — `assets` (§4a2) and `mockup-parity` (§4b2), with their scripts, schema enum values and task sections. Presence verified by content in every repo, not by mtime. **Still once per machine**, though: a second clone of any of these repos has the framework files (they are gitignored, so they travel by refresh, not by pull) — run `update-framework.sh <repo>` there too. | Resolved (re-run per machine) |
| **`TrSetup` has 4,635 tracked build-output files** — 2,957 `bin/`, 1,678 `obj/`, across four projects; found by the 2026-08-31 propagation. Its ignore rules are now correct **and completely inert until the index entries go**, because a tracked file is never ignored. These files carry the static-web-assets manifest with machine-absolute content roots, which is a plausible route to the asset 404 of TF-007. `bash .tfcore/utils/tf-gitignore-audit.sh /mnt/c/3AIGenCode/TrSetup` prints the exact per-directory un-tracking commands. **Owner action — agents never run version control.** | Open (owner action) |
| **`mockup-parity` needs mockups to grade against, and needs them anchored.** It compares elements both sides carry a `data-testid` for. A repo with no `docs/mockups/` gets `NO-MOCKUP` on every screen — reported, never a silent pass. A repo whose mockups are thinly anchored gets `UNGRADEABLE`, which is also correct and also not a pass. Each screen prints `anchor_deficit.add_data_testid_to_mockup`, so closing the gap is a mechanical edit to the mockup rather than a research task — but it is per-project work nobody has done yet. | Open (per project) |
| **Fan-out telemetry starts empty and will look thin for weeks.** `subagent_runs` / `tokens_out_subagents` / `model_tokens_out` (SCHEMA §2.6) exist only on records written after 2026-08-31, under a harness whose window resolves to `tree` scope. `--phases` reports the exclusion split honestly (*not `tree` scope* vs *predates the field*) rather than showing zeros, so this needs patience, not action. | Open (time only) |
| **TfLens has fixes to pick up, not just entries to close.** Beyond re-verifying the seven, three items are live work there: retire `DECISIONS.md` **D-012** (the TF-005 divergence has no reason left to exist now the reference publishes `_n`), drop the TF-010 post-render patch, and check `MissFigures` for the two defects the framework found in itself — an unvalidated miss enum, and a `cost_attribution` recount short-circuiting on a stored `sole` in the **headline** column. All three are spelled out in `docs/TfLens-TechieFlow-Feedback.md`'s 2026-08-31 block. | Open (separate repo) |
| Everything from the 2026-06-12 audit | Resolved |

No other known framework defects are open as of 2026-08-31.

## 7. Maintenance contract

Keep this framework coherent — when you change it, update **all** of these together:

1. **Harness-mirror parity is mandatory — Claude Code only.** Any edit to a `.tfcore/agents/*.md` or `.tfcore/tasks/*.md` MUST be copied byte-identically to `.claude/commands/TechieFlow/{agents,tasks}/`. Claude Code registers the mirror, not `.tfcore/`. (Verify with `diff`.) **Never create `.opencode/command/TechieFlow/`** — OpenCode loads agents/tasks from `opencode.jsonc` `{file:./.tfcore/...}` references; files under `.opencode/command/TechieFlow/` only register as undescribed phantom slash-commands (mirror removed 2026-08-13 — see `docs/TechieFlow-OpenCode-Gaps.md` GAP 1/2).
2. **New task → four wirings:** create under `.tfcore/tasks/`, mirror to the Claude harness dir, register the command on its owning agent (and mirror that agent edit), and add it to `opencode.jsonc`.
3. **Document it** in `WORKFLOW.html` (the relevant section + the §15 quick-ref) and bump its "Last revised" date.
4. **Log it here** — add a dated entry to [§5 Maintenance log](#5-maintenance-log-what-has-been-done-newest-first), update [§6 Known open items](#6-known-open-items) if the change opens/closes one, and refresh "Last updated" at the top.
5. **Session memory** is keyed on the repo's path, so it does **not** follow a move. Today that is `~/.claude/projects/-Users-MyCode-TechieFlow/memory/` on the Mac; the earlier `-Volumes-MacD-MyCode-TechieFlow` and `-mnt-c-3AIGenCode-TechieFlow` directories did not migrate. Update `MEMORY.md` + the relevant memory file when a durable convention changes — and treat **this file** as the durable record, since it is the one that survives every path change.
6. **Never** introduce banned patterns: dated `docs/qa/*` files, `-v2` doc suffixes, a combined library-feedback file, or silently working around a library gap instead of logging it.

---

## 8. Audit appendix (2026-06-12 issue register)

Preserved verbatim for traceability. All items resolved unless noted.

| ID | Area | Sev | Issue (short) | Status |
|----|------|-----|---------------|--------|
| I-01 | Verifier chain | HIGH | verify-phase sourced its work list from BRD "phases", incompatible with post-split checklists | Fixed |
| I-02 | Verifier chain | MED | build-rag told user to run `*verify-phase` — not a registered command | Fixed |
| I-03 | Verifier chain | MED | build tasks said "chain into verifier" but never said HOW | Fixed |
| I-04 | Verifier chain | LOW | verify-phase had no `{AppName}` input though §6 needs it | Fixed |
| I-05 | Status conventions | HIGH | `Done (pre-existing)` status missing from legends; build-task mode detection would REBUILD migrated work | Fixed |
| I-06 | Status conventions | MED | `BLOCKED-BY-LIBRARY` consumed but no task produced it | Fixed |
| I-07 | Status conventions | MED | Checklist + PROJECT-STATUS HTMLs went stale — no phase re-rendered them | Fixed |
| I-08 | Status conventions | MED | split-brd/handoff updated PROJECT-STATUS without citing the gate; missed minimum fields | Fixed |
| I-09 | Status conventions | LOW | handoff set a `current_phase` not in the template enum | Fixed |
| I-10 | Status conventions | LOW | render-workflow-docs rendered a frontmatter field nothing writes | Fixed |
| I-11 | Status conventions | LOW | split-brd unconditionally set next phase to UI build | Fixed |
| I-12 | Library agents | HIGH | Stale legacy trblazeui agent pointing at a nonexistent KB; the harness registered it | Fixed |
| I-13 | Library agents | HIGH | NuGet deploys personas to `.claude/<name>.md` — Claude Code only scans `.claude/commands/` | Fixed via script shim — library-side fix open (see §6) |
| I-14 | Library agents | HIGH | `rsync --exclude` matched basename at any depth — stranded the canonical file, mirrors couldn't self-heal | Fixed |
| I-15 | Scripts | MED | Both scaffolders still created `docs/qa/` | Fixed |
| I-16 | Scripts | MED | Idempotence claims wrong | Fixed |
| I-17 | Scripts/doc | HIGH | WORKFLOW §3 listed `settings.json` as preserved; the script refreshes it by default | Fixed |
| I-18 | Scripts | LOW | `--dry-run` still ran `mkdir -p` | Fixed |
| I-19 | Scripts | LOW | Top-level command sync was a hardcoded one-item list | Fixed |
| I-20 | Scripts | LOW | Dry-run agent preview sourced the wrong tree | Fixed |
| I-21 | Scripts | LOW | scaffold-greenfield ran `realpath` before `mkdir -p` | Fixed |
| I-22 | Scripts | LOW | Misc guards (template subdir, brownfield pause, comment accuracy) | Fixed |
| I-23 | WORKFLOW.html | HIGH | §13.7/§13.8 showed the OLD checklist schema | Fixed |
| I-24 | WORKFLOW.html | HIGH | 7+ references to banned dated `docs/qa/*` files | Fixed |
| I-25 | Standards | MED | `obj` field prefix hardcoded as universal (it's per-project) | Fixed |
| I-26 | Vanilla remnants | MED | core-config stale stock paths (so `/dev` loaded no standards) | Fixed |
| I-27 | Vanilla remnants | MED | author-brd created `docs/brd.md` with wrong name+shape | Fixed |
| I-28 | Vanilla remnants | MED | apply-qa-fixes was a stock Deno/TS task | Fixed |
| I-29 | Vanilla remnants | MED | orchestrator `*help` advertised undefined commands | Fixed |
| I-30 | Vanilla remnants | MED | Stock agents unaware of custom conventions | Mitigated (day1 writes concrete devLoadAlwaysFiles paths) |
| I-31 | Vanilla remnants | LOW | Dependency-hygiene mismatches | Fixed |
| I-32 | Policy/hygiene | MED | `issuedocs/AstroLyfe-BRD-v2.md` (banned `-v2`) | Fixed (archived to OldDocs; empty dir deleted) |
| I-33 | Policy/hygiene | HIGH | day1-greenfield §3.5 contradicted day1-brownfield on superseded-plan handling | Fixed |
| I-34 | Policy/hygiene | LOW | generate-html presented `-v2` BRDs as normal | Fixed |
| I-35 | Cosmetics | LOW | Duplicate bullet, TR-N format, stale wording/date | Fixed |
| I-36 | Templates | LOW | claude-md Permissions section described old allowlist | Fixed |
| I-37 | Memory | LOW | Session memory cited `docs/library-feedback.md` | Fixed |

**Q&A decisions (2026-06-12):** Q1 — do not modify the libraries; log the NuGet-path fix to per-library feedback files (→ TR-001 / TR-RAG-001). Q2 — the two Coding-Standards docs are reference SAMPLES (header comment added). Q3 — trim unused stock TechieFlow, document the kept roster in WORKFLOW.html §14. Q4 — `issuedocs/` deleted, its two BRDs kept archived in `docs/OldDocs/`.
