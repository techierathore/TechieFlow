<!-- ============================================================
     ONE FEEDBACK FILE PER LIBRARY — never a combined file.
     TrBlazeUI and TechieRag are separate codebases with separate
     teams; each team receives (or pulls) only its own file:
       docs/{AppName}-TrBlazeUI-Feedback.md  (issue IDs TR-NNN)
       docs/{AppName}-TechieRag-Feedback.md  (issue IDs TR-RAG-NNN)
     A future library {LibName} gets docs/{AppName}-{LibName}-Feedback.md
     with its own ID prefix (WORKFLOW.html §9.1).
     Create a library's file on the FIRST issue for that library;
     substitute {Library} and the ID prefix below.
     ============================================================ -->

# {Library} Feedback — surfaced during {AppName}

## Summary (filled by /flow-master on consolidation)
- 0 blockers, 0 major, 0 minor, 0 nice-to-have
- Last consolidated: {YYYY-MM-DD}

## Issues

<!-- Append entries as gaps are found. IDs are append-only, never renumbered.
     TrBlazeUI → TR-NNN · TechieRag → TR-RAG-NNN -->

### TR-001 — <short title>
- **Severity:** blocker | major | minor | nice-to-have
- **Repro:** <minimal code snippet>
- **Expected:** …
- **Actual:** …
- **Encountered in:** REQ-UI-013
- **Workaround:** <what you did in app code>
- **Suggested fix:** <hypothesis for the library team>
